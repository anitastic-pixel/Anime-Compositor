# Implementation backlog and task briefs

Version 0.3 | 2026-09-04 | Accepted for baseline

## Backlog rules

All tasks are NOT STARTED. Sequence reflects dependencies, not calendar. Every task attaches a verification artifact per document 12 before it closes. A task without an artifact the owner has reviewed is not done, regardless of test status.

One task per session. The dependency chain is deliberately narrow so that data model and rendering errors surface before an interface hides them.

## G0

B-01 / G0 feasibility. Draw the reference shot per document 22. Run SP-01 save and reopen with interrupted write, SP-03 scrub latency, SP-04 render determinism, SP-05 frame transport into WebView2, SP-06 viewer color exactness. Record toolchain, OS, driver and dependency versions.

Exit: five spike reports with measured numbers, the reference shot in hand, and either confirmation of ADR-003, ADR-004 and ADR-006 or an explicit reopening of whichever they contradict.

Artifact: the spike report table, plus the frames SP-04 compared and the color values SP-06 measured.

## G1-core

B-02 / R-10 / Tagged image buffers, linear-light premultiplied float32 working space, and normal-over compositing math on the CPU. Depends on B-01. No interface.

Exit: T-04 numeric basics and T-09 color and alpha fixtures pass. Artifact: fixture table with expected versus actual to full precision.

B-03 / R-01 / PNG import and sequence manifest, including numeric pattern detection, gap reporting and Unicode paths. Depends on B-02.

Exit: T-01 including the deliberate missing frame and the Japanese filename from the reference shot. Artifact: fixture table plus the diagnostic text the user would see for the missing frame.

B-04 / R-02 / Rational time model and explicit exposure spans. Depends on B-03. This is the central requirement of the product.

Exit: T-02 matches every expected drawing ID and frame range, including the five-frame hold and the one-frame accent. Artifact: the complete 240-row frame-to-drawing table, which the owner checks against the shot as drawn.

B-05 / R-03, R-07 / Model commands, layer order, transforms, hold and linear interpolation, undo and redo. Depends on B-04. Minimal inspection surface only, no real interface.

Exit: T-03 passes and undo restores the model exactly. Artifact: fixture table plus a before, after and undone project JSON the owner can diff by eye.

B-05a / ADR-011 / Tile-based multithreaded render plan across rayon workers. Depends on B-05. Not a separate feature; the renderer is built tiled from the start.

Exit: identical output to single-threaded evaluation, plus measured scaling on the reference machine. Artifact: identical-output proof and a timing table by thread count.

B-05b / ADR-012 / Render trace mode writing intermediate layer buffers as tagged PNGs. Depends on B-05a.

Exit: a trace directory for one composited frame. Artifact: the trace images themselves. This task exists to make every later task diagnosable.

---

B-08 / R-06a / Viewer, frame stepping, work-area playback, resolution selection, and the transport chosen by SP-05. Depends on B-05a. No cache. Playback holds real time and drops the frames it cannot deliver, reporting how many it skipped, per D-32; the preview starts at draft resolution with an always-visible indicator of which resolution is showing, per D-33. DONE. The headless half is B-08a; the headless middle is `src/preview.rs`, which carries the resolution choice, the plan scale and a playback clock driven by an elapsed time the caller supplies, with `verification/B-08_preview_table.md` as its artifact. The window, the transport (D-36's custom URI scheme carrying raw sRGB samples), the wall-clock loop wired to that playback clock, frame stepping, the resolution indicator and the screenshots are in `app/`, with `verification/B-08_window_shell.md` and its three photographs as the artifact. The viewer can now be given a project — named on the command line or dropped on the window — which is B-09's half of it; without one it still opens on the reference shot from a hard-coded path.

Exit: T-06 with measured latency, and preview matching export within declared tolerance. Artifact: screenshots plus a preview-versus-export pixel comparison.

B-09 / R-08 / Versioned persistence, relink, manual save, autosave and recovery. Depends on B-05. Uses atomic replacement verified by SP-01. DONE. The headless half is `src/persist.rs` with `verification/B-09_persistence_table.md` as its artifact, 99 of 99 checks. The viewer half is both directions. A project reaches the window four ways — command-line argument, dropped on the window, the Open dialog, or the recent-projects list — all through the same function, and what the window then says about it is `persist::load`'s own diagnostics: missing media, an effect this build does not have, a file that is not a project at all. It goes back out through `persist::save`, with the shell holding the `Preserved` part of the load and handing it back unread, so a save writes the parts this build does not model as well as the parts it does. The window shows document 26's dirty state and the file it is on, Save As reopens what it wrote, and Ctrl+O, Ctrl+S and Ctrl+Shift+S do what the buttons do. The artifacts are `verification/B-09_open_a_project.md` with five photographs and `verification/B-09_save_table.md`, 17 of 17 checks. Autosave and recovery have now reached the window as well: a timer thread asks every ten seconds whether the project has been unsaved for two minutes, writes a snapshot beside the project when it has, and the window offers the snapshots it finds at startup, newest first. Recovering opens the snapshot as unsaved work against the project on disk, so nothing is overwritten until the person saves. The artifact is `verification/B-09_recovery_table.md`, 26 of 26 checks. What is still unphotographed is the file dialogs, because a script cannot answer one, and the two minutes actually elapsing: the table supplies the clock rather than waiting on it.

Exit: T-07 including interrupted write and disk-full. Artifact: the project files themselves, before and after, plus the recovered file from an interrupted save.

B-10 / R-09 / Immutable export snapshot and PNG sequence writer. Depends on B-08 and B-09. DONE. The renderer half is `src/export.rs`, with `verification/T-08_export_table.md` as its artifact, 47 of 47 checks and six exported frames beside it, and the whole-shot half is `tests/b10_full_shot.rs`, which exports the reference shot twice and requires all 240 pairs to be byte-identical, with `verification/B-10_full_shot_table.md` and `verification/B-10_contact_sheet.png` as its artifacts. The window half is now in as well: an Export button asks where the frames should go and then writes the whole work area there, named for the project and the frame number, at full resolution whatever the preview was showing. The project is cloned when the export is asked for, which is this task's immutable snapshot — editing the open document while the job runs does not reach the job. The job runs off the window's thread, the window says it is exporting and offers to cancel, and cancelling is read between frames. Document 07's default holds: a frame whose drawing is missing blocks the whole export and nothing is written, and the checkbox beside the button is D-28's override, made deliberately in front of a sentence that says what it does. The artifact is `verification/B-10_export_table.md`, 21 of 21 checks. What is unphotographed is the folder dialog, which no script can answer. There is no progress bar, and R-09 does not ask for one.

Exit: T-08 and T-09 with correct range, naming, color, cancellation and error state. Artifact: the exported 240-frame sequence, plus a byte comparison of two consecutive exports proving determinism.

B-11 / R-11, Q-03, Q-04 / Package the offline application, keyboard and display-scaling checks, dependency and license record. Depends on B-08 through B-10. DONE, with two things named rather than quietly left out. The dependency and licence record is `docs/DEPENDENCIES.md` and `Licenses/`, generated from the resolved Windows build and checked in both directions on every CI run, with `verification/B-11_record_table.md` as its artifact. The offline claim has two artifacts because it has two halves: `verification/B-11_offline_table.md` reads the build, 9 of 9 checks, and `verification/B-11_offline_run.md` watches the program run — where the application's own process held no connection and Microsoft's web view held four, which is D-39 and the owner's decision, not a defect in this code. The keyboard, scaling and Unicode checks are five photographs and `verification/B-11_display_and_keyboard.md`; they found three real defects, all fixed. Packaging is `tools/package.ps1`, a portable folder and zip carrying the executable, both licence texts, the manifest and 271 crates' notices, with `docs/SUPPORTED_ENVELOPE.md` inside it as `READ ME FIRST.md` and `verification/B-11_package.md` as its artifact. **Not done: there is no installer** — `bundle.active` stays false because signing a package needs a certificate this project does not have — and the two halves that need a person rather than a script, running the shot with the network adapter actually disabled and a legal review of the licence record.

Exit: T-10, T-15, T-16 and a clean-machine smoke test. Artifact: the installed application running on a clean environment, screenshotted at 100, 150 and 200 percent scaling.

B-12a / ACCEPTED on 2026-09-18 under the owner's delegation (D-70) / The editing window. Depends on B-11. **This unit was proposed rather than agreed when it was written; B-12 was built on it and the owner accepted B-12 on 2026-09-15, so the header now says what happened.** The reason it is here: B-12 asks the owner to complete W-01, and ten of W-01's thirteen steps have no user interface at all. Import, sequence review, creating a composition, assigning exposures, stacking layers, anchors and transforms, applying a matte, one blur, one colour operation and inspecting alpha are all reachable only from a fixture or a test. B-08 through B-11 each built a headless half plus the one window control that half needed, and no backlog item ever scheduled the rest, so the dependency chain reads satisfied while the window is not. Document 05 line 69 sets the bar B-12 is measured against: "complete W-01 without assistance, with all required controls reachable by keyboard." Scope is document 24's command map made reachable — the layer commands, the transform inspector, the effect stack, set and clear a matte, the media bin with sequence grouping and exposure spans, alpha and checkerboard inspection, relink for W-02 — in document 24's five-panel layout, every state change going through a command ID. Not in scope: drawing a mask outline. W-01 says apply a matte, not draw one, and `SetMask` has no gesture a fixture can judge; masks stay a file-level thing until the owner asks otherwise. Exit: every W-01 and W-02 step is reachable in the default layout at 100-200 percent scaling, by keyboard. Artifact: a table per group of commands, photographs of the window, and a mutation report. **BUILT on 2026-09-07, still not formally accepted as a unit** - the owner authorised the work as it went rather than by accepting this entry, and this line stays as written so that is visible. The window is `app/`, and every W-01 and W-02 control named above is in it. The tables are `verification/B-12a_media_table.md`, `verification/B-12a_editing_table.md`, `verification/B-12a_transform_table.md`, `verification/B-12a_matte_table.md`, `verification/B-12a_effects_table.md`, `verification/B-12a_inspect_table.md` and `verification/B-12a_relink_table.md`; the photographs are `verification/B-12a_window_and_keyboard.md`, five of them, retaken on 2026-09-08 because four had gone stale against the build, and `verification/B-12a_alpha_view.md`; the mutation report is the B-12a section of `verification/HARDENING_mutation_report.md`. Drawing a mask outline stayed out of scope as written.

B-12b / The window checked the way the rest of the build is. Depends on B-12a. Not a feature: the B-12a tables each check one group of commands, and this is what checks the things that sit between them - that the page and the window agree about what a route is called (`verification/B-12b_routes_table.md`, `verification/B-12b_command_map_table.md`, `verification/B-12b_state_fields_table.md`, `verification/B-12b_page_table.md`), that a save actually writes (`verification/B-12b_save_works.md`, written because one did not), that the diagnostics a person can provoke are the ones document 28 names (`verification/B-12b_diagnostic_catalogue.md`), that typing coalesces into one undo step (`verification/B-12b_text_coalescing_table.md`), that the decisions the window assumed are written down (`verification/B-12b_provisional_decisions.md`), and that W-01 can be walked end to end by script (`verification/B-12b_w01_walkthrough.md`). It also built the declared performance fixture the moment B-06 and B-07 made it possible: `tests/b12b_declared_fixture.rs` and `verification/T-06_declared_fixture.md`, which closes D-41 and reopens D-40.

B-12c / The last of the window work before the acceptance run. Depends on B-12b. W-02 walked end to end by script (`verification/B-12c_w02_walkthrough.md`); document 05 line 69's keyboard claim checked by reading the page with every mouse-only handler cut out of it, which also names the three gestures that are genuinely mouse-only (`verification/B-12c_keyboard_table.md`); a photograph of the effects panel from a tracked project (`verification/B-12c_effects_panel.png` and `verification/B-12c_effects_shot.json`); the run sheet the owner uses on the day (`verification/B-12_acceptance_run.md`); and the brief for the three decisions the run is waiting on (`verification/B-12c_owner_brief.md`).

B-12d / The one W-01 step with no control. Depends on B-12c. **This unit was not planned; it is the gap B-12c's own run sheet reported.** W-01 lists "creates a composition" third of thirteen and nothing in the build could take that step - document 24 named no `composition.create`, the core had no command behind it, and the composition a person worked in was whichever one their project file already held, so `verification/B-12_acceptance_run.md` recorded step 3 as **not built** and `verification/B-12b_w01_walkthrough.md` recorded it as unwalkable. Scope: `Command::AddComposition` in the core with document 19 line 52's "implementation safety limits" given actual numbers for the first time, `composition.create` in the window on Ctrl+Shift+N, the New composition control on the page, and the row in document 24 with the reasoning beside it. Not in scope: `project.new`. W-01 has no "start a new project" step, a project with no composition would make the viewer's composition optional and branch every frame path, and Ctrl+N is left unbound so document 24's promise to `project.new` still holds. **BUILT on 2026-09-08.** Artifact: `verification/B-12d_new_composition_table.md`, the photograph in `verification/B-12d_new_composition.md`, and the B-12d section of `verification/HARDENING_mutation_report.md`. It also fixed a defect the walkthrough found the moment a second composition became reachable: a saved project reopened on the composition the file happened to list first, which after this command could be an empty one the person had left behind. The fix is a rule of thumb - the first composition with anything in it - because document 07's format has no field for which composition was open, and adding one is a schema change and the owner's. **This is a new capability, not a correction of an omission, and the owner may cut it at B-12.**

B-12 / G1-core acceptance. The owner completes W-01 and W-02 on the reference shot. Fix blocking usability defects. Publish the supported envelope. Depends on B-11, and in practice on B-12a: the acceptance run cannot start until the controls W-01 names exist. **DONE on 2026-09-15**, by the owner. W-01 was walked over four sittings from 2026-09-10 and a final run on 2026-09-15, and W-02 on 2026-09-15; every defect the run raised as blocking was fixed and re-checked by the owner, and the owner's words for each step are in `verification/B-12_run_notes.md`, with the last round of fixes in `verification/B-12_run_fixes.md`. The finished shot is the owner's own 240-frame export. The supported envelope is `docs/SUPPORTED_ENVELOPE.md`, published with the package at B-11. Two things were answered rather than fixed: the matte in step 7, which behaved as document 21 specifies (the matte layer draws itself unless matte only is ticked), and the refusal to export with a missing drawing until the box is ticked, which is the sheet's expected result.

Exit: the owner finishes a shot unaided. Artifact: the finished shot, and the owner's written account of what was awkward.

## G1-rest and beyond

B-06 / R-04 masks and mattes, B-07 / R-05 effects. UNPARKED on 2026-09-06 by the D-12 amendment, after both revisit triggers in document 23 fired and the owner lifted both together. Agreed order is B-06, then B-07, then B-12 / G1. B-06 is specified by document 19 for the data, document 21 for the math and the pipeline position, and document 25 for the cycle fixture `FX-MATTE-001`; it also carried document 25 line 51, the selection and recording of the rasterization rule and its reference tool. **B-06 is DONE on 2026-09-06**, in `src/mask.rs` and the mask and matte steps of `src/compose.rs`, with `verification/B-06_mask_table.md` as its artifact - 41 of 41 checks - and ADR-016 as the record document 25 line 51 asked for, which closes that line's OPEN. It raised D-42 and D-43. **B-07 is DONE on 2026-09-06**, in `src/effects.rs` and step 3 of `src/compose.rs`, with `verification/B-07_effects_table.md` as its artifact - 60 of 60 checks - and ADR-017 as the record of what it found: nothing is tiled at effect time, because the stack runs over the whole layer buffer before the frame plan exists, so ADR-011's tile-margin caution is satisfied vacuously and tile-size invariance at six sizes is what stands in for the comparison R-05 asked for. Document 21 line 89's bounds expansion is declared per effect and checked. It raised D-44, D-45 and D-46. G1-rest is now empty and the next unit is B-12 / G1 acceptance.

B-08b / R-06b bounded preview cache. UNPARKED on 2026-09-05 by D-37, after `verification/B-08_preview_latency.md` fired its trigger. Specified by document 27, recorded by ADR-015, and bounded by D-37 to a cache of decoded source cels with a memory ceiling, reachable from the preview path only and never from export. Its exit artifact is T-06: the same reference-shot playback measured before and after, plus the repeated-loop memory behaviour document 08 asks for. DONE on 2026-09-05, in `src/cache.rs`, with both halves delivered: `verification/B-08b_cache_table.md` for what a cache must never change, `verification/B-08b_cache_budget.md` for what this one is worth. The window-level picture is in as well: `verification/B-08_window_shell.md` was rephotographed after the cache on 2026-09-05 and read 59 frames played and 15 dropped, where the same photograph before the cache read 66 played and 79 dropped. It was retaken again on 2026-09-08 and now reads 42 played and 31 dropped; this entry quoted the 2026-09-05 capture as the current one until 2026-09-16. The artifact attributes the difference to a slower machine-day, measured, and not to the window. Hardened on 2026-09-06 — nine deliberate breaks, six of which the first fixture missed, recorded in `verification/HARDENING_mutation_report.md`.

B-13 / R-12 flat-plane camera and parenting, split by D-56 on 2026-09-15 into three units done in this order, after G1 passed (D-54). **DONE on 2026-09-16, by the owner's word.** The owner walked the playtest sheets for B-13b through B-13e and said B-13 was done. The two defects the B-13e walk found were fixed in 939e4cf, and the camera graph editor the owner asked for was built in 6ed9c42. T-11 is recorded as run and passing in document 11.

B-13a / R-12 specifications and fixtures before code. Parenting is D-57, accepted by the owner on 2026-09-15, with documents 07, 19, 20, 21, 25, 26 and 28, the schema, `tools/parent_reference.py` and `Fixtures/projects/parenting_project.json`; The camera and depth contract is D-58, ACCEPTED by the owner on 2026-09-15, with documents 07, 08, 19, 20, 21, 24, 25 and 28, the schema, `tools/camera_reference.py` and `Fixtures/projects/camera_project.json`. Each is the owner's to accept, and none of B-13b or B-13c starts before its own is accepted.

B-13b / R-12 parenting, built against D-57. Artifact: a fixture table for FX-PARENT-001 to 008, and a playtest sheet for the parent pick list on the layer row. **BUILT on 2026-09-15.** The fixture table is `verification/B-13b_parenting_table.md`, 52 of 52 checks; the window's half is `verification/B-13b_panel_table.md`, 27 of 27 checks; the playtest sheet is `verification/B-13b_parent_playtest.md`. `layer.set_parent` is in document 24 and the pick list is on the layer's inspector beside the matte's. Not in scope and not built: parenting without keeping place, which D-57 leaves undecided and W-04 does not ask for.

B-13c / R-12 camera and depth, built against its accepted contract. Artifact: a fixture table, the generated parallax reference shot, and W-04 walked by the owner for T-11. **BUILT on 2026-09-15.** The fixture table is `verification/B-13c_camera_table.md`, 105 of 105 checks across FX-CAM-001 to 011; the window's half is `verification/B-13c_panel_table.md`, 28 of 28 checks; the playtest sheet is `verification/B-13c_camera_playtest.md`, which is the W-04 walk and is the owner's to perform. `layer.set_depth` and `camera.set_property` are in document 24, the Depth row is on the layer's inspector beside the parent's, and the Camera block is under its own heading because it belongs to the composition. Not in scope and not built: keying a depth or a camera property from the window, dragging the camera in the picture, and a camera that is a layer - each named in D-58 and repeated on the playtest sheet.

B-13d / R-12 a depth that keys, built against D-58 after the owner's playtest of B-13c asked for it. Artifact: a generated table and a playtest sheet. **BUILT on 2026-09-16.** The owner played B-13c on 2026-09-15 and reported that the camera and the parallax work, that the values need pushing hard to read, and that the depth should be "keyable and drag-click like all the other changable values"; the drag was built the same day and the keying is this unit. No new contract: document 24 line 91 already said a depth keys like any other number and document 19 line 25 already said it carries a base and keyframes like every other animatable property, so this is the unbuilt remainder of D-58 rather than a capability the owner has not seen. A depth became one of the property names the command layer takes, which carried it into the timeline's tracks, the graph editor, F9, key navigation, copy and paste and the diamond without any of those being touched. The table is `verification/B-13d_depth_keys_table.md` and the playtest sheet is `verification/B-13d_depth_keys_playtest.md`, which is the owner's to perform. Two defects were found and fixed on the way, neither of them visible from the window until a depth could key: `layer.shift` moved a layer's five transform tracks and left its depth keys behind, and the value the inspector showed came from the transform alone, so a keyed depth would have animated on screen while the panel showed a stale number. Not in scope and not built: keying the camera from the window, which is the owner's chosen "key it where it is" shape and the next unit, and a camera that is a layer, which the owner raised as a question and D-58 leaves as a later contract.

B-13e / R-12 a camera that keys, built against D-58 after the owner's playtest of B-13c asked for it. Artifact: a generated table and a playtest sheet. **BUILT on 2026-09-16.** The owner asked for the camera's values to update as they change, to be keyframable, and to drag-click like every other changeable value; the first was already true and this unit is the other two. The owner chose the shape on 2026-09-16 when asked - key it where it is, the camera staying a property of the composition - and camera-as-layer was deferred. No new contract and no new command: document 24 line 91 said that once a command could name the camera as its target instead of a layer, every property command that already exists would reach it unchanged, so the four commands took a `target` in place of a layer id and the camera came with them - diamonds, tracks, eases, the timeline drag and delete, none of them touched - and F9, the hold and the paste, which were said to arrive the same way and did not. B-13f is that correction. That cost 47 construction sites across 14 files, which is the price the same line names as the reason the table has two camera commands rather than four. The table is `verification/B-13e_camera_keys_table.md` and the playtest sheet is `verification/B-13e_camera_keys_playtest.md`, which is the owner's to perform. One trap was found and closed on the way: the inspector's three camera boxes still sent `camera.set_property`, which sets a base, so on a camera with keys they would have moved a number nothing is drawn from and looked like they had done nothing - they now send `property.set_base` like every other box in the window, and no control sends `camera.set_property` any more. Not in scope and not built: a camera that is a layer, dragging the camera in the picture, and J and K stepping over the camera's keys, which walk the layer list and so do not see a camera that is deliberately not in it.

B-13f / R-12 the camera's keys reached by the gestures that were said to reach them, built after checking B-13e's own claim. Artifact: a contract test in the suite and three corrected sheets. **BUILT on 2026-09-16.** B-13e shipped the sentence "the timeline drag, delete and F9, none of them touched" in this backlog, in document 24, in the generated table's notes and in a playtest step the owner had not yet performed. Two of the three were true. F9, W-20's Ctrl+Alt+G hold and W-20's Ctrl+V paste each built a `layer=` out of the row `keyParts` had just handed them, which for a camera key is the window's own `::camera` name and not a layer id, so the window refused all three; the paste failed a second way, looking its target up with `layers().find` alone, which can never find the camera. Two further defects were found in the same survey and neither was predicted: pressing the name of a camera property on the timeline called `select('::camera')`, which cleared whatever layer the person had chosen and selected nothing, and the playhead did not snap to a camera key. The fix is one id-level namer (`whoseId`) that the row-level one now delegates to, one row lookup (`rowOf`) that `keyParts` already performed and the paste duplicated wrongly, and two guards. The evidence is a text pin in the suite, because the page is not run by it: the body of every function that acts on chosen keys must not spell `layer=` at all. Not in scope and not built, and recorded rather than assumed: the graph editor cannot be pointed at the camera, since it draws the selected layer and the camera is deliberately not selectable - B-13e's notes said it could, which was wrong - and J and K still do not step over the camera's keys. The graph editor half was built later the same day in 6ed9c42, at the owner's request: clicking Camera on the timeline now graphs the camera.

B-14 / R-13 bounded expressions. D-10 left the runtime undecided; D-59, accepted by the owner on 2026-09-16, is the answer. Split on 2026-09-16 into three units done in this order, the way D-56 split B-13.

B-14a / R-13 specification and fixtures before code. **DONE on 2026-09-16: D-59 accepted by the owner ("accept d-59").** D-59 in document 14, the language in document 09, FX-EXPR-001 to 016 in document 25, three identifiers in document 28, the `expression` field in the schema and `property.set_expression` in document 24, with documents 03, 07, 11, 19 and 20 amended. The numbers come from `tools/expression_reference.py`, and `Fixtures/projects/expression_project.json` is the file they are worked on. Artifact: the D-59 entry itself, which the owner has accepted. B-14b may start.

B-14b / R-13 the evaluator, the file and the export refusal, built against D-59. Artifact: a generated table walking FX-EXPR-001 to 016, which is T-12, and a round-trip row for `expression_project.json` in the persistence table. Not in scope: any change to the window beyond the export refusal. **BUILT on 2026-09-16.** The fixture table is `verification/B-14b_expression_table.md`, 184 of 184 numbers and diagnostics matching document 25, with the picture and export checks after it: a failing expression is drawn at its keyed value and reported as an error, and an export whose range meets one writes nothing under either missing-drawing policy and names the frames. The round-trip row is in `verification/B-09_persistence_table.md`. The window needed no change for the refusal: it already prints "Nothing was exported." followed by the reason. Two limits are known. `thisComp.layer("camera")` looks for a layer called camera, where the Python reference reaches the composition camera; no fixture asks for it. And a parent change keeps the child where it is using the parent's keyed rotation, not its expression. `property.set_expression` is not built; that is B-14c. B-14c may start. **DONE on 2026-09-16, by the owner's word** ("works"), after walking `verification/B-14b playtest/`.

B-14c / R-13 expressions in the window: a text box on each property that can carry one, a switch, and the error shown beside the property that has it. Artifact: a panel table and a playtest sheet, the owner's to perform. **BUILT on 2026-09-16.** `property.set_expression` is built, and the panel table is `verification/B-14c_panel_table.md`, 18 of 18. As the owner asked on 2026-09-16, Alt-click on a property's diamond adds an expression (or removes one), as After Effects' stopwatch does; Alt+Shift+= on a focused diamond does the same. The box appears under the property in the inspector, the Camera panel and the timeline, and the `=` beside the property's name switches it off and on. W-23's Alt-click, which took every key off a property, is replaced; pressing the property's name on the timeline and then Delete still does that. **DONE on 2026-09-16, by the owner's word** ("B-14C playtest works"), after walking `verification/B-14c_expressions_playtest.md`. With it B-14 is done, and the owner passed G2 the same day (D-60).

B-15 / R-14 collect and package, entered on 2026-09-16 by the owner's decision (D-60) and split, the way B-14 was, into three units done in this order.

B-15a / R-14 specification and fixtures before code. D-61 in document 14; FX-PACK-001 to 006 in document 25, generated by `tools/package_reference.py` from the shot in `Fixtures/packaging/source/`; `asset.set_redistribute`, `project.collect` and `package.check` in document 24; five identifiers in document 28; `redistribute` in document 19 and the schema. Artifact: those, for the owner to accept or change. **DONE on 2026-09-16: D-61 accepted by the owner ("accept d-61").**

B-15b / R-14 collecting, the manifest, the hash and checking, built against D-61. Artifact: a generated table walking FX-PACK-001 to 006, which is T-13, including the package moved to a second folder, opened there and its frames compared with the source's. Not in scope: the window. **BUILT on 2026-09-16.** The table is `verification/B-15b_package_table.md`, 30 of 30 checks passing: the manifest is the fixture's byte for byte, the package moved into a folder named with a space and Japanese opens there with the same two missing drawings and draws the source's frames, the six check situations give document 25's answers, the refusals leave nothing behind, and collecting leaves the open project, its history, its unsaved state and its file alone. SHA-256 is the build's own, `src/sha256.rs`, and gives FIPS 180-4's four published digests. The three commands are not in the window yet; that is B-15c. B-15c may start.

B-15c / R-14 the window: File > Collect Files..., File > Check Package and the "Can be passed on" tick. Artifact: a panel table and a playtest sheet, the owner's to perform. **BUILT on 2026-09-16.** The panel table is `verification/B-15c_panel_table.md`, 19 of 19. The window has no menu bar, so **Collect Files...** and **Check Package** are buttons beside Save As. Collect Files asks for a folder and says how many drawings were copied, missing and left out; the manifest it writes is the fixture's byte for byte and the window is left as it was. Check Package counts each answer on the status line and lists every file that is not `ok`, by its place in the package, in the notes. Each drawing in the Project panel has a **Can be passed on** tick, which is `asset.set_redistribute`, an undoable command. Collecting runs while the window waits, with no progress bar; the fixture shot takes an instant. The playtest found that Check Package looked beside the original file after Save As; that was fixed the same day, with a row of its own in the table. **DONE on 2026-09-17, by the owner's word** ("B-15c playtest works"), after walking `verification/B-15c_package_playtest.md`. With it B-15 is done.

B-16 / R-15 additional formats, one at a time, EXR first by the owner's choice (D-60), split, as B-15 was, into three units done in this order. WAV and other audio are deferred by the owner on 2026-09-17 (D-63), so B-16 is EXR only.

B-16a / R-15 EXR specification and fixtures before code. D-62 in document 14; FX-EXR-001 to 010 in document 25, generated by `tools/exr_reference.py` into `Fixtures/exr/`; `MEDIA_EXR_ADJUSTED` in document 28; the EXR section of document 21. Artifact: those, for the owner to accept or change, including the choice of the `exr` crate. **DONE on 2026-09-17: D-62 accepted by the owner ("accept d-62").**

B-16b / R-15 the EXR reader and writer, built against D-62, with the dependency recorded. Artifact: a generated table walking FX-EXR-001 to 010, which is T-14 for EXR, whose export rows quote `tools/exr_reference.py check`. Not in scope: the window. **BUILT on 2026-09-17.** The table is `verification/B-16b_exr_table.md`, 71 of 71 checks passing: every drawn file matches OpenEXR's pixels exactly, all ten compressions included, each refused file gives its reason, the adjusted files give `MEDIA_EXR_ADJUSTED` with D-62's reasons, the sequence with a gap imports as the PNG one does, and the four exported files read back as written and come out byte for byte the same twice. OpenEXR's own check of those files is `verification/B-16b_exr_export_check.md`, 60 of 60. The window still offers PNG only until B-16c.

B-16c / R-15 EXR in the window: `.exr` in the import and relink dialogs, and the format and sample choices in the export dialog. Artifact: a panel table and a playtest sheet, the owner's to perform. **BUILT on 2026-09-17.** The panel table is `verification/B-16c_panel_table.md`, 18 of 18: a single EXR imports as a still read as linear light with premultiplied alpha, a file with odd values is added and noted with `MEDIA_EXR_ADJUSTED`, a refused file is not added and says why, the EXR sequence imports with its gap, relinking between PNG and EXR says first that the colour reading changes, and the format list beside Export writes PNG, EXR half or EXR float. The playtest sheet is `verification/B-16c_exr_playtest.md`. **DONE on 2026-09-18** by the owner's word ("pass the b-16c, it worked").

B-17 / adjustment layers, entered on 2026-09-17 by the owner's decision (D-65), split, as B-16 was, into three units done in this order. The exposure list's overhaul and anime-specific processing wait (D-64).

B-17a / adjustment layer specification and fixtures before code. D-66 in document 14; FX-ADJ-001 to 013 in document 25, generated by `tools/adjust_reference.py` into `Fixtures/adjust/`; the Adjustment layers section of document 21; the `adjustment` kind in document 19; `layer.add_adjustment` in document 24. Artifact: those, for the owner to accept or change. **Written on 2026-09-17 and accepted by the owner the same day.** The schema gains the kind in B-17b, with the reader.

B-17b / the adjustment layer in the core, built against D-66: the kind read and written, the frame drawn in segments, the trace naming the adjusted frame. Artifact: a generated table walking FX-ADJ-001 to 013, including a tiled render against an untiled one. Not in scope: the window. **Built on 2026-09-17:** `verification/B-17b_adjust_table.md`, 46 of 46, from `tests/b17b_adjust.rs`: all thirteen cases within 1e-6 of `Fixtures/adjust/expected_adjust.json`, tiled and untiled byte-identical, the file, the commands, the draft preview and the trace.

B-17c / the adjustment layer in the window: the button, Ctrl+Alt+Y, its row on the timeline and its effects in the inspector. Artifact: a panel table and a playtest sheet, the owner's to perform. **BUILT on 2026-09-17.** The panel table is `verification/B-17c_panel_table.md`, 11 of 11: New adjustment layer needs no drawing chosen, lands above the chosen layer or else at the front, covers the frame and runs the whole composition, takes an effect, refuses a blend mode with the reason, and comes back on Undo as an adjustment layer with its effect. The playtest sheet is `verification/B-17c_adjust_playtest.md`. **DONE on 2026-09-18** by the owner's word ("B-17c playtest works").

B-18 / precompositions, entered on 2026-09-18 after B-17 (D-65), split as B-17 was into three units done in this order.

B-18a / precomposition specification and fixtures before code. D-67 in document 14; FX-PRE-001 to 015 in document 25, generated by `tools/precomp_reference.py` into `Fixtures/precomp/`; the Precompositions section of document 21; the `composition` kind in document 19; layer-local time of a composition layer in document 20; `layer.add_composition` and `layer.precompose` in document 24; `COMPOSITION_CYCLE` and `COMPOSITION_REFERENCE_MISSING` in document 28; invalidation in document 27. Artifact: those, for the owner to accept or change. **Done: written on 2026-09-18 and accepted by the owner the same day ("proceed").** The schema gains the kind in B-18b, with the reader.

B-18b / the precomposition in the core, built against D-67: the kind read and written, the inner composition rendered as a layer's source at its local frame, the cycle refused, the missing composition reported, `layer.add_composition` and `layer.precompose`, `composition.delete` refused while used, the draft preview at the draft divisor, the trace naming the nested frame. Artifact: a generated table walking FX-PRE-001 to 015, including a tiled render against an untiled one, a composition layer with a blend mode against a raster layer of the same picture, and Inner rendered alone against its picture inside Main, with a camera. Not in scope: the window. **Built on 2026-09-18:** `verification/B-18b_precomp_table.md`, 70 of 70, from `tests/b18b_precomp.rs`: all fifteen cases against `Fixtures/precomp/`, fourteen within 1e-6 and FX-PRE-015 refused as `COMPOSITION_CYCLE`, tiled and untiled byte-identical, the blend modes against a drawn twin, Inner alone against Main with a camera, the file, the commands with pre-compose as one undo, the draft preview and the trace.

B-18c / the precomposition in the window: the Project panel's button, Pre-compose and Ctrl+Shift+C, the row's mark and the inner composition's name on its bar, double-click to open, the inspector. Artifact: a panel table and a playtest sheet, the owner's to perform. **BUILT on 2026-09-18.** The panel table is `verification/B-18c_panel_table.md`, 14 of 14: Pre-compose with nothing chosen and on half of a parent pair is refused with the reason, on both it leaves one centred composition layer and a new composition Precomp 1, a used composition is not deleted, a composition is not put inside itself, the Project panel's button adds a second layer, and Undo takes Pre-compose back as one entry with Redo staying in the outer composition. The playtest sheet is `verification/B-18c_precomp_playtest.md`. **DONE on 2026-09-18** by the owner's word ("B-18c playtest works").

B-19 / the graph editor's gaps, after B-18 (D-65). Most of the graph is built (D-52, W-series); the owner names what is missing when B-19 starts. **Named by the owner on 2026-09-18** ("all 10 please; along with adding animation/keyframing stopwatches to effects as well"), from the agent's list of ten: (1) drag a key's value in the graph, (2) box selection in the graph, (3) type a key's value or frame, (4) vertical zoom and pan and fit to the selection, (5) numbered value grid lines, (6) several properties in the graph at once, (7) separate X and Y handles for position, (8) snap a dragged key to the playhead and to other keys, (9) effect settings in the graph, (10) auto-bezier, continuous bezier and roving keys; and keyframed effect settings with a stopwatch. Split by what needs a decision first:

B-19a / the graph's window gaps that change no file and no evaluation: items 1, 2, 3, 4, 5 and 8. Artifact: a panel table and a playtest sheet, the owner's to perform. **BUILT on 2026-09-18.** A dot dragged in the graph changes its key's value as well as its frame, Shift holds it to one way, it snaps to the playhead, to other properties' keys and to other keys' values unless Ctrl is held; a drag on the open graph draws a box that chooses keys; a double-click or the key menu opens a box to type a key's frame and value; the wheel zooms the values, the middle button slides them and Fit closes in on the chosen keys; round numbered lines cross the graph. The panel table is `verification/B-19a_panel_table.md`, 11 of 11. The playtest sheet is `verification/B-19a_graph_playtest.md`, which the owner played on 2026-09-19: it works.

B-19b / keyframed effect settings, specification and fixtures before code: a decision in document 14, the parameter as a property in document 19, its evaluation in documents 20 and 21, the cache key in document 27, fixtures in document 25. Artifact: those, for the owner to accept or change. **WRITTEN on 2026-09-18, waiting on the owner:** D-68 in document 14, FX-FXK-001 to 009 in document 25 from `tools/fxkey_reference.py`, with their projects in `Fixtures/fxkey/`. **D-68 ACCEPTED by the owner on 2026-09-18 ("proceed"),** with FX-FXK-009 corrected the same day to D-46's rule: kept, bypassed and warned, not refused.

B-19c / keyframed effect settings in the core, against that decision. Artifact: a generated table walking its fixtures. **BUILT on 2026-09-18.** An effect instance holds the keys of each setting that has any (`src/effects.rs`), a frame is planned with every setting's value at that composition frame held inside its range, so the bounds and the effect cache's key are that frame's numbers, the file reads and writes a keyed setting as a property record and an unkeyed one as the plain number it was, a value outside its range in a file is kept, bypassed and warned of per D-46, moving a layer in time moves its effects' keys, and one new command, `SET_EFFECT_KEYS`, replaces every key of one setting as one entry to undo and refuses a value outside the range. The table is `verification/B-19c_fxkey_table.md`, 47 of 47, generated by `tests/b19c_fxkey.rs`. Left to B-19d, because they live in the window's command layer: `effect.set_parameters` making a key at the playhead on a keyed setting, and the keyframe commands and key clipboard taking an effect's setting.

B-19d / keyframed effect settings in the window: the stopwatch on each setting, its keys on the timeline, and the setting in the graph (item 9), with several properties in the graph at once (item 6). Artifact: a panel table and a playtest sheet. **BUILT on 2026-09-18, the owner's playtest awaited.** No request and no core command was added. The page names a setting `fx:<instance>:<setting>` wherever it names a property, and the window turns `keyframe.add_remove`, `keyframe.move`, `keyframe.set_interp`, `property.set_base` and `property.drag_update`, in their single form and as repeated `key=`, into `SET_EFFECT_KEYS` by reading the setting's keys, changing them and replacing them; a value on a setting with no keys is still `SET_EFFECT_PARAMETERS`. `/curve` and the values of `/boxes` answer for a setting the same way. So the stopwatch, the timeline row, dragging, F9, Delete, copy and paste and everything of B-19a reach a setting through the code that was there. Item 6 is an **All** button in the graph that draws the layer's other keyed properties dashed and grey, and a press on one makes it the one being edited. The table is `verification/B-19d_panel_table.md`, 21 of 21; the sheet is `verification/B-19d_fxkey_playtest.md`, which the owner played on 2026-09-19: it works. Ceilings, on the sheet: a setting has a timeline row only once keyed, effect keys paste only onto a layer holding that effect instance, and the grey lines share the edited property's scale.

B-19e / separate dimensions for position (item 7), and auto-bezier, continuous bezier and roving keys (item 10), specification and fixtures before code, since D-52 gives a key one ease for all its components and none of the three kinds exists in document 19. Then the core and the window, as B-19f and B-19g. **B-19e WRITTEN on 2026-09-18, waiting on the owner:** D-69 in document 14, and FX-SEP-001 to 004, FX-KIND-001 to 007 and FX-ROVE-001 to 004 in document 25 from `tools/keykind_reference.py`. **D-69 ACCEPTED by the owner on 2026-09-18 ("proceed").**

B-19f / the core for D-69. Artifact: a generated table walking its fixtures. **BUILT on 2026-09-18.** `src/keykind.rs` holds the rules: the speeds either side of a key, what an auto and a continuous key keep true, where a roving key falls, and a position taken apart and joined. A key carries `kind` and `roving`, and a position may hold X and Y as two properties (`src/model.rs`), read and written as D-69 says and refused where it says (`src/persist.rs`). The commands are `SET_KEY_KIND`, `SET_KEY_ROVING` and `SEPARATE_POSITION`, each one entry to undo, and setting, moving or removing a key settles the kinds and places the roving keys again. The two halves are named `position_x` and `position_y` wherever a property is named, and an expression may sit on either. `verification/B-19f_keykind_table.md`: **36 of 37.** **The one that does not match is a fault in the fixture, for the owner to decide.** FX-KIND-005 says "2 a frame in and 1 a frame out become 1.5", and its `after` is that. Its `before` in the file does not have 2 a frame in: the first segment is already eased to 1.5, because `tools/keykind_reference.py` worked `after` on the same keys before it copied `before`. D-69 applied to the keys as written gives 1.25. With the first key linear, as the sentence says, the build gives the fixture's `after` to 1e-9, and the table shows both rows. The same slip is in the `before` of FX-KIND-001, 003 and 007, where it changes nothing because an auto key does not read the speed it had. **Proposed correction:** `settle` in the reference copies its keys before changing them, and the four `before` lists are regenerated with a linear first key; no `after` changes. **Done on 2026-09-19 by the owner's word ("fix the fault in fixture"), in a commit of its own before the test changed; the table is 36 of 36, the extra row that showed the sentence's keys having gone.** **Known ceilings:** several key moves sent as one edit are each checked as they land, so a run of roving keys can refuse a state half way; a roving key holds its frame, so a move onto it is refused; unknown fields inside `x` and `y` are not carried through a save; an expression cannot name `position_x`, though `position[0]` reads it; keeping a layer in place under a new parent is refused while its position is separated. The three request ids, `property.separate`, `keyframe.set_kind` and `keyframe.set_roving`, enter document 24 with B-19g, which is when the window can answer them.

B-19g / the window for D-69: the kinds and Rove Across Time in the key menu, the key shapes, Separate Dimensions, and X Position and Y Position as rows of their own. Artifact: a panel table and a playtest sheet. **BUILT on 2026-09-18.** The window answers `property.separate`, `keyframe.set_kind` and `keyframe.set_roving`, which are in document 24's table, and names `position_x` and `position_y` wherever it names a property, so the stopwatch, the rows, the graph, F9, delete and paste reach the two halves through the requests that were there. `position` is still answered on a separated layer: the window sends it on as its X and its Y inside one drag, so the viewer's handles, the arrow keys and a typed pair move the layer as they did and are one entry to undo. The page draws X position and Y position as rows of their own, a button beside Position that separates and joins, four lines in the key menu, a round key for auto bezier and a small round key for roving. `verification/B-19g_panel_table.md`: 24 of 24. **The owner played `verification/B-19g_keykind_playtest.md` on 2026-09-19: "B-19g playtest works".** **Known ceilings:** no motion path is drawn for a separated position, and Alt+Shift+P on one is refused with a message; the kinds and roving are for a layer's own properties, not the camera's or an effect setting's; an expression's failure on X or Y is shown on Position's name in the window's answer. With this, all ten of the owner's graph items and the effect stopwatches are built, and **B-19 is DONE on 2026-09-19:** the owner played B-19a, B-19d and B-19g and each works, and FX-KIND-005 was corrected by the owner's word the same day.

B-20 / R-15 reference audio, taken up by the owner on 2026-09-19, split as B-16 was.

B-20a / the rule and its fixtures before code: D-71 in document 14, ADR-018 in `docs/adr/`, FX-AUD-001 to 035 in document 25, generated by `tools/audio_reference.py` into `Fixtures/audio/`. **WRITTEN and ACCEPTED by the owner on 2026-09-19 ("Proceed").**

B-20b / the core for D-71: the audio asset and layer in the model and the file, the time arithmetic, the WAV header reader, `SET_AUDIO_GAIN`, the refusals, relink and collect. No new dependency. Artifact: a generated table walking FX-AUD-001 to 035, which is T-14 for audio. **DONE on 2026-09-19:** `src/audio.rs`, the audio asset and layer in the model, the file and the schema, `SET_AUDIO_GAIN`, the refusals, and the two diagnostics in document 28. `verification/B-20b_audio_table.md` is 46 of 46, from `cargo test --test b20b_audio`. No new dependency. Nothing is heard yet; that is B-20c.

B-20c / audio in the window: audio files in the import and relink dialogs, playback and one-frame scrub through the window's decoder, the speaker switch and the level, the waveform bar, and the bar styles for the adjustment layer, the composition layer and the camera. Artifact: a panel table and a playtest sheet, the owner's to perform, because sound and sync are judged by ear. After B-20b. **DONE on 2026-09-19 by the owner's word ("audio works now, along with the new design look of the layer bars on the timeline"),** after walking `verification/B-20c_audio_playtest.md`. `verification/B-20c_panel_table.md` is 20 of 20. The owed `cargo test --workspace` run was made the same day: it found `layer.set_gain` listed out of document 24's order in the B-12b contract list, that was corrected, and the run is 112 passed, 0 failed. B-20 is done.

B-21 / more formats in and out, asked for by the owner on 2026-09-19, split as B-16 and B-20 were.

B-21a / the rule and its fixtures before code: D-72 in document 14, FX-FMT-001 to 030 in document 25, generated by `tools/formats_reference.py` into `Fixtures/formats/`. **WRITTEN on 2026-09-19; D-72 ACCEPTED by the owner the same day, with B-21b, B-21c and B-21d told to proceed. None of the three is started: the session that took the acceptance had no working shell and could not compile.**

B-21b / drawings in: BMP, TGA, TIFF, WebP and JPEG through the reader D-72 names, in the core and in the import and relink dialogs. Artifact: a table that walks FX-FMT-001 to 010, each file against its twin. After B-21a is accepted. **BUILT on 2026-09-19:** the `image` crate as D-72 names it, eleven crates under it, all in `docs/DEPENDENCIES.md` and `Licenses/`. `verification/B-21b_formats_table.md` is 11 of 11 from `cargo test --test b21b_formats`: the eight exact formats differ from their twins by 0, the JPEGs by 2 and 1 of an allowed 3, the lossy WebP by 0. `verification/B-21b_panel_table.md` is 13 of 13. **One open point for the owner:** D-72 says a 16-bit TIFF is read down to 8 and said so, which needs a diagnostic D-72 did not write; until decided, a file deeper than 8 bits is refused with MEDIA_UNSUPPORTED_FORMAT, as a 16-bit PNG is. **DONE on 2026-09-19 by the owner's word ("B-21b playtest works, keep the refusal"),** after walking `verification/B-21b_formats_playtest.md`. The refusal of files deeper than 8 bits stands, and D-72 is amended to say so.

B-21c / GIF and animated PNG out: the format choice on the export request and in the export dialog, the two writers, the delays of FX-FMT-020 to 023. Artifact: a table, and the two files of a fixture project for the owner to open in a browser. After B-21b. **BUILT on 2026-09-19; DONE the same day, the owner's playtest passed ("exporting worked very well").** `verification/B-21c_films_table.md` is 18 of 18: the delays of FX-FMT-020 to 023, then eight frames of the reference shot written as a GIF and as an animated PNG and read back, the GIF with delays `4 4 4 4 4 5 4 4`, the animated PNG frame for frame value-exact against the PNG sequence the same export writes, and a cancelled GIF leaving no file. The two files are from the reference shot and not from `cel_holds_project.json`, whose drawings are not on the disk. `verification/B-21c_panel_table.md` is 8 of 8: the list beside Export offers both, the GIF called a preview, one file named after the project, and the status line says so. Decided by the agent under the owner's "refuse or accept with your recommendation": a GIF pixel is there when its alpha is half or more; an animated file that is cancelled or fails is removed, not left half made; an animated PNG refuses a frame rate whose numerator or denominator is above 65535; the window keeps its folder picker and names the file itself. How close a GIF's 256 colours must come is set by no document; the table measures it and the owner's eye judges it. New crates: `gif` and `color_quant`, in the licence record and both B-11 guard lists. Awaits the owner's playtest, `verification/B-21c_films_playtest.md`.

B-21d / MP4 out. **BUILT on 2026-09-19 on the road D-72 recommends, and DONE the same day: the owner closed D-30 ("road 1, Media Foundation confirmed") and passed the playtest.** `verification/B-21d_mp4_table.md` is 16 of 16 from `cargo test --test b21d_mp4`: the three rows of FX-FMT-030 read back out of the written files' bytes, six BT.709 colour rows, the three odd-size refusals, 22 frames of the reference shot as `verification/B-21d film/reference_shot.mp4`, and a cancelled MP4 leaving no file. `verification/B-21d_panel_table.md` is 5 of 5. `windows` is now a direct dependency of the core, on Windows only, in both B-11 guard lists and `docs/DEPENDENCIES.md`; it was already in `Licenses/` under Tauri. The agent's decisions are in D-72. The owner's playtest, `verification/B-21d_mp4_playtest.md`, passed on 2026-09-19 ("b-21c and b-21d can be passed for playtest"). Artifact: a table for FX-FMT-030 read back from the written file, and a film the owner plays. After B-21c.

B-21e / MP4 out on macOS and Linux, through an ffmpeg the user has installed: D-30's second road, chosen by the agent on the owner's word ("choose what is recommended and proceed with road along with road 1 as well"). Nothing is shipped and there is no new dependency: the program starts `ffmpeg` and hands it the same BT.709 frames. **BUILT on 2026-09-19.** `verification/B-21d_mp4_table.md` is 19 of 19: the three rows of FX-FMT-030 again, written through ffmpeg and read back, 24000/1001 exact. The file carries the BT.709 label, which the Windows road's does not. **What cannot be shown: the rows ran on Windows, where the tests run. This program has never been built or run on macOS or Linux, by anyone, and nothing here says it would start there.** With no ffmpeg found, the export is refused with a sentence that says to install it. Ceiling: it asks ffmpeg for `libx264` by name, which the usual macOS and Linux builds have.

B-22a / export choices, the rule and its fixtures before code: D-73 in document 14, FX-FMT-040 to 060 in document 25, from `tools/formats_reference.py`. **WRITTEN on 2026-09-19 at the owner's word ("write D-73"); D-73 ACCEPTED by the owner the same day ("accept D-73, proceed with B-22b and B-22c").**

B-22b / the core: the three MP4 qualities on both roads, GIF dithering, the BT.709 label. Artifact: a table that walks FX-FMT-040 to 060, the label read back from the written file. After D-73 is accepted. **BUILT on 2026-09-19: `verification/B-22b_choices_table.md` is 20 of 20** (`cargo test --test b22b_choices`): the twelve rows of FX-FMT-040, FX-FMT-050 to 052 to the pixel, and FX-FMT-060 read back from the file's `colr` box on both roads. The fixture caught the ffmpeg road writing "unspecified" for primaries and transfer; the road was mended, the fixture untouched. The reference shot is exported each way into `verification/B-22 films/` (not kept in git, several megabytes). Seen by the agent in `dithered.gif`: the banding in soft greys is gone, and in its place a few stray coloured specks, which is the accepted rule carrying its error against a palette of 256; it goes on B-22c's playtest sheet for the owner's eye, and mending it would be an amendment to D-73, not a repair.

B-22c / the window: the quality list and the dither tick box, shown only beside the format they belong to; the list says which entries are lossless. Artifact: a panel table and a playtest sheet with a High and a Preview film of the same shot side by side. After B-22b. **BUILT on 2026-09-19: `verification/B-22c_panel_table.md` is 13 of 13.** The page sends `quality=` with an MP4 and `dither=on` with a GIF and nothing with the others; a quality it does not offer is refused in a sentence; the status line names what was chosen; both are kept for the sitting as the format is. Not reachable by a test and said so in the table: the line that hands the choice to the job sits behind the folder dialog. **DONE on 2026-09-19 by the owner's playtest ("B-22c playtest passes"), `verification/B-22c_choices_playtest.md`**, with the side-by-side pictures in `verification/B-22c playtest/`.

B-22d / the specks in a dithered GIF: D-73a in document 14, FX-FMT-053 to 055 in document 25, the rule and its fixtures before code. **WRITTEN on 2026-09-19 at the owner's word; D-73a ACCEPTED the same day ("accept D-73a, proceed with B-22d"). BUILT the same day: two clamps in `dither_indices`; `verification/B-22b_choices_table.md` is 20 of 20, now walking FX-FMT-053 to 055. The build's own `dithered.gif` beside B-22c's: `verification/B-22d playtest/plain_before_after.png`; the agent sees no specks in it and the sky's bands still gone, as its Python trial showed. Awaits the owner's eye, `verification/B-22d_specks_playtest.md`.** Evidence to judge it by: `verification/D-73a proposal/as_accepted_against_proposed.png`. After acceptance: the two steps in `dither_indices`, the table, a new `dithered.gif`. **The owner passed it on 2026-09-23**, all three steps, the third on their own export of the reference shot ("I think it worked well for a gif"). The agent found 49 pixels that stand out sharply from the smooth patch around them, across that export's 14 frames. Every one is in the pink tree, on the source painting's own darker flecks, drawn in the palette's nearest shade. There were none of B-22c's red dots.

Named by the owner on 2026-09-19, after B-22d: a first build on macOS or Linux is deferred ("defer 3"); the exposure list overhaul and anime-specific processing (D-64) stay later ("1 and 2 will be later"). Asked about next: solid layers, shape layers, and masks with the pen tool and feather. The plan was put to the owner and accepted the same day ("go, yes to all three; proceed with B-23a"): D-75 in document 14 records the three answers.

B-23 / solid layers, asked for by the owner on 2026-09-19, split as B-17 was.

B-23a / the rule and its fixtures before code: D-74 in document 14, FX-SOL-001 to 008 and 020 to 029 in document 25, from `tools/solid_reference.py` into `Fixtures/solid/`; every expected frame drawn large, with its sentence, in `verification/B-23a proposal/solid_cases.png`. **WRITTEN on 2026-09-19 at the owner's word; D-74 ACCEPTED the same day ("accept D-74, proceed with B-23b").**

B-23b / the core: the `solid` kind in the model and the file, its picture as step 1, the ten refusals, `layer.add_solid` and `solid.set` with their history. Artifact: a table that walks FX-SOL-001 to 029. **BUILT on 2026-09-19: `verification/B-23b_solid_table.md` is 34 of 34 - the eight frames within 1e-6 and the same in tiles of one pixel, the ten files refused with where and why, the file written back in the fixture's order and opened again equal, and the two commands with their refusals and one undo. Nothing in the window yet; B-23c is next.**

B-23c / the window: New Solid in the Layer menu (Ctrl+Y), the colour picker and the width and height in the layer's panel, the two commands in document 24's table. Artifact: a panel table and a playtest sheet. After B-23b. **BUILT on 2026-09-19: `verification/B-23c_panel_table.md` is 18 of 18; the window has no Layer menu, so New solid is a button beside New adjustment layer, a line in the timeline's right-click menu and the palette, and Ctrl+Y. **DONE on 2026-09-19 by the owner's playtest ("B-23c playtest works"), with one change asked for the same day and built: Width and Height are dragged, typed or stepped with the arrow keys like every other number in the window, one drag one step of history. The owner's second question, whether the colour and size could be keyed, is D-76, proposed and awaiting them; nothing is built against it. The owner declined D-76 on 2026-09-23 ("i agree with not keyframing it").**

B-24 / masks (D-75): B-24a the rule and its fixtures - curved paths, where today's polygon masks keep their pixels exactly; several masks a layer, each with mode (Add, Subtract, Intersect, Difference, None), invert, opacity, even feather and expansion; the mask path animated. B-24b the core. B-24c still masks in the window: the pen, rectangle and ellipse tools, moving points and handles, the masks as timeline rows. B-24d animated paths, with stopwatches. Point-by-point feather is later, its own unit. After B-23. **B-24a is written on 2026-09-19: D-77 in document 14, with FX-MSK-001 to 019 (frames) and FX-MSK-020 to 030 (refused) in document 25 and `Fixtures/masks/`, from `tools/mask_reference.py`, drawn in `verification/B-24a proposal/mask_cases.png`. The owner accepted D-77 on 2026-09-19 ("accept D-77, proceed with B-24b, along with shortcut for the pen tool"). B-24b, masks in the core, is built the same day: `verification/B-24b_mask_table.md`, 53 of 53, walking every fixture, the file, the commands and the rule itself. B-24c, masks in the window, is built on 2026-09-20: the pen (G), the rectangle and the ellipse (Q), the selection tool back on V, points and handles dragged or nudged with the arrow keys, every mask a row on the timeline and a block of settings in the layer's panel, and four commands in document 24's table - `mask.add`, `mask.set_path`, `mask.set` and `mask.delete`. The checkerboard moved from G to Alt+G to give the pen its key. `verification/B-24c_panel_table.md` is 17 of 17; it awaits the owner's playtest, `verification/B-24c_mask_playtest.md`. B-24d, the path set moving, is built the same day: D-77 already stated the rule, so it needed no new decision, only fixtures first - FX-MSK-031 to 035 in document 25 and `Fixtures/masks/`, five frames each, drawn in `verification/B-24d keys/mask_key_cases.png` - then the core (`verification/B-24d_mask_key_table.md`, 39 of 39) and the window: a stopwatch and a key track on every mask row, `mask.add_remove_key` in document 24's table, and a point moved on a moving path written to the key under the playhead (`verification/B-24d_panel_table.md`, 9 of 9). The `PROJECT_FEATURE_UNSUPPORTED` warning on a keyed path is retired, because the build draws it now. **The owner passed it on 2026-09-22**, all ten steps of `verification/B-24d_mask_playtest.md`, on the build P-15 left: the stopwatch, a second key set by moving a point, the shape travelling between them, a handle growing out of the curve, undo, going to a key, the stopwatch taken off with the shape left where it was, a save and reopen with no diagnostic, an export cut frame by frame, and a second unkeyed mask beside the first. The sheet's own two known limits were struck through the same day: B-24e and B-24f had lifted them two days after it was written. B-24e, on 2026-09-22, lifts B-24d's two limits for a mask's key and a shape's key alike: a key is dragged along its row, eased with F9, Shift+F9 and Ctrl+F9 and held with Ctrl+Alt+G, through `mask.set` and `shape.set` naming the key by its frame, with no new command. `verification/B-24e_panel_table.md`, 13 of 13; **the owner passed `verification/B-24e_path_key_playtest.md` on 2026-09-23** ("works"), all ten steps, with step 7 read as B-24f rewrote it - a key dropped on another is refused rather than merged. B-24f, the same day, lets a path key be chosen with other keys, moved by the arrow keys, deleted, copied, pasted and right-clicked, and draws it in the graph editor as the distance the path has travelled, through the keyframe requests with the path named `mask:<n>` or `shape:<n>`; a released hold brings back the ease it replaced. `verification/B-24f_panel_table.md`, 16 of 16; **the owner passed `verification/B-24f_path_key_playtest.md` on 2026-09-23** ("works"), all ten steps, the graph editor included.**

B-25 / shape layers (D-75, document 04 amended): rectangle, ellipse and pen paths, fill and stroke, several shapes a layer; gradients, trim paths, repeaters and merge paths out at first. B-25a the rule and its fixtures first. After B-24. **B-25a is written on 2026-09-20: D-78 in document 14, the shape layer proposed with its fixtures first - FX-SHP-001 to 017 (frames) and FX-SHP-020 to 030 (refused) in document 25 and `Fixtures/shapes/`, from `tools/shape_reference.py`, the still ones drawn in `verification/B-25a proposal/shape_cases.png` and the moving one in `verification/B-25a proposal/shape_moving_case.png`. It says what a shape layer is: no width or height of its own, its space the composition's; a list of shapes, each a D-77 path that may be open, with an optional fill and an optional stroke; the stroke every point within half its width of the path, which makes joins and caps round by definition; rectangles and ellipses stored as their points and not as parameters; a self-crossing shape drawn by the even-odd rule rather than diagnosed as a mask would be; and the path free to move, because B-24d already moves one. **The owner accepted D-78 on 2026-09-20 ("accept D-78, proceed with B-25b").** B-25b, the shape layer in the core, is built the same day: the `shape` kind and its `shapes` list in the model, `src/shape.rs` for the drawing, document 21's step 1, the file both ways, `SHAPE_INVALID_OUTLINE` in document 28 and the schema. `verification/B-25b_shape_table.md` walks FX-SHP-001 to 017 and 020 to 030, 51 of 51. **B-25c, the window, is built on 2026-09-21**: New shape layer, the pen, rectangle and ellipse drawing shapes on a shape layer (the pen may leave a path open with Enter), a shape's row and stopwatch on the timeline, its fill and stroke in the layer's panel, and document 24's six commands. At the owner's word the pen now shows as a pen over the picture. `verification/B-25c_panel_table.md`, 17 of 17; `verification/B-25c_shape_playtest.md` is for the owner to play. B-25 is complete once that is played. **The owner passed it on 2026-09-23**, all fourteen steps ("1-14 works for B-25c now"), after two faults their walk found were fixed the same day. The pen pointer had never shown in the window: WebView2 does not draw a picture given as a CSS cursor and shows the fallback (WebView2Feedback #1925, #3388), so the page now hides the pointer over the picture and draws the pen itself, following the pointer through a drag. And a shape layer took 12,976 ms a frame in the window: `shape::draw` still asked every sample about every edge on one thread, the cost P-15 took out of masks. The fill is now P-15's `scanline_field` and the stroke `stroke_field`, P-15's band asked of a distance to the path; `the_fast_fields_are_the_slow_ones` in `src/shape.rs` holds both to the old sample-at-a-time field with no tolerance, and `tests/b25b_shapes.rs` is unchanged and passes. Measured once on a Ryzen 9 9900X (24 threads), release build, 1920x1080, a 400-edge ellipse: fill 8,073.4 ms to 4.4, a 4 px stroke 54,461.6 ms to 6.8. **B-25 is done.**

A GPU render path is not a backlog item. It is trigger-gated on a stopwatch reading per ADR-006.

## Performance, proposed on 2026-09-09 and not yet accepted

This section is a plan, not a schedule. It was written on 2026-09-09 from `Markdown/32_Performance_Architecture_Investigation.md` and `Markdown/33_Hardware_Optimization_and_Future_Architecture_Research.md`, both of which are research and neither of which authorises anything, against the measurement that made them necessary: `verification/T-06_declared_fixture.md`, where the declared ten-layer fixture costs 264.17 ms a frame at the median of the tenth loop against the 41.7 ms a 24 fps clock allows. That gap is D-47 in document 14 and D-47 is OPEN; nothing below closes it. Every entry here is PROPOSED, and the owner accepts or cuts each one by name. Where the two research documents disagree, document 33 is the corrected reading: the tenth-loop median is 264.17 ms, not 267.17; the sRGB encode in `to_srgb8_straight` is on the preview path as well as the export path; After Effects' SmartFX does declare regions; Resolve reuses upstream nodes. Where they agree, they agree on the thing that matters most, which is that nobody has measured where the frame goes, so the first unit measures and builds nothing.

Rules that bind every entry. **No expected value in `Fixtures/` or document 25 changes**, which is document 12's rule and needs no restating except that performance work is where the temptation is strongest. **No estimate from either research document is quoted as a result**; the only figures a P-entry reports are ones its own artifact measured, on the recorded machine, build and commit. **Savings are not added or multiplied**: document 33's arithmetic on this is right, and an item that saves half of a stage saves half of that stage, nothing more, until the timer says otherwise. **One unit at a time**, per document 04 line 45, and the order below is a dependency order rather than a promise. **ADR-006 stays with the owner**: its trigger has fired on a recorded measurement, which makes the record open to a decision, not reopened by an agent, and P-07 exists to put one number in front of that decision rather than to take it. **ADR-011, ADR-015 and ADR-017 hold as written** unless an entry says which one it would void and the owner amends it first.

Where this sits against B-12 and the interface redesign document 05 lines 73 to 75 describe. B-12 comes first and is the owner's. P-01 and P-02 are cheap, change no pixels and touch no interface, and can run in the same window as the redesign; P-04 changes how the page asks for frames and should land before the redesign replaces the page, so that the new page inherits the request model rather than rediscovers it. P-01 and P-04 between them are also the observed baseline document 05 line 69 says the efficiency targets wait on. Everything from P-03 on is renderer work and, by document 04's rule, does not run at the same time as the redesign.

P-01 / Where the frame goes. PROPOSED. Depends on B-12 only in courtesy: it changes no picture and can run beside it. Document 32's SP-P1 and document 33's PERF-R01 asked for the same instrument, and this is it: a per-stage timer through one frame of each fixture, the reference shot and `verification/T-06_declared_fixture.json`, at draft and at full, in a release build, on the recorded machine. The stages are the ones the code already has, named in the order they run: the wait for the viewer lock in `serve` (`app/src/main.rs` line 234); building the plan; for each layer, opening and reading the file, PNG decode, the byte-to-f32 conversion and the transfer function (`src/color.rs` line 18), the cache lookup and its copy (`src/cache.rs` line 163), the mask, each effect in the stack; then the tile loop (`src/render.rs` line 274), assembling the frame (line 278), `to_srgb8_straight` (`app/src/main.rs` line 351), and handing the bytes to the page. Separate three cache states and say which each row is: application cache cold, application cache cold with the operating system's file cache warm, and everything warm; document 33 is right that the T-06 "cold" loop never established the first of these. Scope is timers and a table, and the timers should be `Instant::now()` around stages reported through the trace mode ADR-012 already gives the build, not a profiling framework. **Not in scope: any optimisation, however obvious it looks while the timers go in.** Artifact: `verification/P-01_frame_trace.md`, one table per fixture per quality, p50 and p95 per row, whose rows sum to the measured frame time with the residual stated, and a line for each cache state. Exit: the "everything else" row document 32 section 1.2 could not account for, about half the frame, has become named rows, and the residual is small enough to say so. Every later entry is ranked by this table and not by either research document. **DONE**, and the artifact is `verification/P-01_frame_trace.md`: twelve tables, two fixtures by two qualities by three cache states, twenty frames a row, p50 and p95 per stage with the share of the frame and the residual stated, produced by `tests/p01_frame_trace.rs` under `cargo test --release -- --ignored p01_frame_trace`. The timers are `src/perf.rs`, an atomic nanosecond counter per named stage that is off unless the harness switches it on. The exit condition is met: the residual is 3.7% to 8.4% of a frame, so the half-frame document 32 could not name is named. What it found, in the order it ranks the work, is not the order either research document proposed. **The transfer function and premultiply is first in every cold row** (30.7% to 50.7%), which promotes P-03(c) from third item to first. **Copying the cel in and out of the cache is second** (11.1% to 13.4% admitting it cold, 13.4% to 55.6% looking it up warm), which is P-03(a). **The tile loop is 1.3% to 7.8% of every frame measured**, which demotes P-05 to near-worthless and settles the SIMD question below. **The effect stack is 23.2% to 65.2% of every declared-fixture row**, which no research document ranked and which P-09 below now carries. **The encode is 11.6% to 63.2%**, which P-03(c) deliberately excluded and which P-10 below now revisits. Three cache states were run and **the two cold rows came out equal**: emptying the Windows standby list needs a privilege this repository does not have, so this run did not establish a cold file cache and the artifact says so rather than reporting the difference as zero.

P-02 / Prefix reuse, counted on paper. PROPOSED. Depends on nothing and writes no code under `src/`. Document 32 section 6.2 argues that a run of frames whose lower layers hold the same drawings, transforms, effects and mattes composites those layers identically every frame, and that a deterministic renderer can cache that partial composite on a hash of its inputs rather than hope. Document 33 agrees it is promising if the stable prefix dominates cost, and neither document knows whether it does on real artwork. This unit counts, the way `verification/derive_d37_reuse.py` counted decode reuse without running the compositor: read the exposure sheets of both fixtures and report how many frames of each have a stable bottom prefix, how deep it goes, and what fraction of layer samples it would eliminate. Artifact: `verification/P-02_prefix_reuse.md` and the script beside it. Exit: a number. If it is small, P-08 dies here and this section says so; if it is large, P-08 becomes worth proposing to the owner as a G2 record. **DONE**, and the artifact is `verification/P-02_prefix_reuse.md` with `verification/derive_p02_prefix.py` beside it. The exit condition asked for a number and the number is this: **the stable prefix between consecutive frames is one layer deep, on all 239 transitions of both fixtures, with no exceptions.** The layer that moves on ones sits second from the bottom of both stacks, so the run of frames document 32 section 6.2 describes does not occur. A cache keyed on the prefix's inputs rather than on adjacency does find reuse, because the exposures are periodic with periods of 24, 24 and 60 frames and the shot comes round again, but reaching it costs memory this project has twice declined to assume: within the 1 GiB D-40 sets, the cache removes 24.9% of the reference shot's layer composites and 10.0% of the declared fixture's, and in both cases the layers it removes are the same one, the static background, whose decoded pixels `src/cache.rs` already holds. 45.5% on the declared fixture needs 40.45 GiB. The count is exact rather than approximate because neither fixture animates any property and the script asserts that rather than assuming it. **P-08 dies here**, and the entry below says so. What this does not say is that prefix caching is a bad idea in general: it says these two fixtures put the fastest-moving layer at the bottom, and a shot drawn the other way round would rank differently. The script takes a project file and an exposure sheet, so a fixture like that could be counted the day one exists.

P-03 / Tier 0: the bit-exact removals. PROPOSED. Depends on P-01, which supplies the baseline and the order. Six changes that each remove work the frame does not need and each leave every byte of every frame where it was. In document 32's numbering, and with document 33's corrections folded in: (a) hand out shared cache buffers instead of cloning 33 MB on every hit and again on every miss (`src/cache.rs` lines 163 and 173), with the caveat document 33 adds and document 32 missed, that `compose.rs` mutates the source when a mask or an effect is present, so a shared buffer is copied exactly then and never otherwise, and `verification/B-06_mask_table.md` and `verification/B-07_effects_table.md` are the tables that catch a layer contaminating another; (b) decode a frame's cels in parallel, which the decoder allows and the `&mut CelCache` in `plan_frame_cached`'s layer loop (`src/compose.rs` line 139) forbids, with in-flight requests for the same key deduplicated so a matte and its layer decode once, and with the `FrameLog` collected per layer and merged in composition order so document 28's diagnostics keep their order; (c) a 256-entry table for the byte-input side of the transfer function only, built by calling the existing `srgb_to_linear` on all 256 dequantised values so it cannot disagree with the function it replaces, with the encode direction left alone because its domain is continuous and it is on both the preview and the export path; (d) tiles written into the frame rather than into one buffer each and then copied into a zeroed 33 MB frame (`src/render.rs` line 278), preserving the property `tests/b05a_transform.rs` proves for ADR-011, that every tile's destination is fixed before any thread starts; (e) the per-pixel branches in `render_tile` on blend mode, opacity and matte presence hoisted out of the pixel loop, same operations in the same order; (f) a draft tile size chosen for a 480 by 270 frame, which today cuts into twelve tiles for twenty-four threads, while `DEFAULT_TILE_SIZE` stays what it is for export, and `verification/B-07_effects_table.md` already renders one frame at six tile sizes and requires six identical results, so the evidence that this is safe exists. One pull request per item, measured in isolation against P-01's table, in the order P-01 ranks them and not the order above. **Pixels: none may move.** Artifact: verification/P-03_tier0_table.md, per item the before and after from P-01's timer, and `verification/B-10_full_shot_table.md`'s method applied to a refactor: all 240 frames of both fixtures exported before and after the change and every pair byte-identical, with every existing fixture table unchanged. An item that moves one byte is reverted, not given a tolerance. What P-03 is not: it is not a promise of any factor. Document 32 estimates a factor of two or three from this tier and that sentence is an estimate in a research document; P-03 reports what it measured. **P-01 supplies the order, and it is (c), (a), (b), then (d), (e), (f).** (c) is first because the transfer function and premultiply is the largest named stage in every cold row. (a) is second because the cache copy is the largest named stage in three of the four warm rows. (b) is third because the read, the byte-to-float pass and (c) together are half a cold frame and they are per-layer work done serially. (d), (e) and (f) all live inside the tile loop and the frame assembly, which P-01 measures at 1.3% to 7.8% and 0.6% to 11.7%, so they are cheap to do and will not show a factor; they are kept because they are bit-exact and small, not because P-01 argues for them. **(c) is DONE and the artifact is `verification/P-03_tier0_table.md`**, which gains a section per item as each one lands. (c) is a 256-entry table inside `srgb_to_linear` itself, guarded on the argument's bit pattern rather than on its value, because `-0.0 == 0.0` is true and an equality guard moved a byte; nothing else under `src/` changed and `linear_to_srgb` was left alone. **The stage fell 20.5% to 40.7% across P-01's eight cold rows and the cold frame time fell 10.3% to 23.5%**, with the four warm rows unmoved because a cache hit decodes nothing. The proof that no pixel moved is `tests/p03_byte_equality.rs`, new with this item and reused by the five that follow: every frame of both fixtures at both qualities, encoded the way the window encodes one, reduced to a line a frame, run on the commit before the change and on the change, and **960 of 960 lines identical over 4,230,144,000 bytes**. One thing this item changed that is not a timing: **the transfer function is no longer the first-ranked stage of a cold full-resolution reference-shot frame — the encode is**, at 31.9% against 24.5%, which moves P-10 up. (a), (b), (d), (e) and (f) are not started, and the order among them stands. **(a) is DONE as well**, and `verification/P-03_tier0_table.md` gains its section. The cache holds and hands out `Arc<WorkingBuffer>` instead of an owned 33,177,600-byte copy a layer a frame; the two places that write on a cel, the mask and the effect stack, take the copy with `Arc::make_mut`, and **both are guarded** - on `mask.is_renderable()` and on `layer.effects.is_empty()`. The guards are the item: without the second one the first measurement made warm frames *worse*, because `Arc::make_mut` evaluated as an argument to `apply_stack` copies before the empty-stack check inside it, and the reference shot, which has no effects at all, reported a 15.541 ms effect stage. **The two cache stages fall from 14.0-50.3 ms a frame to 0.001-0.033 ms and all twelve frame-time rows improve**, the largest being the reference shot's warm draft frame at 26.929 to 6.310 ms - **0.15x the 41.667 ms budget, the first row this project has measured inside it**. The other eleven rows are still over, the worst at 11.11x, so D-47 stays open. The declared fixture's effect stage rose, because three of its layers do have effects and their copy now happens inside `apply_stack` rather than in the cache; the artifact states that rather than netting it away. Byte equality by `tests/p03_byte_equality.rs`, 960 of 960 lines identical. **(b) is DONE**, and it needed an artifact of its own: `verification/P-01_frame_trace.md` cannot measure it and reads 0.000 ms for it in all twelve rows, because its two cold rows hold `CelCache::none`, which may not keep what it decodes and so is never decoded ahead for, and its warm row already holds every cel. The state P-01 has no row for is the one a person sits through - a real budget and every layer of every frame a miss - so (b) reports `verification/P-03b_first_playthrough.md`, from the same harness, machine and twenty frames, with the viewer's own 1 GiB D-40 budget rather than P-01's 6 GiB, and P-01's artifact is left alone. `CelCache::prewarm` takes the cels the frame is about to ask for, drops the ones it holds, decodes one file once however many layers name it, and stages the rest in a `pending` list that `decoded` collects from - **still counting each one as the miss it is** and still admitting it through `store` in composition order, so the budget, the eviction order and `verification/B-08b_cache_table.md` are what they were when every decode was serial, and that table did not move a digit. **The first pass through the reference shot falls 39.3% a frame, from 43.242 ms to 26.262 ms, which is 1.04x the budget down to 0.63x**, and the declared fixture falls 34.4%; the three decode stages, 35.897 ms serial on four cels and 116.416 ms on ten, become 19.419 ms and 30.728 ms of wall-clock fan-out. That is 1.85x and 3.79x, not 4x and 10x: **this item is bounded by the layer count and by one disk, not by the thread count**, and the artifact says so rather than quoting twenty-four. Two things this entry specified were not built, and both are recorded rather than dropped quietly. The `FrameLog` collected per layer and merged in composition order is **unnecessary under this design**: the fan-out runs before the layer loop rather than inside it, nothing it declines to work out costs more than one serial decode, and `resolve_layer` still raises every diagnostic against the right layer in order, so **no diagnostic is ever raised on a worker thread** and there is no order to restore. And the fan-out **does not run on the no-budget path**, which is export: ADR-015 bound 3 says export neither reads the cache nor writes it, letting the decode run there would speed export up, and amending an accepted bound is the owner's decision and not a step in this item. One thing this item added that is not a saving: `src/perf.rs` gains a `Prewarm` stage measured as wall-clock from fan-out to join, and a `perf::untimed` that suppresses the three per-cel timers on the worker thread inside it, because summing their core time into counters that are compared against a frame's wall-clock would break the module's own disjointness promise and drive P-01's residual negative - a bug that reads as a result. The pixel proof needed a second half as well: the manifest renders through `CelCache::none`, which this item skips, so `tests/p03_byte_equality.rs` now renders every frame a second time through one cache with the viewer's budget and compares the two encodes byte for byte, which is `tests/b08b_cache.rs`'s rule extended to the declared ten-layer fixture. 960 of 960 manifest lines identical, and every budgeted frame equal to its unbudgeted twin. **(d) is DONE**, and it is the one item document 15 predicted would not show a factor that partly did. A tile allocated a buffer, accumulated the layer stack into it, and that buffer was copied row by row on one thread into a separately allocated, separately zeroed 33 MB frame. The frame is now allocated once at zero and **carved into one disjoint set of row slices per tile before any thread starts**, which is exactly the ADR-011 property `tests/b05a_transform.rs` proves and which the borrow checker now enforces instead of an index computed after the fact. Tiles accumulate into the frame's own pixels, starting from the same zero the private buffer did, so the arithmetic per pixel is unchanged; one 33 MB allocation, one 33 MB zero-fill and one 33 MB copy a frame stop happening. The tile decomposition, `DEFAULT_TILE_SIZE` and the six-tile-size table in `verification/B-07_effects_table.md` are untouched. **The assembly stage falls from 8.6-11.8 ms to 0.075-0.122 ms on every full row, from 15.1% of a warm full reference-shot frame to 0.2%, and all twelve of P-01's rows improve, the best two by 11.5% and 11.6%**; item (b)'s first-playthrough row improves in all four, the reference shot's full frame by 11.5%, and its warm draft frame is now 0.61x the budget. Sixteen rows measured, sixteen improved. What did **not** happen is the whole copy disappearing: the tile loop rose by 1 to 2 ms on the full rows, because a tile writing into its rows of a 33 MB frame writes into memory that does not fit in a core's cache while a small private buffer did, and the artifact reports that rather than netting it away. The trade is 11.7 ms of serial copy against 1.9 ms spread over twenty-four threads. Byte equality by `tests/p03_byte_equality.rs`, 960 of 960 manifest lines identical to the run item (b) left and every budgeted frame equal to its unbudgeted twin. **(e) is CUT**, and the cut is the deliverable: it was built twice, measured, and made the tile loop slower both times. The first version chose the mode's blend function once per layer as a function pointer and compiled the opacity and matte tests away as const generics, and was worse in eleven of P-01's twelve rows - the tile loop up 6% to 15% - because an indirect call at every pixel is one the optimiser cannot see into where the `match` it replaced was inlined. The second monomorphised everything, sixteen compiled copies of the pixel loop with every blend call direct, which recovered the reference shot and did not recover the declared fixture. Decided on the stage rather than the frame, because paired repeat runs of the same build differ by more than the two builds do: **seven of eight paired tile-loop measurements are slower with the hoist and none is faster**, +1.3% on the reference shot's full frame and +6.1% to +6.4% on the declared fixture. The branches were never the cost - a test on a value that does not change across a loop is predicted correctly every time after the first - while sixteen copies of the pixel loop are a real instruction-cache cost, and the ten-layer fixture walking through more of those copies than the four-layer one is exactly the direction that explanation predicts. Nothing under `src/` differs from what (d) left, and `verification/P-03_tier0_table.md` carries the numbers so the idea is closed rather than plausible. Reopen it only on the same condition document 15 already sets for SIMD, a later P-01 run showing the tile loop grown into a large fraction of the frame. **(f) is DONE, and P-03 is finished.** `compose::DEFAULT_TILE_SIZE` is 128 pixels because `verification/B-05a_scaling_table.md` measured it - on a 1920x1080 frame. A draft preview is 480x270, which 128 pixels cuts into four columns of three: **twelve pieces of work for twenty-four hardware threads**. `tests/p03f_draft_tile_size.rs` redoes that measurement at the extent a draft actually renders at, timing `render::render` alone - the decode and the encode are outside the span because this item moves neither and at a first playthrough they would hide it entirely - across seven sizes, twenty frames and two whole sweeps of the list on both fixtures, and **compares all 560 renders against the 128px render of the same frame byte for byte**. 128px is the worst size in all four columns; below 48 the table stops improving and starts disagreeing with itself, which is per-tile overhead beginning to cost more than the parallelism buys. `compose::DRAFT_TILE_SIZE` is therefore 48 pixels, sixty tiles, applied only when the preview quality is `Draft`, and **`DEFAULT_TILE_SIZE` is untouched, so an export still renders in 128px tiles measured on the extent an export renders at**. **The draft tile loop falls 6.5% to 16.2% across P-01's six draft rows and 6.3% and 12.3% in item (b)'s first-playthrough rows, while the six full-resolution rows - which cannot have changed, because their code did not - read -5.2% to +0.0% and are the control that says what this machine's noise on that stage looks like.** What this item did not do is move a frame time: the tile loop is 1.6 to 4.8 ms of a draft frame, so a sixth of it is 0.1 to 0.8 ms, the twelve frame-time rows read between -4.6% and +3.3% with no relation to whether a row is draft or full, and the row whose tile loop fell furthest reads +2.6% on the frame. That is item (e)'s lesson applied rather than re-learned: where the change is smaller than the run-to-run spread, the stage is the measurement and the frame is not. Byte equality by `tests/p03_byte_equality.rs`, **960 of 960 manifest lines identical to the run item (d) left** - the strongest run of that test so far, because every draft frame in it was rendered in 48-pixel tiles and compared against a manifest produced in 128-pixel tiles. One thing this item leaves standing rather than tidying: five dated artifacts (`verification/P-01_frame_trace.md`, `B-08b_cache_budget.md`, `B-08_preview_latency.md`, `T-06_declared_fixture.md` and `T-06_performance_envelope.md`) record "Tile size: `compose::DEFAULT_TILE_SIZE`" and are **not** regenerated, because they are records of runs that really did use 128-pixel tiles throughout and an artifact rewritten to match today's build is not a record; each test source now names the constant its own rows render in.

P-04 / The editor answers while the renderer works. PROPOSED. Depends on P-01, which measures the lock wait this fixes. Document 33's structural finding, confirmed against the source: `serve` takes the viewer mutex at `app/src/main.rs` line 234 and holds it through planning, rendering and `to_srgb8_straight` at line 351, so every command that needs the same mutex waits for the frame, and on the declared fixture that is a quarter of a second per keystroke; and the page's `inFlight` flag in `app/ui/index.html` drops any request that arrives while one is out, which is neither a latest-request mailbox nor a cancellation. Scope: snapshot what a frame needs under a short lock and render outside it; requests carry a generation and a stale result is discarded rather than shown; the page keeps the latest request rather than the first. This is document 06's snapshot contract applied to the viewer, and it is also what document 33 calls PERF-R02. **Pixels: none.** Artifact: verification/P-04_responsiveness.md: an event log of command acknowledgement times, p50 and p95, while a declared-fixture frame is in flight, before and after; and a window recording made with `tools/capture_window.ps1`, which never builds, so `cargo build -p anime_compositor_app --release` first. Document 33 proposes p95 command acknowledgement at or below 16 ms and useful drag preview at or below 50 ms as candidate targets; **they are not adopted here.** P-04 reports the measurement, and if the owner wants a target it goes into document 08 beside the ones already there. This is the one performance entry that touches the interface and it is the reason the ordering paragraph above puts it before the redesign. **DONE**, and the saving is the largest single number this backlog has produced. A keystroke pressed while a frame of the declared ten-layer fixture is being made **falls from 46.757 ms to 0.001 ms at p50 and from 181.882 ms to 0.004 ms at p95** in draft, and from 67.240 to 0.001 and 193.009 to 0.005 at full resolution; frame times did not regress. `serve` now copies what a frame needs out of the viewer under a short lock - document 06's snapshot contract applied to the viewer - and renders from the copy, and everything the window says about the frame is written into the response headers under that same lock, so the sentence on screen and the pixels on screen still came from one instant of one document, which is the only thing holding the lock across the render ever bought. The decoded cels move behind a lock of their own: a render holds it for its whole length and no command touches it. The copy costs **p50 0.0114 ms over two hundred**, which is 0.026% of a frame and is what the whole saving is bought with. The page keeps the latest request rather than the first, and every request carries a generation so a result that is no longer the newest is discarded rather than drawn; playback opts out of the mailbox by asking with `keep` false, because queueing a playback request would put a frame from the past in front of the present, and D-32's skip accounting is unchanged. `verification/P-04_responsiveness_table.md` runs on every build and carries no duration at all - it asks whether a press was answered *inside* a frame that was still being rendered, which on the build before this one is zero by construction, and the harness was run against that build to prove the test is a regression guard and not a tautology: "no command was answered inside a frame: 5 presses against 4 frames". The numbers are dated in `verification/P-04_responsiveness.md`. `verification/P-04_window.png` photographs two arrow presses at full resolution landing as **frame 2**, where before P-04 the second press was thrown away and the window would have read frame 1; taking it needed `tools/capture_window.ps1` to learn keys that have no printable character, which is the one file outside P-04's scope this unit touched. **Document 33's candidate targets - p95 acknowledgement at or below 16 ms, drag preview at or below 50 ms - are still not adopted**, and the measurement clearing both is a fact about this machine and this fixture rather than a promise.

P-12 / The one artifact that is not the same twice. PROPOSED, and small. `verification/B-09_recovery_table.md` is written on every build and CI checks `verification/` byte for byte, so a page that can come out two ways is a gate that can fail for no reason. It can: over five full-suite runs on 2026-09-09 the row **recovering says which snapshot was opened** named the snapshot in **slot 1** four times and the one in **slot 0** once. The cause is in `src/persist.rs` line 1671: `recovery_candidates` sorts by the file system's modification time and breaks a tie by the **lowest** slot, and two snapshots written milliseconds apart tie at the granularity Windows records. So the tie-break hands back the older of the two, which is a defect in recovery itself and not only in the page: document 07 promises the newest snapshot, and on a tie this returns the oldest of the tied ones. Scope: decide what "newest" means when the file system cannot say - the snapshot's own recorded time from inside the JSON is the candidate, the rotation order is the other - and make the page deterministic either way. **This is a specification question about what recovery offers, not a test fix**, so it is the owner's decision which rule applies. Artifact: `verification/B-09_recovery_table.md` unchanged across ten consecutive full-suite runs, and the row that flipped stated. Found while running P-05's gates; nothing in P-05 touches it. **DONE**, and it was a defect in recovery and not only in the page. `persist::autosave` now gives each snapshot it writes a modification time strictly later than every other snapshot beside it, so "newest" is an ordering the writer states rather than a coincidence of clock granularity the reader hopes for. It is best effort by design: a file system that refuses the change leaves the ordering exactly as coarse as it was and no worse, because a snapshot that was written is worth more than a timestamp that was not adjusted. **No format change**, so nothing about what a snapshot contains moved and document 28's preservation rules are untouched. The guard is in `verification/B-09_persistence_table.md`: **thirteen snapshots written back to back with no wait, and the last one written is the one recovery offers**, then the row that actually fails on the old build - **every other snapshot dated an hour ahead, and the one just written is still the one recovery offers**. Without the stamp that row reads **slot 4** against an expected **slot 0**, which is an hour-stale edit handed back as the newest; it was run both ways to confirm the check fires. The 20 ms sleeps `tests/b09_persistence.rs` carried to make the rotation observable are gone with it - they were the same defect worked around rather than fixed. Thirteen back-to-back writes do **not** tie on this machine, which is why the flake took five full-suite runs to show once and why the hour-ahead row exists: it is the tie taken to its conclusion, a file system whose times do not order the writes. Artifact: `verification/B-09_persistence_table.md`, and `verification/B-09_recovery_table.md` unchanged and now unable to read otherwise.

P-05 / Skip a layer for the tiles it cannot touch. PROPOSED. Depends on P-03, so that it is measured against the cleaned-up tile loop and not the current one. Today `render_tile` samples every layer at every pixel of every tile (`src/render.rs` line 293 onward) with no test of whether the layer's transformed extent meets the tile at all, and cel artwork is mostly transparent. This is Fusion's domain of definition and Nuke's region of definition, applied to a layer stack: give every `LayerDraw` a box in composition space, the transformed source rectangle grown by one pixel for the bilinear footprint, with any effect's `bounds_expansion` already inside the buffer, and skip the layer for tiles outside it. Geometric box only; an alpha-derived box is tighter and costs a scan, and document 33 is right that it needs a stronger invariant than alpha alone, so it is a later entry if this one pays. **Pixels: expected identical**, and the argument is document 32 section 5.4's: a sample more than a pixel outside the source is exactly `[0, 0, 0, 0]`, and `blend_pixel` in `src/composite.rs` is the identity for that source in all four of document 21's modes. The one wrinkle to check is the sign of zero, since `0.0 + -0.0` is `+0.0`, and the check is the artifact rather than the argument. Artifact: verification/P-05_culling_table.md in the shape of `verification/H-01_whole_picture_table.md`: whole-frame byte equality with and without culling on both fixtures, all 2,073,600 pixels, plus timings at draft and at full where the effect should be largest. ADR-017 note: this entry is void the day effect evaluation moves into the tile loop, which is the condition ADR-017 already names for itself. **P-01 has largely answered this one before it starts, and the answer is no.** The tile loop is 1.3% to 7.8% of a frame in all twelve rows, so the whole stage this entry makes cheaper is smaller than the residual in some of them. It stays in the list because the byte-equality artifact is worth having and because the culling box is the same box P-09 would need, but it is now expected to report a saving too small to matter, and it must run **after** P-03 and P-09 rather than before, so that it is measured against a frame where the tile loop is a larger fraction than it is today. **DONE**, and the entry was right about the saving and wrong about why. Every `LayerDraw` now has a box - its source rectangle grown by the one pixel bilinear sampling reaches for, mapped through the layer's own transform - computed once per layer per frame in `src/render.rs` and tested against each tile before the layer is sampled. **Pixels: none moved.** `tests/p05_culling.rs` runs on every build and renders both fixtures **twice from one plan**, once by the renderer and once by `render_without_culling` which is the loop as it was, at draft and at full, on frames 0, 14, 100 and 239: `verification/P-05_culling_table.md` reads **21 of 21 checks passed**, every full-resolution row comparing all 2,073,600 pixels with **0 pixels differ**. The sign-of-zero wrinkle the entry names **does not occur**: the comparison is made on the f32 working buffer bit for bit as well as on the encoded frame, and both read identical, so no blend anywhere in either fixture reached `0.0 + -0.0`. **The measured saving is not small; it is nothing.** The box excludes **0 of the 7,020** layer-and-tile pairs of those eight full-resolution frames, because every cel in both fixtures is a full-frame drawing with transparent margins, and a geometric box cannot see transparency - which is the entry's own reason for ranking an alpha-derived box as a later item, now measured rather than argued. The skip itself works: the same page excludes **120 of 135** pairs for a hand-built quarter-size layer in one corner of a 1920x1080 frame, and that frame too is byte-identical. `verification/P-05_culling.md` is the timing half, `#[ignore]`d and run deliberately in release on the recorded machine: **+2.5% and +1.3% of the tile loop at draft, -0.6% and -0.3% at full**, against a tile loop P-01 measured at 1.3% to 7.8% of a frame - under a fifth of one percent of a frame at the worst row. It is kept on that basis, as a test that costs nothing and will pay on layers smaller than the frame, and not on a saving. One measurement error is recorded there rather than buried: with the culled render always running first on a fresh plan the table read **+24% at draft**, which was the cold cache of whichever render went first and not the box; the two now alternate frame by frame and the figure fell to under 3%. The corners were hoisted out of the tile loop while chasing that false reading, which is a better shape either way and changed no measurement. Artifacts: `verification/P-05_culling_table.md` and `verification/P-05_culling.md`.

P-06 / Draft-resolution cels, shown to the owner as a difference. PROPOSED, and it cannot be built without a register entry first. Draft quality reduces the composite to 480 by 270 and decodes every cel at 1920 by 1080 anyway, so the stage that dominates the frame is untouched by the setting that exists to make it cheaper. A cel held at quarter resolution is one sixteenth the bytes: document 33 prices the declared fixture's 166 distinct drawings at about 5.13 GiB at full and about 0.32 GiB at quarter, inside the 1 GiB D-40 now sets. **Pixels: change on the preview path**, because downsampling then transforming is not transforming then downsampling, which makes this a specification question and not an optimisation. D-33 already puts the preview at draft by default and requires the viewer to say when it differs from export; whether that sentence covers this difference is the owner's to say, in document 14, before any of this lands. The export path is untouched by construction, since ADR-015 keeps the cache out of export and `plan_frame` and `crate::export` pass `CelCache::none()`. Scope: a branch that is measured and not merged. Artifact: verification/P-06_draft_cels.md: frame time and cache bytes at draft with quarter-resolution cels downsampled in premultiplied linear, an image-difference table against the current draft output on both fixtures with the largest differences shown as pictures, and the export shown byte-identical even though ADR-015 makes that structural. The table is the deliverable; this unit exists to let the owner see the cost and decide, not to argue for it. **DONE**, measured on 2026-09-10 in release on this machine, and **not merged**: the whole quarter-resolution path lives in `tests/p06_draft_cels.rs` behind an `#[ignore]`d measurement, and **no file under `src/` changed**. The page is `verification/P-06_draft_cels.md` and it carries four findings. **The frame gets about half its tile-loop time back.** A draft frame falls from 1.914 ms to 1.007 on the reference shot and from 4.141 to 1.944 on the declared fixture. **Reducing a cel costs far more than one frame's saving.** Reducing one frame's cels takes 42.3 ms on the reference shot and 135.5 on the declared fixture, against a saving of about one and two milliseconds a frame, so a reduced cel has to be held and reused across **47 and 62 frames** before it has paid for itself; on a miss it is a loss. **The bytes are where it actually moves, and document 33's arithmetic holds.** The reference shot's 56 distinct drawings are 1.73 GiB at full and 0.11 at a quarter, the declared fixture's 166 are 5.13 GiB and 0.32 - both over D-40's gibibyte at full, both several times inside it at a quarter. That is the frame time above: a cache that fits is a cache that stops missing. **The picture changes, and not subtly.** More than half of every draft frame's 129,600 pixels differ once encoded to eight bits, by up to **46 of 255 on the reference shot and 93 on the declared fixture**, mean about one code value, and the amplified difference pictures beside the page show it sitting on edges and fine detail with flat areas black. Averaging sixteen source pixels is not the same as taking one bilinear sample of them; which of the two is correct is not a question this measurement can answer. The export was checked anyway and is byte-identical over eight frames, as ADR-015 makes it. **The register entry in document 14 is still owed**, and nothing here lands without it.

P-07 / One GPU tile, and the number the decision needs. PROPOSED, and dependent on P-01 through P-05 having been measured with the gap still open. If Tier 0 and culling put the declared fixture inside the budget, this unit dies unstarted and this line says so. ADR-006's trigger has fired on `verification/T-06_declared_fixture.md` and the record is the owner's to reopen; this unit does not reopen it. What the owner would need in order to choose between the two roads document 32 Part 5 Tier 3 names, a GPU preview tier with the CPU as sole authority or a fixture regime that moves to tolerances, is a number that does not exist: how far a GPU render of `H-01` actually lands from the CPU render on the machine's own card. Scope: throwaway code under `spikes/`, quarantined as document 06 requires and never reused: `render_tile` as a `wgpu` compute shader, the `H-01` fixture through it, the maximum and mean distance in code values over all 2,073,600 pixels; one 1080p f32 upload and download timed, because a design that moves 33 MB each way per frame spends its winnings on the bus; and a note on what `Affine`'s f64 (`src/render.rs` line 36) becomes in a shading language whose runtime floats are f32, tested on a large translation and a near-singular scale, since document 33 is right that this is not an f32 translation of f32 code. Artifact: verification/P-07_gpu_distance.md. Nothing ships. **DONE**, measured on 2026-09-10 on this machine's own card, an NVIDIA GeForce RTX 4070 Ti SUPER over Vulkan. The spike is `spikes/p07_gpu`, quarantined under document 06; nothing from it is in the build. The page is `verification/P-07_gpu_distance.md` and it carries three findings. **The two renders are close.** On the H-01 fixture the largest disagreement over all 2,073,600 pixels of four frames is about six hundredths of a millionth of a code value in the working space - one step of f32 at those magnitudes - and once encoded to the eight bits a person sees, three of the four frames are identical and the fourth differs at **one pixel by one code value out of 255**. Put the geometry under strain, rotating the whole stack about an anchor 8,388,608 px off canvas, and it is still **519 pixels of 2,073,600 by one code value**, none by more. **The arithmetic is not equally safe everywhere.** A near-singular scale puts an all-f32 inverse **32 source pixels** away from the f64 one this build computes. That is not a rounding difference, it is a different pixel: whatever a GPU tier did, it could not be handed the transform and asked to invert it in f32. **The bus is not free.** One 1080p f32 frame is 31.6 MiB and costs about 15 ms to move on and off the card, before a single pixel is composited - better than a third of the 41.7 ms one frame gets at 24 fps, spent on transport alone. What this does **not** do is reopen ADR-006 or choose between document 32 Part 5 Tier 3's two roads. It is the number those decisions needed and did not have.

P-08 / The stable-prefix composite cache. **CUT by P-02, confirmed on 2026-09-18 (D-70); not scheduled.** It was PROPOSED only if P-02 says it pays, and then as a G2 record of its own rather than a backlog line, because it is exactly the kind of feature document 04's anti-bloat rule exists to challenge. Document 27 already lists the transformed-layer and composition-frame caches this build has not built; this is a partial-composite cache between them, keyed on a hash of the prefix's complete inputs, and document 33's caveat governs it: determinism makes the key sound, it does not make it complete, and every input document 27 line 29 names goes in. Pixels: identical if and only if the key is complete, which is the whole risk. Artifact when it comes: `verification/B-08b_cache_table.md`'s shape extended, both fixtures rendered with and without the cache and every frame byte-identical. Not scheduled. **CUT by P-02, which was the condition this entry was written under.** `verification/P-02_prefix_reuse.md` measured the reuse this cache would sell: within the 1 GiB D-40 sets it removes one layer, the static background, and nothing else, on both fixtures; and the layer it removes is one whose decoded pixels the cel cache already holds. Against that stood a second cache at 33,177,600 bytes an entry competing for the same budget, and a key that has to include every input document 27 line 29 names or be wrong in a way no fixture table would catch. This entry is not proposed and is not scheduled. What would reopen it is a fixture whose fast layer is above a still background rather than below it, which is the case document 32 had in mind, and `verification/derive_p02_prefix.py` would count it without changing a line.

P-09 / The effect stack runs whole-layer at full resolution, even at draft. PROPOSED. Depends on P-01, which found it. **Neither research document ranked this and P-01 puts it first or second in every declared-fixture row**: 26.6% of a cold draft frame, 65.2% of a warm draft frame, 46.2% of a warm full frame. The cause is structural rather than accidental. ADR-017 puts effect evaluation whole-layer, before the frame plan and outside the tile loop, and `src/compose.rs`'s `resolve_layer` applies the stack to the source buffer at the cel's own 1920 by 1080 whatever the preview is showing. Draft quality scales the composite to 480 by 270 and the effect stack never hears about it, which is the same shape of problem as P-06: the setting that exists to make the preview cheap does not reach the stage that dominates it. Two roads, and this entry measures rather than commits. The first is to evaluate the stack on a draft-resolution source, which **changes preview pixels** and is therefore P-06's question and the owner's, not this entry's. The second is bit-exact: the stack is re-evaluated for every frame that references a cel, and a cel whose stack and whose resolved parameters are unchanged produces the same buffer every time, so the result is cacheable on the key `src/cache.rs` already builds plus a hash of the resolved stack. Document 33's caveat on P-08 governs this key as well, and it is a much shorter key. Scope of this unit: count how many of a playthrough's effect evaluations are repeats, the way `verification/derive_d37_reuse.py` counted decode reuse, and report it; **no code under `src/`**. **Pixels: none.** Artifact: `verification/P-09_effect_reuse.md` and the script beside it. Exit: a number, and a recommendation of which of the two roads the owner should be offered. **DONE**, and the artifact is `verification/P-09_effect_reuse.md` with `verification/derive_p09_effect_reuse.py` beside it. **93.3% of the declared fixture's 720 effect evaluations in a playthrough are repeats of one already done**, because ADR-017 fixes an evaluation's whole input to the drawing and the resolved stack, and the fixture has 48 distinct pairs. The reference shot has no effect instances at all, which is why P-01 measures its stack at 0.0%. One least-recently-used cache shared by the three instances is the wrong shape: the request stream cycles, so it removes 33.3% at every size up to a cliff at 48 held results, and that cliff is 1.48 GiB against D-40's 1 GiB. Split by instance it works, and the recommendation is to buy the expensive one first: **the blur alone removes 228 of its 240 evaluations for 0.37 GiB.** Of the two roads P-09 was asked to choose between, it recommends the bit-exact one, which is P-11 below; the other is P-06's, changes preview pixels and stays the owner's. P-09 timed nothing and split no timer by instance, so the blur being the expensive instance is an argument from what the three effects do rather than a measurement of them, and P-11 measures it before it buys it.

P-10 / The encode for the page. PROPOSED. Depends on P-01. P-03(c) deliberately left the encode direction of the transfer function alone, on the argument that its domain is continuous and that it is on the export path as well as the preview path. P-01 measures `to_srgb8_straight` at 11.6% to 28.6% of a cold full frame and **63.2% of a warm full reference-shot frame**, where it is the largest single stage, so that argument is now one to re-examine rather than a settled one. What this entry may do is narrow and bit-exact: the unpremultiply, the encode and the quantise run per sample over 2,073,600 pixels in one serial pass, and that pass is independent per pixel with no reassociated arithmetic, so splitting it across the threads `rayon` already owns produces the same bytes in the same order. **Pixels: none may move**, and the proof is `verification/B-10_full_shot_table.md`'s method: every frame of both fixtures exported before and after the change, every pair byte-identical. What it may **not** do is approximate the transfer function, table it, or change its arithmetic; that is the direction P-03(c) excluded and it stays excluded. Artifact: verification/P-10_encode.md, the before and after from P-01's timer on the recorded machine, with the byte-equality table. **P-03(c) has moved this entry up.** With the decode table in, the encode is the **largest** named stage of a cold full-resolution reference-shot frame at 31.9% and 31.8%, not the second largest: `verification/P-03_tier0_table.md` has the pair of rankings side by side. Nothing about the encode changed; the stage it was measured against halved. This entry is now the one to schedule once the remaining P-03 items are in, and before P-05. **DONE**, and it is the largest single saving this project has measured. The unpremultiply, the transfer function and the quantise run once per pixel with nothing a pixel does depending on any other pixel, so the pass is now one `rayon` map across the pool the renderer already owns, writing into a destination allocated once and split into one disjoint four-sample piece per pixel - the same property P-03(d) established for tiles, enforced the same way. **The arithmetic is untouched**: the same `unpremultiply`, the same `linear_to_srgb` on the same three channels, the same quantiser, because this entry may not approximate or table the encode direction and did not. **The stage falls 78.4% to 93.1% in all twelve of P-01's rows** - 52-57 ms to about 3.9 ms on every full-resolution row - and **eleven of the twelve frame times fall with it**, the warm full-resolution reference shot from 64.431 ms to 13.061 ms (**-79.7%, 0.31x the budget**) and the warm draft from 5.621 ms to 2.254 ms (0.054x). The factor is fourteen at full resolution and five at draft, not twenty-four: 33 MB of floats in and 8 MB of bytes out makes this pass memory-bound past a certain thread count, and the artifact says so rather than quoting a thread count. P-01 was run **twice** on the after build and both runs are in the artifact, because one row - the declared fixture's warm draft frame - read +12.7% in one run and -0.6% in the other against a stage that fell 3.4 ms in both; a row whose repeats differ by 19 ms is reported as unmoved rather than as a result, which is item P-03(e)'s lesson applied. In P-03(b)'s first-playthrough harness **a first pass through the reference shot at full resolution is now inside the frame budget at 0.79x**, down from 1.75x and from 2.57x before P-03 began; the declared ten-layer fixture is still outside it at 4.14x and 4.97x and D-47 stays open, with the effect stack now the largest thing left, which is P-11. Byte equality by `tests/p03_byte_equality.rs`, **960 of 960 manifest lines identical to the run P-03(f) left** - the sharpest use of that test so far, because every line of the manifest is a digest of exactly what this item computes, so one sample quantised differently on a worker thread would change one. Artifact: `verification/P-10_encode.md`. One consequence stated rather than left implicit: the encode is not the cache, so ADR-015 bound 3 does not touch it and **export gets this saving on every frame it writes**; the open question bound 3 does raise, whether the parallel decode may run on the no-budget path, is still the owner's and is untouched.

P-11 / Cache the evaluated effect result. PROPOSED, on P-09's recommendation. Depends on P-09, which counted it, and on P-01, which is the timer it reports against. ADR-017 evaluates a layer's effect stack whole-layer on the cel's own pixels before the frame plan exists, which makes an evaluation's whole input the decoded cel and the resolved stack, and `verification/P-09_effect_reuse.md` counts 93.3% of a playthrough's evaluations as repeats of one already done. Scope: a cache of evaluated results keyed on the cel key `src/cache.rs` already builds plus a hash of the resolved stack, sized in bytes and sharing D-40's budget rather than adding to it, with document 27 line 29's list of inputs in the key or the key is wrong. **Not one least-recently-used cache shared by every instance**: P-09's table shows that shape removing 33.3% at every size up to a cliff at 1.48 GiB, which is over budget, because the request stream cycles and least-recently-used is at its worst against a cycle. Per instance, and the blur first, at 0.37 GiB for 228 of its 240 evaluations. The first thing this unit does is split the effect stack in `src/perf.rs` by instance and re-run P-01's harness, because P-09's claim that the blur is the expensive one is an argument from what the three effects do and not a measurement; if the timer disagrees, the unit buys whatever the timer names instead. **Pixels: none may move**, and the proof is `verification/B-10_full_shot_table.md`'s method: every frame of both fixtures exported before and after, byte-identical, with `verification/B-07_effects_table.md` unchanged. Artifact: verification/P-11_effect_cache.md, the before and after from P-01's timer on the recorded machine, the hit and eviction counts in the shape of `verification/B-08b_cache_table.md`, and the byte-equality table. What would void this entry is the owner taking P-06's road instead, which makes the stage cheap rather than skippable; the two do not conflict, and this one does not prejudge that. **DONE**, and the timer answered the entry's opening condition before the cache was built. `src/perf.rs` now splits the effect stack into five stages - the copy the stack writes into, the exposure, the tint, the blur, and this unit's cache - and on the declared fixture's six P-01 rows the **blur reads 111.749 to 123.073 ms p50 against the exposure's 0.970 to 1.183 and the tint's 0.846 to 0.892**, which is 28.4% to 80.3% of a frame in one instance. The timer agreed with P-09, so the cache was built for the blur as directed. **A first playthrough of the declared fixture falls from 172.498 ms a frame to 46.729, 49.478 and 50.817 across three runs - 4.14x the 24 fps budget down to 1.12x to 1.22x - and at full resolution from 206.926 to 71.660, 72.037 and 68.958, 4.97x down to 1.65x to 1.73x.** The blur's own p50 is now **0.000 ms** with a p95 of 111.269: more than half the frames of a pass do not run it, and the ones that do pay what they always paid, which is why the p95 frame time barely moves. The key is held **by value, not as a hash** - the cel key `src/cache.rs` already builds, the drawn mask, and the resolved effect stack itself - because document 27 line 29 requires every input in the key and a hash of a stack is a promise that two stacks differing in the eighth decimal of a sigma will collide loudly rather than silently; that costs 1.061 ms p50 in a stage of its own and it is 2% of what it saves. The budget is **split, never enlarged**: 448 MiB of D-40's gibibyte is effect results and the remaining 576 MiB is cels, on a second least-recently-used list for the reason P-09 measured, so the cel stream cannot evict an effect result about to be asked for. One rule was built, measured and **rejected**: admitting only stacks that expand their bounds - document 21's own declaration, and the class of effect that reads a neighbourhood - gave a clean cache of 9 hits and 9 evaluations with nothing dropped, and made Draft **slower** at 55.406 and 54.830 ms, because the blur's hit rate hardly moved and an exposure hit also skips the copy the stack writes into, which is 3.953 ms p50. The eviction count of the shipped build, 18 hits against 38 evaluations and 24 dropped, is therefore reported as a fact about the budget and not a fault to fix. **Pixels: none moved.** `tests/p03_byte_equality.rs` renders 960 frames across both fixtures at both qualities with no cache and again with the viewer's own budget, every pair equal and the manifest **identical line for line to the one P-10 left**, and this is the first run of that test in which the budgeted half went through the effect cache. `verification/P-11_effect_cache_table.md` runs in the normal suite and is the correctness half: the key notices a blur of 4.5 where it stored 4.0, notices a corner of the mask moving, notices a mask removed, and **an effect this build cannot draw raises its warning on the frame served from the cache exactly as on the frame that filled it** - document 27's "caching must never define correctness" made a test, because a diagnostic that appeared only on a miss would be a cache defining one. Export is untouched: ADR-015 bound 3 stands, `CelCache::none` has no effect budget, so every figure here is a preview. Artifact: `verification/P-11_effect_cache.md`. Left open and named there: `verification/T-06_declared_fixture.md` and `verification/T-06_performance_envelope.md` still build `CelCache::with_budget(DEFAULT_BUDGET_BYTES)`, which is now the cel-only cache rather than the viewer's, and whether they move to `CelCache::viewer()` is an acceptance decision for the next deliberate envelope run, not a performance one. The largest stage left on the declared fixture is the decode at 32.472 ms p50, which is P-04's and P-05's territory.

P-13 / Spread the Gaussian blur across the thread pool. PROPOSED. Depends on P-01, which is the timer it reports against, and on P-11, which made the blur a cost the frames that miss the cache still pay in full. **Asked for by the owner on 2026-09-12, and not the thing they asked for**: the question was whether calculation precision could be lowered "to say, the hundreths" to buy performance, and the answer was that it could not. Rounding a number adds an operation rather than removing one; the pixels are already f32 and the f64 that is left runs in a tile loop costing 4.2 to 4.3 ms a frame; and hundredths is 1e-2 against the 1e-5 and 2e-5 document 25 line 57 allows, which `Fixtures/` being read-only makes the owner's decision rather than an implementation step. What the same measurement showed is that `convolve` in `src/effects.rs` was a plain `for` loop on one thread inside the stage that dominates every declared-fixture row, while the tile loop beside it has been spread across the rayon pool since B-08a. Scope: hand out one destination row per task with `par_chunks_mut`, and nothing else. A row reads the source and writes only itself on either axis of the separable pass, so the arithmetic does not move and the borrow checker is what says the rows are disjoint. **Pixels: none may move**, and the proof is stricter than document 25's tolerance - the two results are compared by their bits, because a change that reassociated nothing may not differ in the last place either. What this entry may **not** do is narrow a type, table the Gaussian, or cut the `ceil(3*sigma)` radius document 21 specifies; all three change pixels and all three are the owner's. Artifact: `verification/P-13_parallel_blur.md`, the before and after from P-01's timer on the recorded machine, with `verification/P-13_parallel_blur_table.md` as the correctness half. **DONE.** **A warm draft frame of the declared ten-layer fixture falls from 132.8 ms to 42.0 and 44.2 ms across two runs - 3.19x the 41.667 ms a 24 fps clock allows down to 1.01x and 1.06x, which is the first time any row of this fixture has reached the budget** - and a warm full-resolution frame from 153.6 to 62.5 and 59.8, 3.69x down to 1.50x and 1.44x. The cold rows fall from 8.31x and 8.91x to about 6.0x and 6.5x. The blur's own stage is read at p95 rather than p50, because P-11's cache answers more than half the frames of a pass without running it at all: **116.4 to 153.1 ms across the fixture's six rows becomes 13.7 to 17.3**, a factor of seven to nine against twenty-four hardware threads, which is memory-bound past a certain thread count in the same way `verification/P-10_encode.md` recorded for the encode. **The reference shot does not move**, which is the check that these figures are the blur and not the weather: it has no effect instances at all, and its rows read 81.5 against 82.7 and 83.6 cold, 2.5 against 2.3 and 2.5 warm. **Pixels: none moved.** `verification/P-13_parallel_blur_table.md` blurs a cel-sized buffer at the fixture's own sigma of 4.0 in a pool of one thread and on the pool the machine would use and compares all 8,584,704 samples bit for bit, zero differing, on every build. `tests/b07_effects.rs` is unchanged and still checks the blur against weights generated a second way. P-01 was run twice on the after build and both runs are in the artifact, which is P-10's lesson applied. **D-47 stays open**: a cold frame of this fixture is still 6.0x the budget, and the fresh table names what is left - the transfer function and the premultiply at 46.8% to 52.5% of every cold row, which is P-03(c)'s territory and deliberately excluded there, and the copy the effect stack writes into at 29.6% of the warm draft row, which is a copy rather than arithmetic. The one precision trade this unit declined and left with the owner is the kernel radius: two and a half sigma instead of three drops about a sixth of the taps and changes pixels by roughly 1e-3, so it is an amendment to document 21.

P-14 / The blur while its radius is being dragged. **DONE on 2026-09-12; closed on 2026-09-18 (D-70) with ADR-006 left alone and neither CPU answer started.** It was PROPOSED. **Asked for by D-51 and by nothing else**: the owner asked at the second sitting whether the effects should be accelerated, and the decision on 2026-09-12 was not to start a GPU path but to take the one measurement ADR-006's escape clause asks for first. Depends on P-01, which is the timer it reports against, and on P-11, which is the reason this case is the interesting one: P-11 keys an effect's result by the value of its settings, so a blur whose radius has not changed costs nothing on the next frame, and **a blur whose radius is being dragged is a new value every frame and therefore the one case the cache cannot help at all.** No number is claimed for it anywhere in this pack today. Scope: drive the blur radius through a run of values the way a drag does - the window sends one per pointer move since W-07 - and record the frame time across that run on `verification/T-06_declared_fixture.json` at draft and at full, on the D-01 machine in a release build, with the cache hit rate beside it so that the zero is visible rather than assumed. Report p50 and p95 over the run and the worst single frame, because what makes a drag unpleasant is the worst frame in it and not the average. **Not in scope: making it faster.** If the answer is over budget the two CPU answers D-51 already names are the next unit and neither is this one - the blur at draft resolution for the duration of a drag, since a blur's cost scales with the pixels it touches, and a bounded preview radius - and both come before a second renderer because neither changes an exported pixel. Artifact: a page of its own under `verification/`, named for this unit and written when it runs, with the machine, the build and the pool size recorded per document 12, and a sentence saying plainly whether a person dragging that slider on that shot would find it usable. Exit: D-51's escape clause has the measured result it names, and ADR-006 is either left alone with evidence or reopened with evidence, rather than either one being argued from taste. **Done on 2026-09-12. `verification/P-14_blur_drag.md`, written by the `p14_blur_drag` measurement in `tests/p01_frame_trace.rs` under `--release --ignored`.** Sixty pointer moves at the page's own 0.1 step, sigma 4.1 to 10.0, the playhead held on one frame: draft is 34.8 ms at the median and 40.0 at its worst frame, which is inside the 24 fps budget; full is 52.8 and 59.7, which is 1.43x it. The blur is evaluated on all sixty frames and the harness asserts it, so the zero this entry predicted is shown rather than assumed. ADR-006 is left alone with evidence and neither CPU answer is started: D-51 carries the reading.

Not proposed, and why, so that the next reader does not re-derive it. SIMD in the sample and blend loops (document 32 T2-1): **P-01 has now answered this and the answer is no.** The tile loop is 1.3% to 7.8% of every frame it measured, so a blend loop that took no time at all would leave both fixtures outside the budget; reopening it needs a later P-01 re-run showing the loop grown into the largest stage, and even then it can be bit-exact across independent pixels only with fused multiply-add off and no reassociated sums, and that is a constraint to carry, not a reason to start. Half-precision cel storage (T2-2): P-06 is a sixteen-fold reduction that leaves export alone, against a two-fold one that changes values, so P-06 is decided first. A faster decoder or a new on-disk format (T2-3): a format decision with import, relink and provenance consequences, against document 04's anti-bloat rule, and document 32 itself suspects this build is near the decoder's floor; P-01 tests that suspicion for free. Multi-frame rendering (T1-4): competes with P-03(b) for the same twenty-four threads and should not be tuned blind; revisit with P-01's numbers after P-03. The 28 to 44 GiB application memory ceilings document 33 proposes as an experiment: document 08 line 43 declines to assume a machine's spare memory, D-40 was decided on that basis twice, and the same document's own repeat-frame table shows diminishing returns from a bigger source-only cache; a bigger cache of a different thing is P-08's question. Video, audio, FFmpeg, hardware decode and the hybrid engine document 33 recommends building toward: outside every tier in document 23, and document 33 says so itself. Growing the effect contract (document 32 section 6.3): the one declaration that would pay now, whether an effect is the identity for its current parameters, is a line per effect and should be added the next time `src/effects.rs` is open for another reason, not as a unit.

Housekeeping the research turned up, each a note and none an expected value. **Two of the three are now done.** `verification/D-37_decode_cost.md` priced 57 held cels at 473 MB on a four-bytes-a-pixel basis, which is what the PNG decoder writes rather than what `src/cache.rs` holds; the page now says 1.89 GB, `verification/derive_d37_reuse.py` prints both figures so that neither can be quoted for the other again, and `src/cache.rs`'s own module note, which flagged the discrepancy, is updated to match. That correction changes the shape of the decision rather than a digit in it, because 1.89 GB is past the 1 GiB D-40 later set as the default budget and 473 MB was comfortably inside it. `design/README.md` said not to design panels for masks, effects or cache state; the mask and effect halves of that sentence are removed, because D-12 was amended on 2026-09-06, both are built, and the window already has the panels. The cache half stands. Document 32 quotes 267.17 ms throughout where the checked-in page says 264.17; it is the owner's research document and is left as written, but nothing should be quoted from it without checking the page. What is **not** fixed, and is the owner's rather than an agent's: the redesign brief in document 05 lines 73 to 75 predates the panels the window now has, the same way that README did.

B-24g / the drag on the picture, built on 2026-09-22 out of the B-24c playtest. The owner passed all fourteen of B-24c's steps and reported two things none of them asks about: dragging a point lagged considerably, and a point pressed showed nothing. The first is a queue, not a renderer: `command` awaited one request per pointer event, a pointer reports about sixty times a second and `verification/B-08_preview_latency.md` measures a frame at about eighty milliseconds, so a two-second pull queued something like a hundred and twenty requests behind a renderer that could answer twelve, and the picture went on catching up after the hand stopped. Every request but the last described a shape nobody would look at, and each carries the whole path rather than a step of it, so the ones overtaken can be dropped unsent. The page now keeps one request in flight and the newest waiting, drops the rest, and holds `drag_end` until the queue is empty so that the transaction still closes on the last shape; the outline itself was always drawn by the page and still follows the hand at the pointer's own rate. The same queue serves the layer's own handles, which had the same shape of problem. The second is smaller: a point pressed, hovered over or tabbed to is now filled rather than hollow, and a handle with it, so a press says it landed before the hand has moved. What this does not do, and the honest ceiling to state: the picture still cannot answer faster than the renderer can draw it, about twelve frames a second on the reference shot, so the picture trails the outline by a frame. Making the picture itself keep up is P-04 in the performance section above, which is proposed and not accepted. `verification/B-24g_drag_playtest.md` is the owner's sheet; no generated table changed, because no command changed and the page's tests pass unaltered. **The owner passed it on 2026-09-23**, all ten steps, with nothing reported beside them.

P-15 / the mask while it is being drawn and set, built on 2026-09-22 out of the B-24g playtest. **Asked for by the owner twice in one sitting**: after B-24g they reported that dragging a mask point still lagged heavily, and after the first half of this entry landed they reported the points smooth and asked for the settings next. The first question they asked was which of a graphics card, more cores or faster threads to reach for; the answer was none of the three, and the reason it could be answered at all is the one thing this entry built before it changed anything. **The instrument first.** No measurement in this project covers the window: `verification/P-01_frame_trace.md` stops at a buffer in memory and says so, `verification/B-08_preview_latency.md` says the same of itself, and both are run on fixtures. The frame response now carries an `x-ms` header with everything the window spent on it, and the page splits a drag update four ways - the command, the window, the trip into the page, the paint - and prints the medians when the hand lets go. The owner dragged a mask point at Draft resolution and it answered **command 4.6 ms, window 17750.1 ms, trip 50.8 ms, paint 0.1 ms**, against the 2.502 ms P-01 measures for a warm draft frame of the reference shot. My own stated expectation before that reading was that the trip dominated and that the fix was to stop shipping 8.3 MB of pixels per update; it was wrong by three orders of magnitude, and the measurement is what said so. **What it was.** `src/mask.rs` asked each of a pixel's sixteen samples which side of the outline it was on, and each of those questions walked every edge: `width * height * 16 * edges`, which on a 1920x1080 layer with a mask of eight corners is 1,392 edges and about forty-five thousand million crossing tests, once per update per masked layer. Two things hid it for as long as they did - **no fixture P-01 traces carries a mask**, so its `layer mask` row reads 0.000 ms in all twelve of its tables, and **Draft resolution does not help**, because document 21 puts the mask in layer space before the transform and the composite, so a draft preview of a 1080p cel still rasterises the mask at 1920x1080. **What changed, in three parts.** The crossings of the outline with a sample row do not depend on where along the row the sample is, so they are worked out once for the row and the row is walked once, carrying only how many crossings the walk has passed; that is `point_inside`'s parity unchanged. D-77's expansion has no parity to carry, but it has a reach: a growth takes a sample that is inside **or** within the expansion of the outline, a shrink one that is inside **and** further than it, so an edge whose run of y lies further than the expansion from the sample row cannot change either answer and is dropped before any distance is worked out. And the rasterising and the feather's blur are both worked out a row at a time, where a row reads the outline and writes only its own pixels, so they now run across the pool `rayon` already owns. **Pixels: none moved**, and that is the half of this entry that is a fixture rather than a timer. `verification/B-06_mask_table.md` goes from 41 checks to 45. Two compare the new field against the sample-by-sample field pixel for pixel over twelve generated outlines; two more do the same for an expanded mask, grown by 2.3 pixels and shrunk by 1.7, against a field worked out a second time inside the fixture with **its own insideness test and its own distance**, written the other way round from the build's. All four require zero differing pixels and a largest difference of 0.000000 - `==` on the f32s and not a tolerance. **What it costs now.** `verification/P-15_mask_cost.md`, on the D-01 machine in a release build: on a 1920x1080 layer, a plain mask falls from **30,567.8 ms to 6.33** and an expanded one from **269,706.1 ms to 32.68**, which is 4,828 and 8,254 times; the smallest gain in the table, at 480x270, is 893 times. A second table separates the two causes by running the same call in a rayon pool of one thread: the threads are 5.6x of it for a plain mask, 10.1x expanded and 8.8x feathered, and the rule is the rest. **One thing that is not speed.** The mask panel's three numbers - Opacity, Feather, Expansion - waited for the hand to let go before the picture changed, which was the right trade when an expanded mask cost seconds a frame and is not one now. They send `mask.set` with `drag=1` per pointer move through B-24g's queue, so W-05's rule makes the whole pull one thing to undo and the queue drops whatever a faster hand overtook. Ceilings, stated rather than left to be found: an expansion as wide as the mask keeps every edge in the band and costs what it used to; two masks on a layer cost twice, because nothing here shares work between them; the blur was spread across the cores and not made cheaper; and **nothing is cached between updates**, so each one rasterises the mask again from its points, which is the next thing to reach for if a drag is still not fast enough. **No graphics card is involved**: ADR-006 and P-07 are untouched, and this entry is evidence that the first thing to do about speed was not a graphics card. `verification/P-15_mask_drag_playtest.md` is the owner's sheet. **The owner passed it on 2026-09-23**, all ten steps, with nothing reported beside them; the four numbers its step 2 asks for were not reported.

B-24h / a point added to a path that moves, and a history entry that says what it was, built on 2026-09-22 out of the B-24d playtest. **Asked for by the owner in four words** ("also tackle said limits") after passing B-24d, against the two Known limits that sheet wrote down. **The first.** A path with keys could not gain or lose a point, because a path's points are the path's and every key holds them all. D-79 in document 14 is the rule as built: the point is cut into the base and into every key at the same fraction along the same segment, in one command, so D-77's agreement is never broken and the file rule that refuses an uneven key is untouched. The cut is de Casteljau's; because it and document 20's interpolation are both affine in the control points, cutting then interpolating is the same curve as interpolating then cutting, so no key's shape and no in-between shape moves. That is checked against document 19's cubic written out a second time inside the fixture, at two hundred places along every segment, agreeing to about 1e-14 of a pixel, and against the drawn pixels: a straight segment cut moves **not one pixel**, a curved one moves 5 pixels of 3072 by one of the sixteen samples ADR-016 takes, which is `flatten` cutting two short curves at different places than one long one and is the one honest exception, with the ceiling written into the test. A straight segment gets a corner rather than a curve, so a square stays a square. `path.add_point` and `path.remove_point` serve a mask and a shape alike, through the helper B-25c already had, so D-78's half of the limit lifts with the same code and no second pair of commands. In the window: **the pen on the path adds a point where the hand pointed, the pen on a point takes that point off** (both were Alt and a click until D-80 rebound them on 2026-09-23, before this sheet was played, so that Alt could mean what it means in After Effects; the **+** and **-** keys on a tabbed point never changed). **The second.** Every mask edit there has ever been read "Set mask of N points" in the history, because the core takes the whole list of masks as one command. An entry now says what changed - "Add a point to Mask 1", "Move a point of Face at frame 6", "Set Mask 1's feather to 6 px", "Rename Mask 1 to Face" - worked out by comparing the list as it was with the list as it is, inside the core, so no command gained a field, no file changed, and a window that sends the same command by another route still gets the right entry. Nineteen of them are walked in the table. **Evidence:** `verification/B-24h_path_point_table.md` (38 checks, the curve, the keys, the pixels, the refusals, a save and reopen, the entries) and `verification/B-24h_panel_table.md` (the window's half); `verification/B-24h_path_point_playtest.md` is the owner's sheet. `verification/B-24c_panel_table.md` and `verification/B-24d_mask_key_table.md` regenerate with the new entry names and nothing else. Ceilings, stated: a point can be added anywhere along a segment but the fraction is chosen by the nearest place on the drawn curve, so a click on a nearly straight segment can land a pixel from where the hand meant; and removing a point is not claimed to move nothing, because it cannot - it is a different shape on purpose. **The owner passed it on 2026-09-23**, all ten steps, with nothing reported beside them.

B-24i / several mask points chosen at once, **built on 2026-09-23** out of the owner's word at the B-24f playtest ("I want to multi-select mask points, and be able to drag select as well"), after the owner accepted D-80 whole ("proceed with what you personally recommend"). D-80 in document 14 is the rule as built. The window's single lit point becomes a set: Shift and a click adds or removes one, a click on a mask's outline takes all of them, a box dragged across the picture takes everything inside it and Shift adds to that, Escape clears it. A drag moves every chosen point together in one entry of history, Shift holding it to one line; the arrow keys nudge them all; Delete takes them all off. The box is W-06's marquee, which already exists and already selects layers when no mask is chosen - that path is untouched. The only core work is `path.remove_point` taking several indices in one command, highest first, so deleting four points is one entry and not four. **Evidence:** `verification/B-24h_panel_table.md` grew six rows - three points named at once coming off the base and both keys together, the one history entry they make ("Remove 3 points from Mask 1"), the one Undo that brings all three back, a list naming a point the path has not got refused whole with nothing taken off, and a list given to `path.add_point` refused because each added point is a place along its own segment - and `verification/B-24i_point_playtest.md` is the owner's sheet, which is the **only** check on the choosing and the box, because that arithmetic is the page's and no test here can run the page's code. The history entry for a set moved now counts them, "Move 3 points of Mask 1", and moving all of them is "Move Mask 1's path". **The owner passed it on 2026-09-23**, all twelve steps, with nothing reported beside them.

B-24j / the bezier gestures, **built on 2026-09-23** out of the same word ("do bezier curves with these when I click and drag to modify curve amount, etc. make it 1 to 1 with AE's mask ecosystem"). D-80 is the rule. The pen pulls a mirrored pair of handles out of a point when it is pressed and dragged rather than clicked, which is the only way a curve can be drawn rather than assembled - today the pen places corners and nothing else. A smooth vertex keeps its two handles in line, so dragging one turns the other and leaves its length alone; Alt and a drag on a handle breaks the pair; Alt and a click on a vertex converts it, a smooth one losing its handles and a corner growing a mirrored pair under the drag. Adding and deleting a point move to the pen's hover, After Effects' Add Vertex and Delete Vertex, which is what frees Alt for converting. **This rebinds B-24h's Alt and a click**, so `verification/B-24h_path_point_playtest.md` is rewritten before the owner plays it; the **+** and **-** keys on a tabbed point are unchanged. No core work at all: every one of these writes points and handles through `mask.set_path` and `shape.set_path`. **Whether a point is smooth is read from its handles rather than stored** - both out and opposite along one line - so no file and no schema changed. **The keyboard got the half it was owed:** pulling a handle is a drag and a drag is a mouse, so **]** and **[** on a point that has been tabbed to lengthen and shorten its pair of handles along the line between its neighbours, a tenth of the way to them a press; without them, curving a path would have been the first thing in this window a person with no mouse could not do at all. **Evidence:** `verification/B-24j_bezier_playtest.md`, which is the **only** check - every gesture here is arithmetic in `app/ui/index.html` and no test in this repository can run the page's code, which is stated in the sheet rather than papered over. `verification/B-12c_keyboard_table.md` grew two rows, one for choosing several points and one for curving a path, each naming the keyboard twin of the gesture. **The owner passed it on 2026-09-23**, all eleven steps, with nothing reported beside them.

B-24k / the free transform box around a mask, **proposed on 2026-09-23** and deliberately kept out of D-80. In After Effects a double-click on a mask with the selection tool puts a box around it whose corners scale it and whose outside turns it, with the anchor at its middle. It is a different thing from every gesture in B-24i and B-24j: it moves every point of a mask by one transform rather than moving points, it needs a box, a pivot and a modifier for each of scale, turn and skew, and it has no bearing on whether a point can be chosen or a curve drawn. Named here so that "1 to 1 with After Effects" has somewhere to put it rather than being quietly assumed complete without it. Nothing is built against it. **The owner chose it next on 2026-09-23** ("let's begin with 1 then 2", 2 being R-01), and the agent proposed it the same day as **D-81** in document 14. It moves, scales and turns, with no skew, as After Effects' mask box does. **The owner accepted D-81 the same day ("proceed") and B-24k is built the same day**: the box is the page's own arithmetic, sent through `mask.set_path` and `shape.set_path` one drag to one history entry. `verification/B-24h_panel_table.md` grew three rows for that bargain; `verification/B-24k_free_transform_playtest.md` is the owner's sheet and the only check on the arithmetic. **Passed on 2026-09-23** ("works; work on limits"), all fourteen steps.

B-24l / the free transform box's limits, **built on 2026-09-23 against D-81a**, the second half of the owner's answer to B-24k. The box holds several masks (Shift and a double-click), has keys (arrows move; keypad + and - turn, and scale with Alt), and Undo brings the box back with the shape. Skew, perspective, a whole-layer box and a saved box stay out, as in After Effects. On the way, B-24g's drag queue was found dropping the last step of all but one layer in a several-layer drag; it now keeps each property's newest request. `verification/B-24h_panel_table.md` is 26 of 26; `verification/B-24l_box_limits_playtest.md` is the owner's sheet.

B-26 / null layers, asked for by the owner on 2026-09-23 ("think we should add a null layer just in case?"), split as B-23 was, and put ahead of R-01 at the owner's "proceed".

B-26a / the rule and its fixtures before code: D-82 in document 14, FX-NULL-001 to 007 and 020 to 029 in document 25, from `tools/null_reference.py`. **Written on 2026-09-23; D-82 accepted by the owner the same day ("proceed").**

B-26b / the core, once D-82 is accepted: the `null` kind in the model and the file, left out of every frame, the ten refusals, `layer.add_null`, and a table that walks FX-NULL-001 to 029. **Built on 2026-09-23:** `tests/b26b_null.rs` writes `verification/B-26b_null_table.md`, 34 of 34 checks.

B-26c / the window: New Null in the Layer menu (Ctrl+Alt+Shift+Y), its outline and anchor on the picture, its grey strip on the timeline, Ctrl+Alt+Y no longer answering with Shift held, and a playtest sheet. **Built on 2026-09-23:** `verification/B-26c_panel_table.md`, 10 of 10, and `verification/B-26c_null_playtest.md`, 11 steps. **Passed by the owner on 2026-09-23** ("pass for null playlist, it works").

B-27 / the pick whip, asked for by the owner on 2026-09-23 ("I want to be able to alt click, then click drag from stopwatch to other layers' or null value of say position, so that one is parent, etc connected like AE does"), split as B-26 was, and put ahead of R-01 at the owner's "proceed".

B-27a / the rule and its fixtures before code: D-83 in document 14, FX-WHIP-001 to 020 in document 25, from `tools/pickwhip_reference.py`, which writes `Fixtures/pickwhip/`. **Written on 2026-09-23; D-83 accepted by the owner the same day ("proceed").**

B-27b / the core, once D-83 is accepted: `property.link`, which writes a link's text by D-83's rule as one step of history and refuses a property linked to itself, and a table that walks FX-WHIP-001 to 020. **Built on 2026-09-23:** `expr::link` writes the text; `tests/b27b_pickwhip.rs` sets it as the expression, checks it is one step that Undo takes back, evaluates it on five frames, and checks the link survives a rename and a save; `verification/B-27b_pickwhip_table.md`, 109 of 109 checks.

B-27c / the window: the parent whip on each layer's row and beside the Parent list, the value whip beside an expression box, the line that follows the hand, the lit row and its label, Escape to cancel, and a playtest sheet. **Built on 2026-09-23:** `verification/B-27c_panel_table.md`, 22 of 22 (each of FX-WHIP-001 to 013 sent as the page sends it writes the fixture's text as one step Undo takes back; the refusals; the parent whip sends what the Parent list sends); `verification/B-27c_pickwhip_playtest.md`, 13 steps. **Passed by the owner on 2026-09-23** ("works!").

B-28 / Import a cut from its XDTS timesheet, document 15's reuse entry R-01 taken up by the owner on 2026-09-23 ("let's proceed with the first build"), split as B-27 was. The owner has no real timesheets and chose it to prepare for learning how a timesheet drives a compositor, so the fixtures are synthetic and the sample cut is written to be learned from.

B-28a / the rule and its fixtures before code: D-84 in document 14, FX-XDTS-001 to 040 in document 25, from `tools/xdts_reference.py`, which writes `Fixtures/xdts/`, and `research/2026-09-23_timesheet_primer.md` for the owner. **Written on 2026-09-23; D-84 accepted by the owner on 2026-09-24 ("cool, proceed"), and FX-XDTS-028 added the same day when B-28b's check against OpenToonz found Clip Studio Paint's entries before frame 0, which the owner chose to carry in.**

B-28b / the core, once D-84 is accepted: first, a check of D-84's reading of holds against OpenToonz's BSD-3 reader, reported before any code, with the commit read; then the reader, the `timesheet` record on a layer in the model and the file, the sixteen TIMESHEET IDs in document 28, and a table that reads every FX-XDTS cut and compares it with `Fixtures/xdts/expected_xdts.json`. **Built on 2026-09-24:** the check is in D-84 (OpenToonz 232cdf7d80, which added FX-XDTS-028 by the owner's choice); `src/timesheet.rs` reads a cut and makes it one undo step; `tests/b28b_timesheet.rs` writes `verification/B-28b_timesheet_table.md`, 65 of 65 checks since B-28f added its text columns and `sheet_text` rows, 60 before (every cut's composition, layers, timing, drawings and notes as the reference has them, the seven refusals, what a person reads for each of the sixteen IDs, the import as one step, and the record in the file).

B-28c / the window: Import Cut, which asks for a folder and sends `timesheet.import`; the notes in the notes list and the summary on the status line; the record shown as one line in a layer's inspector; the command in document 24; a panel table; and a playtest sheet on FX-XDTS-040. **Built on 2026-09-24:** Import cut... in the media bin asks for a folder and sends `timesheet.import`; the cut is opened, its notes go to the notes strip and a summary to the status line, and a layer's inspector has a Timesheet line. `verification/B-28c_panel_table.md` is 15 of 15, and `verification/B-28c_timesheet_playtest.md`, 13 steps. **Passed by the owner on 2026-09-24** ("nice, it works for both playtests").

B-28d / the Sheet view, D-84a: the Sheet tab in the timeline panel, its grid worked out in the window's Rust side and asked for as `/sheet`, a panel table that compares FX-XDTS-040's sheet frame by frame with document 25's table, and a playtest sheet. **Written and built on 2026-09-24:** `verification/B-28d_sheet_table.md` is 57 of 57, and `verification/B-28d_sheet_playtest.md`, 12 steps. **Passed by the owner on 2026-09-24** ("nice, it works for both playtests").

B-29 / workspaces, D-85: four places holding panels that move and stack, the Sheet as a panel of its own, and the Workspace list with Standard, Timing, Save workspace and Reset workspace. The page alone; no project file, fixture or command changes. **Built on 2026-09-24:** the full workspace run is 140 passed, 0 failed. `verification/B-12c_keyboard_table.md` lists the four new controls and loses the two retired ones. `verification/B-28d_sheet_table.md` is still 57 of 57, its page row now finding the Sheet panel. `verification/B-29_workspace_playtest.md`, 13 steps, is the check: no test here can run the page. **Passed by the owner on 2026-09-24** ("perfect!", with a photograph of the Timing workspace).

B-28e / writing into the Sheet, D-84b: FX-SHEET-001 to 016 in document 25 first; then `exposure.write` in the window's Rust side, one `SetExposureSpans` a cell, and a table that plays every case from document 25 itself; then the page's cell cursor, typing, Enter, x and Delete, and a playtest sheet. **Built on 2026-09-24**, after the owner accepted D-84b ("proceed"): the full workspace run is 141 passed, 0 failed. `verification/B-28e_sheet_writing_table.md` is 23 of 23: all sixteen FX-SHEET cases, each with what went into the history and Undo giving Before back, and the refusals and the missing-drawing warning in words. The command map, page and keyboard tables list `exposure.write` and the Sheet as a Tab stop. `verification/B-28e_sheet_writing_playtest.md`, 13 steps. **Passed by the owner on 2026-09-24** ("works").

B-28f / dialogue and camera columns, D-84c: the second reader and `Fixtures/xdts/expected_xdts.json` first, with FX-XDTS-019 and the amended FX-XDTS-014, 034 and 040; then the core's import and `sheet_text` in the project file, B-28b's test comparing text columns, `/sheet` and the Sheet showing them, a table on FX-XDTS-040 and a playtest sheet. **Accepted on 2026-09-24** ("proceed"); the fixtures are regenerated first. **Built the same day, and passed by the owner the same day ("works")** after walking the playtest sheet: `src/timesheet.rs` reads fields 3 and 5 as text columns and `src/persist.rs` keeps them as the composition's `sheet_text`; `verification/B-28b_timesheet_table.md` is 65 of 65 (every fixture's text columns as the reference has them, written, reopened, a kind this build does not know kept, a malformed entry refused); `verification/B-28f_sheet_text_table.md` is 9 of 9 (FX-XDTS-040's Dialogue and Camera cells frame by frame against document 25, and Undo taking them with the cut); the B-28c table now expects 2 notes for the sample cut; `verification/B-28f_sheet_text_playtest.md` is the sheet the owner walked.

B-28g / printing the Sheet, D-84d: FX-PRINT-001 to 004 in document 25 first; then `/sheet/print`, the printable page written by the window from the Sheet's own cells, a table that checks its pages against document 25 and its cells against `/sheet`, the sample cut's printable page in `verification/`, the page's Print... button and Ctrl+P, and a playtest sheet that prints to PDF. **Accepted on 2026-09-24** ("proceed"); the cases in document 25 are the fixtures. **Built the same day, awaiting the owner's playtest:** the window answers `/sheet/print` with the whole printable page, written from the same grid as `/sheet`, six seconds a page in two halves of three, sized to the height of whatever paper the dialog chooses; the Sheet's **Print...** and Ctrl+P put it in a hidden frame and open the system print dialog; `verification/B-28g_sheet_print_table.md` is 18 of 18 (each FX-PRINT case's header, halves, columns and end line against document 25, every printed cell against the Sheet's, and nothing to print without drawing columns); `B-28g_sheet_print_040.html`, `B-28g_sheet_print_300_frames.html` and `B-28g_sheet_print_30_fps.html` are the printable pages, which print one Sheet page to a sheet of paper on both Letter and A4 (checked with Edge's headless print to PDF); `verification/B-28g_sheet_print_playtest.md` is the sheet for the owner. **First playtest, the same day:** the owner's PDF was a bare table in seven pages, with the date and `tauri.localhost` in its corners, because the window's content security policy refused the styles of a page made at run time. Fixed the same day: the look is in the page's own style, between its `sheet paper` marks, and the printable page copies it (the interface stays one file, as B-11 requires); the pages are printed from the window's own page; the paper has no margins for the dialog to print on. Checked by printing under the same policy with Edge's headless print to PDF, headers and footers left on: one Sheet page to a sheet, Letter and A4. The table is still 18 of 18. **Then, the same day, closer to a studio timesheet** (the owner asked for rounds of print, look and improve): a ruled title block of labelled boxes (CUT, TIME, RATE, SHEET and an empty MEMO), rows of one height, bold drawing numbers, a heavy line at each second with its frame number bold, a thin line and a ▶ at each half second, fixed widths for the frame and text columns; and `B-28g_sheet_print_reference_shot.html`, the project the owner printed, kept beside the others. What is printed in each cell is unchanged; the table is still 18 of 18.

B-28h / the paper sheet laid out as a studio's, D-84e: FX-PRINT-005 to 008 and the amended FX-PRINT-001 to 003 in document 25 first; then frames counted from 1 on paper, the second numbers, the empty Action column, six cel columns, the Print the Sheet window with 3 or 6 seconds a page and black or red, B-28g's table reading the new cases, the printable pages kept in `verification/`, and a playtest sheet. **Accepted on 2026-09-24** ("proceed"); the cases in document 25 are the fixtures. **Built the same day:** `/sheet/print?seconds=&colour=` writes rows from 1 with a sec column, the Action column (a Sheet text column of kind action if there is one), the drawings padded to six cel columns, one or two halves a page, and `class="red"` for the red style, which colours the form's rules, headings, numbers and labels and leaves the marks and entries black; any other number of seconds writes no pages and says `A page holds 6 or 3 seconds.` Print... and Ctrl+P open a Print the Sheet window whose choices the window keeps beside the preferences. `verification/B-28g_sheet_print_table.md`, 42 of 42; the 3-second and red pages kept beside it; checked by printing under the window's policy with Edge's headless print to PDF, one Sheet page to a sheet on Letter and A4. Playtest: `verification/B-28h_sheet_paper_playtest.md`.

B-28i / the title block, D-84f, after B-28h: FX-PRINT-009 and 010 and FX-PRINT-001's amended header in document 25 first; then `sheet_details` in the project file, Composition Settings' four boxes, the two-row title block, B-28g's table checking the boxes, and a playtest sheet. **Accepted on 2026-09-24** ("proceed"); the cases in document 25 are the fixtures. **Built the same day:** a composition's `sheet_details` (episode, scene, cut, animator) in the model and the project file, written only when one of them is, a non-string refused with its place and an unknown key inside kept; `SET_COMPOSITION_SETTINGS` carries the four, so one Undo takes them back with the rest; Composition Settings (Ctrl+K) has a Sheet heading with the four boxes; and the printed title block is two rows, NAME, EPISODE, SCENE, CUT and ANIMATOR above and TIME, RATE, SHEET and MEMO below. `verification/B-28g_sheet_print_table.md`, 49 of 49, with `B-28g_sheet_print_title_block.html` kept beside it; `tests/b09_persistence.rs` checks the refusal and the unknown key; checked by printing under the window's policy with Edge's headless print to PDF, one Sheet page to a sheet on Letter and A4. Playtest: `verification/B-28i_title_block_playtest.md`, passed by the owner the same day ("works").

B-28j / the Action column and key drawings, D-84g, after B-28i: FX-SHEET-017 to 024, FX-PRINT-011 and FX-XDTS-040's amended Sheet in document 25 first; then `SET_SHEET_TEXT` and `SET_KEY_DRAWINGS` in the core, `key_drawings` in the project file, the Sheet's Action column and K, the circles on screen and on paper, B-28e's and B-28g's tables reading the new cases, and a playtest sheet. **Accepted on 2026-09-24** ("proceed"); the cases in document 25 are the fixtures, moved into its Sheet writing and Sheet printing sections the same day. **Built the same day**: `verification/B-28e_sheet_writing_table.md` 36 of 36, `verification/B-28f_sheet_text_table.md` 10 of 10 and `verification/B-28g_sheet_print_table.md` 52 of 52, the page for FX-PRINT-011 kept as `verification/B-28g_sheet_print_keys.html`. The Action column is on every Sheet with drawing columns; a note sits on one frame. Playtest: `verification/B-28j_action_keys_playtest.md`, passed by the owner on 2026-09-25 ("works").

B-30 / Line smoothing, document 15's reuse entry R-02 taken up by the owner on 2026-09-25 ("ok, let's do line smoothing"), as the fourth effect in the Effects panel.

B-30a / the rule and its fixtures before code: the proposal's three pictures in `verification/B-30a proposal/`, D-86 in document 14, the rule in document 21, FX-SMOOTH-001 to 022 in document 25 from `tools/smooth_reference.py`, which writes `Fixtures/smooth/`, and OpenToonz's licence in `docs/third_party/OpenToonz-LICENSE.txt`. **Written on 2026-09-25; D-86 accepted by the owner the same day ("proceed with B").**

B-30b / the core: `core.line_smooth` in the effect model with its two settings, their ranges and their keys, the port with OpenToonz's notice, the file reading and writing it, `effect.add` putting it at the top of the stack, the packaged build carrying OpenToonz's licence, and a table that renders every FX-SMOOTH case against `Fixtures/smooth/expected_smooth.json`. **Built on 2026-09-25**: `verification/B-30b_line_smooth_table.md`, 54 of 54, every FX-SMOOTH case within 2.5e-7 of the reference. The check that `effect.add` puts it at the top is the window's, and comes with B-30c.

B-30c / the window: Line Smoothing in the Effects panel's list with its Softness and Threshold boxes, a panel table, and a playtest sheet on a real drawing. **Built on 2026-09-25**: `verification/B-12a_effects_table.md` checks it lands at the top of the stack at softness 50 and threshold 10, `verification/B-12b_state_fields_table.md` (75 of 75) that the two boxes are the settings the command reads, and `verification/B-30c_line_smooth_playtest.md` is the sheet, on `verification/B-30c_line_smooth_drawing.png`. **Passed by the owner on 2026-09-25 ("works")**, after the agent also rendered two of the owner's own drawings through it at softness 0, 50 and 100 and judged 50 the right default. B-30 is done.

B-31 / Selective colour blur, the first of document 15's reuse entry R-03, the cel colour operations, taken up by the owner on 2026-09-25 ("Selective colour blur"), as the fifth effect in the Effects panel.

B-31a / the rule and its fixtures before code: the proposal's three pictures in `verification/B-31a proposal/`, D-87 in document 14, the rule in document 21, FX-SELBLUR-001 to 024 in document 25 from `tools/selblur_reference.py`, which writes `Fixtures/selblur/`, and F's Plugins' licence in `docs/third_party/F-s-PluginsProjects-LICENSE.txt`. **Written on 2026-09-25; D-87 accepted by the owner the same day ("accept").**

B-31b / the core: `core.selective_color_blur` in the effect model with its two settings, their ranges and the blur's keys, the port with F's Plugins' notice, the file reading and writing it, `effect.add` putting it at the top of the stack and Line Smoothing below it, the packaged build carrying F's Plugins' licence, and a table that renders every FX-SELBLUR case against `Fixtures/selblur/expected_selblur.json`.

B-31c / the window: Selective Colour Blur in the Effects panel's list with its Blur box and up to eight colour boxes, a panel table, and a playtest sheet on a real drawing.

## Reuse from open source, proposed on 2026-09-22 and not yet accepted

This section is a plan, not a schedule. It was written on 2026-09-22 from `research/2026-09-22_github_reuse_survey.md`, which is research and authorises nothing. The owner asked whether anything already built could be branched from or taken in. The answer for whole applications is no, and it is recorded in document 30; the answer for parts is the four entries below, each of which stays PROPOSED until the owner accepts it by name in document 14.

Rules that bind every entry. **A port is a dependency, so document 10 applies in full**: the origin URL, the exact commit, the licence text and the copyright notice are recorded before any code is written, and the notice ships with the build. **Nothing here is copied from a GPL, LGPL or MPL project**; Friction, Natron, Synfig, Glaxnimate and Blender are reference only, which is what document 30 now says. **The fixtures come first**, as they did for B-24a and B-25a: a ported algorithm is specified, drawn and fixtured against our own expected values, never accepted because the original passes its own tests. A port that cannot be explained in a proposal is not ready to be built.

R-01 / The exposure timesheet read (XDTS). PROPOSED, and the highest value of the four for anime work, because it is the one thing on the list that removes work the owner cannot do at all today. CELSYS publishes the format as a public specification PDF, so the parser is written from the specification rather than from anyone's code. Three permissive readers exist to check ours against: OpenToonz `toonz/xdtsio.cpp` and `opentoonz/xdts_viewer`, both BSD-3, and `lunafuse/vdts-editor`, MIT, which is also the only reader found for the older TDTS. No Rust crate exists. Its fixture would be a timesheet in and an exposure out, and the round trip back. Document 31's roadmap places it at G2b; document 04 has no line for it yet, and adding one is part of the decision. **Taken up on 2026-09-23 as B-28, under D-84 (proposed).**

R-02 / Line smoothing. PROPOSED. OpenToonz `common/trop/tantialias.cpp` is about four hundred lines of BSD-3 and uses the published method that OLM's page says OLM Smoother uses. `loilo-inc/smooth` and its OpenFX port `cr-market/anime-smoother-ofx` are Apache-2.0 and a different method, which makes them a second opinion rather than a second copy. This is document 31's D-06 and its G3a. OLM's own tools are Apache-2.0 but ship as built plugins only, so there is no OLM source to take. **Taken up on 2026-09-25 as B-30, under D-86 (accepted).**

R-03 / The cel colour operations. PROPOSED. `bryful/F-s-PluginsProjects` is MIT and holds about forty effects by a working anime creator; `cr-market/FsPluginsOFX` ports eight of them, including main-line repaint, selective colour blur and select colour, in a form separated from the After Effects SDK. This is document 31's D-06, D-07 and D-09. The smallest useful first behaviour is one operation, not the set. **Taken up on 2026-09-25 as B-31, selective colour blur first, under D-87 (accepted).**

R-04 / The blur and glow family. PROPOSED, and last of the four because the existing Gaussian blur already covers part of it. OpenToonz `stdfx` holds directional, radial, rotate, line and motion blur, erode and dilate, glow, bloom and glare, and the RGB and HSV keys, all BSD-3, and its `igs_*` cores depend on nothing but the C++ standard library. This is document 31's D-07, D-08 and D-10.

Not proposed, and why, so that the next reader does not re-derive it. Toon Dilate and distance gradation: no source exists, and OLM's page describes the method well enough that writing it is smaller than searching further. KiraKira and paraffin gradients: nothing was found at all. An OpenFX host: the ASWF headers and its HostSupport library are BSD-3 and would be the reference, but no Rust host exists and the job is large, so it stays where document 31 put it, at G3e behind a plugin the owner actually needs. Lottie: `velato` is a permissive Rust reader worth using as a model, but the format is only worth reading when vector interchange is in scope. A whole-application fork: refused in document 30 on 2026-09-22.

The libraries that would come with later features, none of them proposed here and each named so the search is not repeated: `kurbo` for miter and bevel joins, caps and trim paths, and `i_overlay` for merge paths, when D-78's exclusions are lifted; `harfrust` and `skrifa` for text layers, drawn through our own shape rasteriser so that text and shapes match; `wgpu` if ADR-006's stopwatch fires. Written ourselves rather than taken: gradients, repeaters, the `.cube` lookup table, frame blending and the waveform display, each because the dependency is larger than the code it saves. Avoided: `symphonia`, which is MPL-2.0.

## Definition of ready and done

Ready: requirement, input fixture, expected behavior, prerequisites and dependency status are understood, and the expected values already exist independently of any implementation.

Done: behavior works, relevant fixtures pass, the verification artifact exists and the owner has reviewed it, documentation matches actual behavior, and the report states what was not run. A partial interface is not completion.

For the first coding session use B-01. Do not attempt several backlog items in one session because they look small.

Related documents: 03, 11, 12, 14, 22, 23 and 25.
