# Start here

Version 0.3 | 2026-09-04 | Accepted baseline

## What this is

The planning baseline for a cel exposure and finishing compositor for 2D animation, built and owned by one person, distributed as open source, running offline on Windows.

This is a specification, not evidence that anything has been implemented. No test in this pack has been run. No performance number in it has been measured.

## What changed in version 0.3

Version 0.2 was a well-specified plan resting on two things that turned out to be false: that the open decisions would be resolved by measurement, and that a human would review the code. Version 0.3 corrects both.

Every gating decision is now closed. Windows 11 x64 only, on a declared reference machine. Open source. Rust core with rayon and a Tauri interface in HTML and CSS. CPU-only tile-based rendering, with GPU deferred behind a stopwatch. The owner draws the reference shot. See document 14.

The first milestone was cut roughly in half. G1 splits into G1-core, which is import, exposure, layers, transforms, undo, save and recovery, export, color and offline operation, and G1-rest, which is masks, effects and the preview cache. The parked work is fully specified and carries explicit revisit triggers. See D-12 and document 23.

Most importantly: the owner has no programming background and cannot read the code. Human source review is therefore removed from the quality system and replaced by verification against independent fixtures and artifacts a non-programmer can judge. Document 12 specifies this and is the most load-bearing document in the pack. See D-13.

## How to read this

Read document 12 first. Nothing else in this pack means what it appears to mean without it, because it defines what "done" is and how anyone knows a claim is true.

Then product intent: 01 charter, 02 workflows, 03 requirements, 04 scope, 23 priority matrix.

Then contracts: 18 ADRs, 19 data model, 20 time model, 21 rendering math, 24 commands, 26 undo, 28 diagnostics.

Then delivery: 07 project format, 11 verification, 22 reference shot, 25 fixture catalog, 29 build, 10 legal, 13 delivery.

Research context: 16, 30 and 31. On 2026-09-22 the owner asked whether anything already built could be branched from or taken in: `research/2026-09-22_github_reuse_survey.md` is the answer, document 30 records it (no application to fork, because the layer-based ones are all copyleft; parts may lawfully be ported), and document 15's last section proposes four ports - the XDTS timesheet read, line smoothing, the cel colour operations and the blur and glow family - each awaiting the owner.

Before any code, read root `AGENTS.md`, `CLAUDE.md` and `CONTEXT.md`.

## Status vocabulary

ACCEPTED means the decision is the working contract until changed through document 14. PROVISIONAL means a recommended default still awaiting evidence. DEFERRED means deliberately postponed with a trigger, not rejected. PARKED means specified and deliberately not being built. OPEN means unresolved.

Performance thresholds remain unmeasured. No benchmark, compatibility level or legal clearance is implied anywhere in this pack.

---

## Current gate

**B-01 is recorded.** The reference shot is drawn and committed at `Fixtures/reference_shot/`, SP-01, SP-03, SP-04, SP-05 and SP-06 have been run, SP-07 adds the real-shot measurement ADR-006's exit condition asks for, and ADR-003, ADR-004 and ADR-006 are confirmed with none reopened. The artifact is `spikes/B-01_G0_spike_report.md`.

The two risks that decided the Tauri interface are settled. SP-06 found the webview alters nothing, in readback and on the physical display. SP-05 found frames do reach the viewer fast enough, but with almost no margin: 39.54 ms per frame at full resolution against a 24 fps target, which is 3.3 times the cost of compositing the frame in the first place. Neither failed, so ADR-004's native-surface fallback is not triggered, but the document 27 cache and a draft-resolution preview are load-bearing rather than optional.

Amended 2026-09-05. Those two halves went different ways, and the difference is worth stating so this paragraph is not read as licence to build the cache. The draft-resolution preview is now the viewer's default and is decided: D-33. The document 27 cache is not in G1-core at all - R-06a says "no bounded cache; render on demand and accept the cost", and B-08b is PARKED under D-12 with a revisit trigger in document 23. What absorbs the missing margin in G1-core is therefore the draft default plus D-32, which lets playback drop frames rather than stretch the clock, and not a cache.

Amended again later the same day, and this is the part to read carefully. That revisit trigger has fired. `verification/B-08_preview_latency.md` measured the production preview path - not a spike - and recorded 12.2 frames per second in draft and 10.0 at full resolution against a 24 fps target, with about three quarters of every frame spent reading and decoding cels. Decoding costs the same at both resolutions, because a drawing is decoded at its own size before anything scales it, so the draft default cannot reach that cost. The trigger firing did **not** unpark the cache by itself, and this paragraph was never licence to build it: it was recorded as D-37 and left to the owner, who answered it the same day and unparked it. B-08b is being built, bounded by D-37 and ADR-015 to a cache of decoded cels with a memory ceiling, in the preview path only. G1-core is unchanged and nothing in it is unmet - D-32 already says what playback does when it cannot keep up, and now there is a number saying how often that will be, and something being done about the number. Masks and effects are still parked; their triggers have not fired.

**G0 is passed. The owner opened the gate on 2026-09-04.** The instruction was "open the gate; proceed to B-02". Production implementation is under way.

This decision was the owner's, as it had to be. An agent must not open a gate on its own reading of the evidence, and that rule still stands for G1 and G2.

**G1 is passed. The owner opened the gate on 2026-09-15.** The answer was "Pass now, gaps noted": the three gaps are written down as accepted and open in D-54. The owner accepted the measured memory headroom the same day (D-55), which is the other half of what document 04 asks before G2. B-13 is done by the owner's word on 2026-09-16. Current work is B-14 in `Markdown/15_Initial_Backlog.md`, the expressions of R-13. B-14a is done: the owner accepted D-59, the answer to D-10, on 2026-09-16. B-14b, the evaluator, the file and the export refusal, is built on 2026-09-16 and T-12 passes. The owner accepted B-14b ("works") the same day. B-14c, expressions in the window, is done by the owner's word the same day, and with it B-14.

**G2 is passed, and G3 is entered for B-15 and B-16. The owner decided both on 2026-09-16** (D-60). B-15, collect and package (R-14), is done. B-15a is done: the owner accepted D-61, the packaging contract, on 2026-09-16. B-15b, collecting, the manifest, the hash and checking, is built on 2026-09-16 and T-13 passes: `verification/B-15b_package_table.md`, 30 of 30. B-15c, the window, is built on 2026-09-16 and done on 2026-09-17 by the owner's word ("B-15c playtest works"), after walking `verification/B-15c_package_playtest.md`. Current work is B-16, other formats, EXR first. B-16a is done: the owner accepted D-62, the EXR contract, on 2026-09-17. B-16b, the EXR reader and writer, is built on 2026-09-17 and T-14 passes for EXR: `verification/B-16b_exr_table.md`, 71 of 71, and OpenEXR's own check of the exported files, `verification/B-16b_exr_export_check.md`, 60 of 60. B-16c, EXR in the window, is built on 2026-09-17: `verification/B-16c_panel_table.md`, 18 of 18, and done on 2026-09-18 by the owner's word ("pass the b-16c, it worked"), after walking `verification/B-16c_exr_playtest.md`. B-16 is done for EXR. Audio (WAV) is deferred by the owner on 2026-09-17 (D-63). **Next, by the owner's decision on 2026-09-17** (D-64, D-65): foundations first, so the exposure list's overhaul and anime-specific processing wait, and G3 continues with B-17 adjustment layers, then B-18 precompositions, then B-19 the graph editor's gaps. B-17a is done: the owner accepted D-66, the adjustment layer, on 2026-09-17. B-17b, the adjustment layer in the core, is built on 2026-09-17: `verification/B-17b_adjust_table.md`, 46 of 46. B-17c, the adjustment layer in the window, is built on 2026-09-17 and done on 2026-09-18 by the owner's word ("B-17c playtest works"), after walking `verification/B-17c_adjust_playtest.md`. B-17 is done. B-18a, the precomposition specification, is done: the owner accepted D-67 on 2026-09-18 ("proceed"). B-18b, the precomposition in the core, is built on 2026-09-18: `verification/B-18b_precomp_table.md`, 70 of 70, FX-PRE-001 to 015 within 1e-6. B-18c, the window, is built on 2026-09-18: `verification/B-18c_panel_table.md`, 14 of 14, and done the same day by the owner's word ("B-18c playtest works"), after walking `verification/B-18c_precomp_playtest.md`. B-18 is done. Current work is B-19, the graph editor's gaps (D-65). The owner named its scope on 2026-09-18 ("all 10 please; along with adding animation/keyframing stopwatches to effects as well"), and document 15 splits it into B-19a to B-19g. B-19a, the six gaps that change no file, is built on 2026-09-18: `verification/B-19a_panel_table.md`, 11 of 11, and the playtest sheet `verification/B-19a_graph_playtest.md` was played by the owner on 2026-09-19 and works. B-19b, the specification for keyframed effect settings, is written on 2026-09-18: D-68 in document 14 is PROPOSED with its fixtures, FX-FXK-001 to 009, and the owner accepted it the same day ("proceed"). B-19c, keyframed effect settings in the core, is built the same day: `verification/B-19c_fxkey_table.md`, 47 of 47. B-19d, the window half, is built the same day and awaits the owner's playtest: `verification/B-19d_panel_table.md`, 21 of 21, and `verification/B-19d_fxkey_playtest.md`, played by the owner on 2026-09-19: it works. B-19f, the core for D-69, is built the same day: `verification/B-19f_keykind_table.md`, 36 of 37, and the one is a fault found in the fixture FX-KIND-005 that **awaits the owner's decision** (document 15, B-19f). B-19g, the window for D-69, is built the same day: `verification/B-19g_panel_table.md`, 24 of 24, and `verification/B-19g_keykind_playtest.md` was played by the owner on 2026-09-19 and works. That is all ten graph items and the effect stopwatches built; **Next is B-20, reference audio, which the owner asked for on 2026-09-19.** D-71 and ADR-018 were accepted by the owner on 2026-09-19. B-20a, the fixtures, and B-20b, the core, are done: `verification/B-20b_audio_table.md` is 46 of 46. B-20c, audio in the window, is built on 2026-09-19: `verification/B-20c_panel_table.md`, 20 of 20. It is done the same day by the owner's word ("audio works now, along with the new design look of the layer bars on the timeline"), after walking `verification/B-20c_audio_playtest.md`; the full workspace run is 112 passed, 0 failed. B-20 is done. B-21a is done: the owner accepted D-72, more drawing formats in and GIF, animated PNG and MP4 out, on 2026-09-19, with FX-FMT-001 to 030. B-21b, the new drawing formats in, is built on 2026-09-19: `verification/B-21b_formats_table.md`, 11 of 11, and `verification/B-21b_panel_table.md`, 13 of 13; it is done the same day by the owner's word ("B-21b playtest works, keep the refusal"). B-21c, GIF and animated PNG out, is built and done on 2026-09-19, the owner's playtest passed. **B-21d, MP4 out, is built and done on 2026-09-19**: `verification/B-21d_mp4_table.md`, 16 of 16, and `verification/B-21d_panel_table.md`, 5 of 5. The owner closed D-30 the same day ("road 1, Media Foundation confirmed") and passed the playtest. B-21e, the same MP4 through an installed ffmpeg on systems that are not Windows, is built the same day at the owner's word; its three rows are in the same table, now 19 of 19, and it has never been run on macOS or Linux. **D-73, export choices, was accepted by the owner on 2026-09-19 and B-22b (the core, `verification/B-22b_choices_table.md`, 20 of 20) and B-22c (the window, `verification/B-22c_panel_table.md`, 13 of 13) are built against it; B-22c is done by the owner's playtest the same day. D-73a, an amendment that ends the coloured specks in a dithered GIF, was accepted by the owner the same day and B-22d is built against it (FX-FMT-053 to 055; 050 to 052 retired); it awaits the owner's eye, `verification/B-22d_specks_playtest.md`. The owner set the next work the same day (D-75): solid layers (B-23), then masks with the pen and feather (B-24), then shape layers (B-25), which narrows document 04's exclusion of vector animation. D-74, solid layers, was accepted by the owner the same day and B-23b builds it in the core: `verification/B-23b_solid_table.md` walks FX-SOL-001 to 029, 34 of 34. B-23c puts it in the window the same day: New solid and Ctrl+Y, the colour and size in the layer's panel, `verification/B-23c_panel_table.md` 18 of 18; the owner's playtest passed the same day ("B-23c playtest works") and Width and Height were made draggable at their word. D-76, whether a solid's colour and size can be keyed, is proposed and awaits the owner; nothing is built against it. B-24, masks (D-75), is the current work: B-24a is written the same day as D-77, the mask rule, with FX-MSK-001 to 030 in `Fixtures/masks/` and every case drawn in `verification/B-24a proposal/mask_cases.png`. The owner accepted D-77 on 2026-09-19 ("accept D-77, proceed with B-24b, along with shortcut for the pen tool") and B-24b, masks in the core, is built the same day: `verification/B-24b_mask_table.md`, 53 of 53. B-24c, masks in the window, is built on 2026-09-20: the pen on G, the rectangle and the ellipse on Q, the selection tool on V, the checkerboard moved to Alt+G to make room, points and handles dragged or moved with the arrow keys, a row a mask on the timeline and a block of settings a mask in the layer's panel, and `mask.add`, `mask.set_path`, `mask.set` and `mask.delete` in document 24's table. `verification/B-24c_panel_table.md` is 17 of 17 and it awaits the owner's playtest, `verification/B-24c_mask_playtest.md`. B-24d, a mask's path set moving, is built on 2026-09-20 against the same decision, which had stated the rule already: FX-MSK-031 to 035 first, drawn in `verification/B-24d keys/mask_key_cases.png`, then the core (`verification/B-24d_mask_key_table.md`, 39 of 39) and the window's stopwatch, key track and `mask.add_remove_key` (`verification/B-24d_panel_table.md`, 9 of 9); the old `PROJECT_FEATURE_UNSUPPORTED` warning on a keyed path is retired. It awaits the owner's playtest, `verification/B-24d_mask_playtest.md`**: the performance entries in document 15 are proposed and the owner accepts each by name. **B-19 is done on 2026-09-19:** all three playtests work, and the owner had FX-KIND-005 corrected, so `verification/B-19f_keykind_table.md` is 36 of 36. B-19e, the specification for separate X and Y and for auto, continuous and roving keys, is written the same day as D-69, with its fixtures, and accepted with it. **B-25, shape layers (D-75), is the current work.** B-25a, the rule and its fixtures, is written on 2026-09-20: D-78 in document 14, with FX-SHP-001 to 017 and 020 to 030 in `Fixtures/shapes/` and document 25, drawn in `verification/B-25a proposal/shape_cases.png` and `verification/B-25a proposal/shape_moving_case.png`. The owner accepted D-78 the same day ("accept D-78, proceed with B-25b"). **B-25b, the shape layer in the core, is built the same day**: `verification/B-25b_shape_table.md`, 51 of 51, every frame within 1e-6 of `tools/shape_reference.py`'s and every one of the eleven bad files refused whole. **B-25c, the shape layer in the window, is built on 2026-09-21**: `verification/B-25c_panel_table.md`, 17 of 17, and `verification/B-25c_shape_playtest.md` for the owner to play. The pen now shows as a pen over the picture. **B-24e, a mask's or shape's path key dragged along its row, eased with F9 and held with Ctrl+Alt+G, is built on 2026-09-22**: `verification/B-24e_panel_table.md`, 13 of 13, and `verification/B-24e_path_key_playtest.md` for the owner to play. **B-24f, a path key chosen, moved, deleted, copied, right-clicked and graphed like a property's key, and a released hold bringing its ease back, is built on 2026-09-22**: `verification/B-24f_panel_table.md`, 16 of 16, and `verification/B-24f_path_key_playtest.md`. **B-24c passed on 2026-09-22**, all fourteen steps, and the owner reported two things beside them: the picture lagged behind a dragged point, and a pressed point was not lit. **B-24g answers both on the same day**: the drag queue keeps one request in flight and drops the ones overtaken, and a point pressed, hovered or tabbed to is filled. `verification/B-24g_drag_playtest.md`. **B-24g was not enough, and the window said so rather than an argument**: the owner reported the drag still lagging, so the frame response now carries an `x-ms` header and the page splits a drag update four ways, and it answered **17750.1 ms inside the window for one Draft frame** against P-01's 2.502 ms for a warm draft frame with no mask. **P-15 is that, fixed, on 2026-09-22**: a mask's coverage is worked out a sample row at a time, an expanded mask drops the edges further away than its expansion before measuring any distance, both run across the thread pool, and the mask panel's three numbers follow the hand while they are dragged. `verification/B-06_mask_table.md` goes from 41 checks to 45 with **zero differing pixels** on all four new ones, and `verification/P-15_mask_cost.md` reads 30,567.8 ms to 6.33 for a plain 1080p mask and 269,706.1 to 32.68 for an expanded one. `verification/P-15_mask_drag_playtest.md` is the sheet. **B-24d passed on 2026-09-22**, all ten steps, on the build P-15 left; two of that sheet's own known limits were struck through with it, because B-24e and B-24f had lifted them. **B-24h is the other two, on the same day, at the owner's word ("also tackle said limits")**: a path with keys can now gain a point or lose one, because the point is cut into the base and into every key at once (D-79), which moves no key's shape and no shape between keys - checked against document 19's cubic written out a second time, to about 1e-14 of a pixel, with the drawn pixels moving by at most one of ADR-016's sixteen samples where flattening re-cuts a curve, and not one pixel on a straight segment; and a mask's history entry now says what changed instead of "Set mask of N points", worked out by comparing the list of masks before and after inside the core. `verification/B-24h_path_point_table.md` is 38 of 38, `verification/B-24h_panel_table.md` is the window's half, and `verification/B-24h_path_point_playtest.md` is the owner's sheet. **B-24e passed on 2026-09-23**, all ten steps, the owner answering "works", with step 7 read as B-24f rewrote it: a path key dropped on another is refused. **B-24f passed on 2026-09-23**, all ten steps including the graph, the owner answering "works". The owner asked for more of After Effects' mask editing the same day - several points chosen at once, a box dragged around points, and the bezier handles behaving as After Effects' do ("make it 1 to 1 with AE's mask ecosystem") - which is **D-80, accepted on 2026-09-23** ("proceed with what you personally recommend", against the agent's recommendation to take it whole, the rebinding included) and B-24i to B-24k in document 15. **B-24i and B-24j are built the same day**: a mask's points are chosen as a set - Shift and a click, a box dragged from the dark around the picture, a click on the outline for all of them - and moved, nudged and deleted together in one entry of history each, `path.remove_point` taking a comma list for that reason; and the bezier gestures are After Effects' - the pen pressed and dragged pulls a mirrored pair of handles, a smooth point's partner handle turns with the one being pulled, Alt breaks a pair, Alt converts a point, and adding and deleting a point move to the pen, which frees Alt to mean what it means there. **Whether a point is smooth is read from its handles, not stored**, so no file changed; **]** and **[** on a tabbed point are the keyboard's way to curve a path, which the window would otherwise have had none of. `verification/B-24h_panel_table.md` grew six rows for the comma list; `verification/B-24i_point_playtest.md` and `verification/B-24j_bezier_playtest.md` are the owner's sheets, and they are the **only** check on the page's own arithmetic, which no test here can run. `verification/B-24h_path_point_playtest.md` was rewritten for the rebinding before the owner played it. **B-24g, P-15, B-24h, B-24i and B-24j passed on 2026-09-23**, every step of each sheet, the owner answering "1 thru 5 pass". **B-25c passed the same day**, all fourteen steps, after the walk found two faults fixed on the spot: the pen pointer, which WebView2 never drew, is drawn by the page, and a shape layer, 12,976 ms a frame in the window, is drawn by P-15's method, identical pixel for pixel. B-25 is done. **B-22d passed the same day**: the owner exported the reference shot as a dithered GIF from the window ("I think it worked well for a gif") and passed the sheet's other two steps. B-22 is done. **D-76 was declined by the owner the same day** ("i agree with not keyframing it"): a solid's colour and size stay unkeyed, and Scale or a shape layer's keyed rectangle grows one over time. The owner chose the next work the same day ("let's begin with 1 then 2"): B-24k, the free transform box around a mask, then R-01, reading exposure timesheets. **D-81, the box, was accepted by the owner the same day ("proceed") and B-24k is built against it**: a double-click on a mask's outline or Ctrl+T puts After Effects' box round it, to move, scale and turn it, and **it passed the owner's playtest the same day** ("works; work on limits"). **D-81a and B-24l, the box's limits, followed the same day**: the box holds several masks, has keys, and Undo brings it back with the shape, while skew, perspective, a whole-layer box and a saved box stay out as in After Effects; a several-layer drag stopping short was found and fixed on the way. It awaits the owner's playtest, `verification/B-24l_box_limits_playtest.md`. **D-82, null layers, was proposed the same day as B-26a with its fixtures first** (FX-NULL-001 to 029, `tools/null_reference.py`), at the owner's word, and the owner accepted it ("proceed"); B-26b built the core the same day (`verification/B-26b_null_table.md`, 34 of 34), and B-26c put it in the window (`verification/B-26c_panel_table.md`, 10 of 10); the owner passed `verification/B-26c_null_playtest.md` ("it works"). **D-83, the pick whip, was proposed the same day as B-27a** with FX-WHIP-001 to 020 (`tools/pickwhip_reference.py`, `Fixtures/pickwhip/`), and the owner accepted it ("proceed"); B-27b built the core the same day (`verification/B-27b_pickwhip_table.md`, 109 of 109), and B-27c put it in the window the same day (`verification/B-27c_panel_table.md`, 22 of 22); the owner passed `verification/B-27c_pickwhip_playtest.md` ("works!"). R-01, reading XDTS exposure timesheets, is B-28: D-84 was accepted by the owner on 2026-09-24 ("cool, proceed"), with FX-XDTS-001 to 040 first (`Fixtures/xdts/expected_xdts.json`, from `tools/xdts_reference.py`), and amended the same day to carry in Clip Studio Paint's entries before frame 0 (FX-XDTS-028) after checking it against OpenToonz's reader. B-28b, the core, is built on 2026-09-24: `verification/B-28b_timesheet_table.md`, 60 of 60. B-28c, the window, is built the same day: `verification/B-28c_panel_table.md`, 15 of 15, and passed by the owner's playtest, `verification/B-28c_timesheet_playtest.md`, on 2026-09-24. B-28d, the Sheet view the owner asked for (D-84a, accepted as direction the same day), is built on 2026-09-24: `verification/B-28d_sheet_table.md`, 57 of 57, and passed by the owner's playtest, `verification/B-28d_sheet_playtest.md`, the same day ("nice, it works for both playtests"). B-28 is done. **B-29, workspaces, is next at the owner\'s word the same day** ("Slot workspaces", "Workspaces first"), ahead of typing into the sheet, dialogue and camera columns, and printing the sheet: D-85, accepted as direction, and B-29 built on 2026-09-24 and passed by the owner the same day ("perfect!"), `verification/B-29_workspace_playtest.md`. **D-84b, writing into the Sheet, is accepted the same day** ("proceed"), with FX-SHEET-001 to 016 in document 25, and B-28e built it the same day, and the owner passed its playtest the same day ("works"). **D-84c, dialogue and camera columns, is accepted the same day** ("proceed"), with FX-XDTS-019 and amended FX-XDTS-014, 034 and 040 in document 25; **B-28f is built the same day** (B-28b table 65 of 65, B-28f table 9 of 9) and the owner passed its playtest, `verification/B-28f_sheet_text_playtest.md`, the same day ("works"). **D-84d, printing the Sheet, is accepted the same day** ("proceed"), with FX-PRINT-001 to 004 in document 25; **B-28g is built the same day** (B-28g table 18 of 18). The owner looked at its second look, closer to a studio's timesheet, and answered "looks great! let's do 1 through 7" of the seven further steps the agent listed; its playtest's steps go into B-28h's, which prints the same things. **D-84e, the paper sheet laid out as a studio's, D-84f, the title block, and D-84g, the Action column and key drawings, are accepted the same day** ("proceed"), with their cases in document 25; they are to be built in that order as B-28h, B-28i and B-28j. **B-28h is built the same day** (B-28g table 42 of 42); its playtest is `verification/B-28h_sheet_paper_playtest.md`. The owner answered "proceed" to it, read as passed. **B-28i is built the same day** (B-28g table 49 of 49); its playtest is `verification/B-28i_title_block_playtest.md`, passed by the owner the same day ("works"). **B-28j's fixtures are in document 25 the same day**: FX-SHEET-017 to 024, FX-PRINT-011 and FX-XDTS-040's Sheet with its Action column.

Current task is B-02 in `Markdown/15_Initial_Backlog.md`: tagged image buffers, the linear-light premultiplied float32 working space and normal-over compositing on the CPU. Its artifact is `verification/B-02_fixture_table.md`.

## Document map

00 Start here. 01 Charter. 02 Workflows. 03 Requirements. 04 Scope and roadmap. 05 Interface specification. 06 Architecture. 07 Project and media format. 08 Rendering, color and performance overview. 09 Effects, parked with R-05. 10 Legal and licensing. 11 Verification plan. 12 Development operating guide, the verification protocol. 13 Delivery and capacity. 14 Decision and risk register. 15 Backlog. 16 Production tool research. 17 Evidence register. 18 ADR index. 19 Core data model. 20 Time and animation model. 21 Rendering math and the tile contract. 22 Reference shot. 23 Feature priority matrix. 24 Command and interaction map. 25 Test fixture catalog. 26 Undo and command model. 27 Cache model, parked with R-06b. 28 Error and diagnostics catalog. 29 Build and reproducibility. 30 Competitive analysis. 31 Anime workflow research.

Root files: `AGENTS.md` enforceable agent rules, `CLAUDE.md` working instructions, `CONTEXT.md` vocabulary, `HANDOFF.md` session context, `docs/adr/` full decision records, `Schemas/` project schema, `Fixtures/` fixture data and expected values, `design/` interface design work.

## Authority

The charter governs intent. Requirements govern observable behavior. ADRs and documents 19 through 21 govern implementation contracts. Document 12 governs what counts as evidence. Document 14 records changes.

A conflict between documents is a specification defect, resolved explicitly rather than by picking whichever prose is newer or easier to implement.

Markdown is the only source. Word review copies and the checksum manifest were removed in version 0.3; git history is the change record.

## Version 0.3 acceptance

The pack now contains closed decisions, a deliverable first milestone and a verification protocol suited to its actual reviewer. Remaining open items are weekly capacity, deliberately uncommitted, and the public product name, deliberately deferred.
