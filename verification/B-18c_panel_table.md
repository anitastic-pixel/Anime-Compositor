# B-18c: precompositions in the window

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

D-67 decided what a composition layer is and B-18b built it in the core, checked pixel by pixel in `verification/B-18b_precomp_table.md`. This is the window's half: Pre-compose and Ctrl+Shift+C send `layer.precompose` with the chosen layers, the button on a composition's row in the Project panel sends `layer.add_composition`, and double-clicking a composition layer sends `composition.open`.

Every row calls what the window calls, on `Fixtures/projects/cel_holds_project.json`, and reads back what the page is given.

## What this does not cover

What the layer looks like on the timeline and in the panels, and whether the picture stays the same when layers are pre-composed. That is `verification/B-18c_precomp_playtest.md`, for a person. Whether the pixels are right is B-18b's table.

| Check | Expected | Actual | Result |
|---|---|---|---|
| Pre-compose with nothing chosen says so | Pre-compose needs at least one layer of this composition chosen. The edit was not applied. Nothing in the project changed. | Pre-compose needs at least one layer of this composition chosen. The edit was not applied. Nothing in the project changed. | pass |
| Pre-compose on a layer whose child is not chosen is refused, with the reason | "Grade" and "Cel" are tied by a parent or a matte, so they are pre-composed together or not at all. Choose both. | "Grade" and "Cel" are tied by a parent or a matte, so they are pre-composed together or not at all. Choose both. | pass |
| and nothing moved | Cel, Grade | Cel, Grade | pass |
| Pre-compose on both says what happened | Precomp 1 holds the chosen layers now, and one layer shows it here. Double-click that layer to open it. Ctrl+Z puts them back. | Precomp 1 holds the chosen layers now, and one layer shows it here. Double-click that layer to open it. Ctrl+Z puts them back. | pass |
| one layer stands where the two were | Precomp 1 | Precomp 1 | pass |
| and the project has the new composition | Main, Precomp 1 | Main, Precomp 1 | pass |
| the page is told its kind and its composition, and it has no drawing | composition, a composition_id, no asset_id | composition, a composition_id, no asset_id | pass |
| it is centred: anchor and position at the centre of 1920 by 1080 | [960,540] and [960,540] | [960,540] and [960,540] | pass |
| deleting Precomp 1 while the layer shows it is refused, naming who uses it | This composition is shown by a layer of "Main", so it stays. Delete that layer first if this one should go. | This composition is shown by a layer of "Main", so it stays. Delete that layer first if this one should go. | pass |
| double-click opens Precomp 1, and the two layers are in it, in their order | Cel, Grade | Cel, Grade | pass |
| inside it, adding the outer composition as a layer is refused: it would hold itself | "Main" cannot be shown here, because it would end up inside itself. The edit was not applied. Nothing in the project changed. | "Main" cannot be shown here, because it would end up inside itself. The edit was not applied. Nothing in the project changed. | pass |
| the Project panel's button adds a second layer of Precomp 1, at the front | Precomp 1, Precomp 1 | Precomp 1, Precomp 1 | pass |
| Undo twice: the button's layer goes, then Pre-compose goes as one entry | Cel, Grade; Main | Cel, Grade; Main | pass |
| and Redo makes it again | Precomp 1; Main, Precomp 1 | Precomp 1; Main, Precomp 1 | pass |

**14 of 14 checks pass.**
