# B-26c: null layers in the window

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

D-82 decided what a null is and B-26b built it in the core, checked pixel by pixel in `verification/B-26b_null_table.md`. This is the window's half: the New null button, its line in the timeline's right-click menu and the command palette, and Ctrl+Alt+Shift+Y send `layer.add_null`; the picture is given each null's 100 by 100 square to outline, since the null itself is never drawn.

Every row calls what the window calls, on `Fixtures/projects/cel_holds_project.json`, and reads back what the page is given. That project's drawings are not on disk, so the drawn layer has no outline in these rows and the nulls do.

## What this does not cover

What the null looks like on the picture, the timeline and in the panels, and whether dragging its square moves it. That is `verification/B-26c_null_playtest.md`, for a person. Whether the frames are right is B-26b's table.

| Check | Expected | Actual | Result |
|---|---|---|---|
| New null (Ctrl+Alt+Shift+Y) needs no drawing chosen and adds Null 1 at the front | Cel, Null 1 | Cel, Null 1 | pass |
| the page is told it is a null, and it has no drawing | null, no asset_id, no exposure_spans | null, no asset_id, no exposure_spans | pass |
| it stands at the composition's centre, its anchor the middle of its 100 by 100 square, for the whole composition | [50,50] and [960,540], frames 0 to 5 | [50,50] and [960,540], frames 0 to 5 | pass |
| Undo says what it would take back | Add layer Null 1 | Add layer Null 1 | pass |
| the picture is given its square to outline, 100 by 100 about the centre | given, [910,490 1010,490 1010,590 910,590] | given, [910,490 1010,490 1010,590 910,590] | pass |
| a blend mode on the null is refused, with the reason | "Null 1" is a null, which is never drawn, so it has no picture for that to work on. | "Null 1" is a null, which is never drawn, so it has no picture for that to work on. | pass |
| the null as Cel's matte is refused, with the reason | "Null 1" is a null, which is never drawn, so it cannot be a matte. | "Null 1" is a null, which is never drawn, so it cannot be a matte. | pass |
| Null 2 is added at the front, and its outline comes after Null 1's as its row is above | Cel, Null 1, Null 2; after | Cel, Null 1, Null 2; after | pass |
| Null 2 parented to Null 1 stays where it was | 0,0 | 0,0 | pass |
| and moving Null 1 100 pixels right carries Null 2 100 pixels right | 100,0 | 100,0 | pass |

**10 of 10 checks pass.**
