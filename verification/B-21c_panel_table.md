# B-21c: GIF and animated PNG out of the window

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

D-72 added a GIF and an animated PNG as things an export can be, and `verification/B-21c_films_table.md` reads both files back frame by frame. This is the window's half: the list beside Export offers them, the job names one file rather than one a frame, and the status line says so. The folder picker is skipped, as in every panel table: no test can answer a Windows dialog.

| Check | Expected | Actual | Result |
|---|---|---|---|
| the list beside Export offers a GIF, and says it is a preview | true | true | pass |
| and an animated PNG | true | true | pass |
| gif: the file is named | cel_holds_project.gif | cel_holds_project.gif | pass |
| gif: three frames are one file, and the window says so | Exported 3 frames as one file, the chosen folder's cel_holds_project.gif. | Exported 3 frames as one file, the chosen folder's cel_holds_project.gif. | pass |
| apng: the file is named | cel_holds_project_animated.png | cel_holds_project_animated.png | pass |
| apng: three frames are one file, and the window says so | Exported 3 frames as one file, the chosen folder's cel_holds_project_animated.png. | Exported 3 frames as one file, the chosen folder's cel_holds_project_animated.png. | pass |
| the folder holds those two files and nothing else | cel_holds_project.gif, cel_holds_project_animated.png | cel_holds_project.gif, cel_holds_project_animated.png | pass |
| a cancelled GIF leaves no half-made file, and the window says so | Nothing was exported. Export stopped at your request. An animated file is whole or it is nothing, so no file was left.; file there: false | Nothing was exported. Export stopped at your request. An animated file is whole or it is nothing, so no file was left.; file there: false | pass |

**8 of 8 checks pass.**
