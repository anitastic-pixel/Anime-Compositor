# B-14c: expressions in the window

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

D-59 gave a property an expression and B-14b taught the core to work one out. This is the window's half: Alt-click on a property's diamond (or Alt+Shift+= on it) adds a box under the property with `value` in it, which changes nothing until something else is typed; the `=` beside the property's name switches it off and on; and a text the language cannot read is kept, with the reason in red under it.

Every row sends the request the page sends, to the same handler, and reads back what the page is given: the project for the text and the switch, and the frame's `/boxes` answer for the number the panel shows and the error beside it.

## What this does not cover

Whether the box is comfortable to type into, whether the red line is readable, and whether Alt-click feels like After Effects. That is `verification/B-14c_expressions_playtest.md`, for a person.

What an expression works out to in detail. That is `verification/B-14b_expression_table.md`, 184 of 184 against independently written numbers; this table only checks that the window shows the same answer.

## What changed for Alt-click

W-23 had Alt-click on the diamond take every key off the property. The owner asked on 2026-09-16 for After Effects' behaviour instead, so it now adds or removes the expression. Taking every key off is still two steps: press the property's name on the timeline, which chooses all its keys, then press Delete.

| Check | Expected | Actual | Result |
|---|---|---|---|
| Alt-click on the rotation diamond adds `value`, which changes nothing yet | {"enabled":true,"text":"value"} | {"enabled":true,"text":"value"} | pass |
| and the panel still reads the rotation it read before | 0, none | 0, none | pass |
| typing `time * 90` into the box makes frame 24 (one second) read 90 | 90, none | 90, none | pass |
| the switch turns it off, keeping the text, and the rotation is its own again | {"enabled":false,"text":"time * 90"}, 0 | {"enabled":false,"text":"time * 90"}, 0 | pass |
| undo switches it back on | 90 | 90 | pass |
| half-typed text is kept as typed, not refused | {"enabled":true,"text":"time *"} | {"enabled":true,"text":"time *"} | pass |
| and the error beside it names what is wrong, while the rotation falls back to its own | 0, EXPRESSION_SYNTAX | 0, EXPRESSION_SYNTAX | pass |
| a position expression gives a pair of numbers, the second one kept from the value | [120,0] | [120,0] | pass |
| Alt-click again removes the expression | null | null | pass |
| and undo brings it back | {"enabled":true,"text":"[120, value[1]]"} | {"enabled":true,"text":"[120, value[1]]"} | pass |
| the camera's lens takes an expression the same way | 1000, none | 1000, none | pass |
| and the camera's depth refuses a pair of numbers with the reason beside it | EXPRESSION_TYPE | EXPRESSION_TYPE | pass |
| a locked layer refuses an expression | null | null | pass |
| and the window says why, in words | The layer "Cel" is locked, so it was not changed. Unlock the layer to edit it. | The layer "Cel" is locked, so it was not changed. Unlock the layer to edit it. | pass |
| a property that cannot carry one is refused by name | true | true | pass |
| and the refusals put nothing in the history | 7 | 7 | pass |
| Save writes the text and the switch into the file | true | true | pass |
| and opening that file shows the same broken expression and its error | EXPRESSION_SYNTAX | EXPRESSION_SYNTAX | pass |

**18 of 18 checks pass.**
