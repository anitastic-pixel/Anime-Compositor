# B-15c: collecting and checking from the window

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

D-61 decided what a package is and B-15b built it, checked against the fixtures in `verification/B-15b_package_table.md`. This is the window's half: a "Can be passed on" tick on each drawing in the Project panel, and Collect Files and Check Package beside Save As.

Every row calls what the window calls, on `Fixtures/packaging/source/shot.json`, and reads back what the page is given. The folder picker Collect Files opens is the one step skipped: no test can answer a Windows dialog, so the rows start with the folder already chosen.

## What this does not cover

Whether the tick and the two buttons are where you expect them, and whether the package opens on another computer. That is `verification/B-15c_package_playtest.md`, for a person.

| Check | Expected | Actual | Result |
|---|---|---|---|
| the licensed sheet opens unticked and the cels ticked | unticked, ticked | unticked, ticked | pass |
| unticking the cels says so in the history's words | Keep asset-cel out of packages | Keep asset-cel out of packages | pass |
| and the cels are unticked, with unsaved changes | unticked, true | unticked, true | pass |
| undo ticks them again | ticked | ticked | pass |
| ticking the licensed sheet is one history item | Let asset-licensed be passed on; ticked | Let asset-licensed be passed on; ticked | pass |
| and undo unticks it | unticked | unticked | pass |
| a tick on a drawing that is not there is refused and changes nothing | refused, 0 undo items | refused, 0 undo items | pass |
| Collect Files says how many drawings were copied, missing and left out | Collected into (the chosen folder): 10 drawings copied. 1 drawing was missing, listed without a copy. 1 drawing was left out, marked not to be passed on. The open project is unchanged. | Collected into (the chosen folder): 10 drawings copied. 1 drawing was missing, listed without a copy. 1 drawing was left out, marked not to be passed on. The open project is unchanged. | pass |
| the manifest it wrote is FX-PACK-001's, to the byte | true | true | pass |
| and the window, its history and its unsaved state are as they were | true | true | pass |
| collecting into the same folder again is refused | Files were not collected: the chosen folder is not empty. Choose an empty folder, or a new one. | Files were not collected: the chosen folder is not empty. Choose an empty folder, or a new one. | pass |
| with the licensed sheet ticked, it is copied too | 11 drawings copied, nothing left out | 11 drawings copied, nothing left out | pass |
| Check Package on a project that is not a package names the manifest | PACKAGE_MANIFEST_INVALID | PACKAGE_MANIFEST_INVALID | pass |
| on the package, it counts each answer | Checked 12 files against the package manifest: 10 ok, 1 missing, 1 excluded. | Checked 12 files against the package manifest: 10 ok, 1 missing, 1 excluded. | pass |
| and lists the two that are not ok, by path, in the notes | media/asset-licensed/sheet.png, media/asset-gone/gone_0001.png | media/asset-licensed/sheet.png, media/asset-gone/gone_0001.png | pass |
| a cel changed by one byte is reported changed | Checked 12 files against the package manifest: 9 ok, 1 changed, 1 missing, 1 excluded. | Checked 12 files against the package manifest: 9 ok, 1 changed, 1 missing, 1 excluded. | pass |
| with its code in the note | true | true | pass |
| after Save As, the package beside the saved file is the one checked | Checked 12 files against the package manifest: 9 ok, 1 changed, 1 missing, 1 excluded. | Checked 12 files against the package manifest: 9 ok, 1 changed, 1 missing, 1 excluded. | pass |
| a project with no file has nothing beside it to check | This project has no file yet, so there is no package beside it to check. Open the project inside a collected folder first. | This project has no file yet, so there is no package beside it to check. Open the project inside a collected folder first. | pass |

**19 of 19 checks pass.**
