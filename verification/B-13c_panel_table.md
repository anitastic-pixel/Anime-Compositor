# B-13c: what the depth row and the camera block do

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

D-58 gives a composition a camera and a layer a depth, so that a pan across layers set at different distances parts the way it does under a rostrum: the near layer crosses the frame faster than the far one, from one camera move rather than a key on every layer. That the arithmetic is D-58's - one scale per layer, folded into document 21's step 4 so the picture is still resampled once - is checked against independently generated numbers in `verification/B-13c_camera_table.md` and is not repeated here. What is checked here is the part between the controls and that arithmetic: that the number typed is the number stored, that the millimetres a person reads are the pixels the file holds, and that a composition nobody has pointed a camera at still saves without one.

Two commands carried it when this was written. `camera.set_property` still carries all three of the camera's properties. `layer.set_depth` still puts a layer on a plane, but nothing in the window sends it any more: B-13d made a depth one of the properties the command layer takes by name on 2026-09-16, so the Depth row sends `property.set_base` like every other number in the inspector, and `layer.set_depth` is kept for the keep-place conversion parenting needs, which is not built. The rows below are the base values and are unchanged; what a keyed depth does is `verification/B-13d_depth_keys_table.md`.

## What to look at

- **A composition with no camera is shown the default, not blanks.** The panel fills the three boxes with the 50 mm lens on a 36 mm back that D-58 takes from After Effects, placed in the middle and pulled back by its own zoom, because that is what the renderer would use. Nothing is written until one of them is changed - the last row checks that undoing everything gives back a file with no camera in it at all, which is why a project made before the camera existed opens and saves unchanged.
- **The lens is millimetres here and pixels in the file.** D-22 puts a unit conversion at the boundary rather than in the middle, and this is a second boundary of the same kind: 50 mm on a 1920-wide composition is stored as 2666.67 pixels, and the panel converts it back so the person is shown the 50 they typed.
- **The depth was a typed box and is a scrub now.** It was typed here because a depth was not a property the core took by name and could not be keyed, so a control that dragged would have promised an animation this build could not write. B-13d removed the reason on 2026-09-16, after the owner played B-13c and asked for exactly this: a depth is a property like the five above it, it drags, and it keys.
- **A depth in front of the composition's plane is a negative number**, not a refusal. The plane the composition is drawn at is 0 and a layer may sit either side of it; what is refused is a layer at or behind the camera, which is `CAMERA_PLANE_BEHIND` and is shown in the other table.
- **A locked layer refuses a depth like any other edit**, in the same sentence, naming the rule.

## What this does not cover

The numbers. These rows check what the controls do to the project; where a layer at a depth actually lands is FX-CAM-001 to 011 in `verification/B-13c_camera_table.md`, worked from D-58 by a generator that never builds a matrix.

Animating the camera. Nothing in this window keys a camera property: its three boxes set the base and that is all. The camera fixture is keyed, and the renderer follows a keyed camera, so what is missing is the gesture and not the arithmetic. The depth is no longer in this paragraph - B-13d gave it the diamond on 2026-09-16, and `verification/B-13d_depth_keys_table.md` is where it is checked.

Dragging the camera in the picture. The camera is placed by typing, not by pulling it about in the viewer; W-04 asks for a camera move and not for a handle to make it with.

| Check | Expected | Actual | Result |
|---|---|---|---|
| the fixture's layer sits on the plane the composition is drawn at | null | null | pass |
| and the composition has no camera at all until one is touched | null | null | pass |
| setting a depth says which plane the layer was put on | Set depth to 640 | Set depth to 640 | pass |
| and the panels are given it back | 640 | 640 | pass |
| a depth in front of the composition's plane is a negative number, not a refusal | -200 | -200 | pass |
| undo puts back the depth that was replaced | 640 | 640 | pass |
| and undoing the first one puts the layer back on the composition's own plane | null | null | pass |
| a request with no depth in it is asked for one rather than given a default | How far back? A depth is a number of pixels, and 0 is the plane the composition is drawn at. | How far back? A depth is a number of pixels, and 0 is the plane the composition is drawn at. | pass |
| words are not a depth, and are quoted back | "six hundred" is not a depth. A depth is a number of pixels. | "six hundred" is not a depth. A depth is a number of pixels. | pass |
| no layer named at all is asked for | Which layer? Choose one in the layer list. | Which layer? Choose one in the layer list. | pass |
| none of those three refusals put anything in the history | 0 | 0 | pass |
| a locked layer refuses a depth, and says which rule stopped it | The layer "Cel" is locked, so it was not changed. Unlock the layer to edit it. | The layer "Cel" is locked, so it was not changed. Unlock the layer to edit it. | pass |
| which changed nothing | 1 | 1 | pass |
| touching the lens says which of the camera's properties was set | Set the camera's zoom | Set the camera's zoom | pass |
| the file holds the lens in pixels, not in millimetres | 2666.6666666666665 | 2666.6666666666665 | pass |
| and the panel shows back the 50 mm that was typed | 50 | 50 | pass |
| a camera that was not there is created by touching it, with the rest at its default | [960,540] | [960,540] | pass |
| including the depth, which is the default lens's own zoom, behind the frame | -2666.6666666666665 | -2666.6666666666665 | pass |
| placing the camera takes both numbers in one request | [760,540] | [760,540] | pass |
| and its depth is one | -1920 | -1920 | pass |
| undo takes back the camera one property at a time | [960,540] | [960,540] | pass |
| a request naming no property is asked which one | Which of the camera's properties? Its position, its depth or its zoom. | Which of the camera's properties? Its position, its depth or its zoom. | pass |
| a property the camera does not have is named back with the three it does | "tilt" is not one of the camera's properties. They are position, depth and zoom. | "tilt" is not one of the camera's properties. They are position, depth and zoom. | pass |
| a place needs both numbers, and says so with an example | The camera's position is two numbers, across and down: value=960,540. | The camera's position is two numbers, across and down: value=960,540. | pass |
| a lens is one number, and says what it is measured on | The camera's zoom is one number, a lens in millimetres on a 36 mm back: value=50. | The camera's zoom is one number, a lens in millimetres on a 36 mm back: value=50. | pass |
| words are not a number, and are quoted back | "wide" is not a number. | "wide" is not a number. | pass |
| none of those five refusals put anything in the history | 3 | 3 | pass |
| undoing everything gives back the file that was opened | identical, with no camera and no depth anywhere in it | identical, with no camera and no depth anywhere in it | pass |

**28 of 28 checks pass.**
