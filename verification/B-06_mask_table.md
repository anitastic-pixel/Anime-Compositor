# B-06 — the polygon mask and the alpha matte

**41 of 41 checks passed.**

Generated by `tests/b06_mask.rs`. Covers requirement R-04, document 19's mask and matte records, document 21 steps 2 and 5, and document 25's line 51.

## What to look at

A mask cuts a shape out of one layer. A matte uses a second layer's transparency to cut the first. This table is the arithmetic of both, on small squares where every number can be checked by hand. It is not a picture of the reference shot; that is a separate and later kind of check.

Four rows are worth reading on their own:

- **All 64 pixels agree with a rasterizer written a second time, a different way.** The build casts a ray and counts crossings. The check sums angles. They share no code. Agreement at every pixel of the field is the strongest thing this table says.
- **A mask and its inverse sum back to the unmasked pixel.** This is an identity rather than a number, so it holds at every coverage value including the partial ones along an edge, and it cannot be satisfied by a rasterizer that is wrong in a consistent way.
- **Masking a drawing handed out by the cache does not change what the cache holds.** Layers share drawings. If this were wrong, masking one layer would cut every other layer using the same artwork, and only when the cache happened to hit — a fault that would come and go with the memory budget and never show in a single-layer test.
- **A matte of the same alpha but a different colour gives the same answer.** Document 21 says a matte contributes its *alpha*. The matte in these rows is black, so a build that read its colour instead would give zero rather than a quarter.
- **A layer used as a matte stops being drawn only when a flag says so.** D-42. The seven matte-only rows set the flag, count the layers the frame actually draws, undo it, and save and reopen the project. A file that shows a layer switched off while that layer visibly shapes the picture would be a file nobody can read; this is the alternative, and these rows are the check that it survives the file.

## The rasterization rule, which document 25 line 51 asks B-06 to record

**A 4x4 ordered grid of sample points per pixel; coverage is the number of samples inside the polygon divided by sixteen; insideness is the even-odd rule.** The sample for grid cell (i, j) of pixel (x, y) sits at `(x + (i+0.5)/4, y + (j+0.5)/4)`, symmetric about the pixel centre document 21 puts at `(x+0.5, y+0.5)`.

The reference tool is the second implementation in the fixture itself, not a drawing library. A library would have brought its own fill rule and its own antialiasing, and a disagreement would have said nothing about whether this build is right.

Two consequences are stated rather than implied. **Coverage is an exact rational** — sixteen integer decisions over sixteen — so it does not depend on vertex order, summation order or the machine. And **an edge is accurate to one sixteenth**, which is this build's honest resolution. Document 21 requires that multisample details be fixture-tested before claiming subpixel equivalence, and nothing here claims it.

The even-odd choice is not load-bearing: even-odd and nonzero winding differ only on self-intersecting polygons, and document 19 rejects those. On every polygon this build accepts, the two rules give the same answer.

## What this does not cover

Subpixel edge goldens stay OPEN, as document 25 line 51 leaves them: this table checks topology exhaustively and the edge quantum exactly, and claims nothing finer. The matte rows drive the renderer directly rather than through a saved project, so they show the arithmetic and not the wiring; the wiring is covered where `compose` is. And no row here is a photograph — whether a mask looks right on your shot is a question only the picture can answer.

## Checks

| Check | Expected | Actual | Result |
|---|---|---|---|
| a pixel wholly inside the polygon is fully covered | `1.000000` | `1.000000` | pass |
| a pixel wholly outside the polygon is not covered at all | `0.000000` | `0.000000` | pass |
| the pixel just outside a boundary at an integer is not covered | `0.000000` | `0.000000` | pass |
| all 64 pixels agree with an independently written winding-number rasterizer | `0 disagreements` | `0 disagreements` | pass |
| and the largest coverage difference anywhere in the field is zero | `0.000000` | `0.000000` | pass |
| an edge halfway across a pixel covers exactly half of it | `0.500000` | `0.500000` | pass |
| an edge a quarter of the way across a pixel leaves three quarters covered | `0.750000` | `0.750000` | pass |
| an edge that lands between sample columns still covers a whole number of sixteenths | `0.500000` | `0.500000` | pass |
| a sample sitting exactly on a vertical edge counts as inside | `1.000000` | `1.000000` | pass |
| a sample sitting exactly on a horizontal edge counts as inside too | `1.000000` | `1.000000` | pass |
| the build's sample grid is the 4x4 ADR-016 records | `4 per side` | `4 per side` | pass |
| every coverage value under a diagonal edge is a whole number of sixteenths | `0 off the grid` | `0 off the grid` | pass |
| coverage multiplies all four premultiplied channels equally | `(0.200000, 0.100000, 0.050000, 0.250000)` | `(0.200000, 0.100000, 0.050000, 0.250000)` | pass |
| so the straight colour under a half-covered edge is the colour it started with | `0.800000` | `0.800000` | pass |
| a mask and its inverse sum back to the unmasked pixel everywhere | `0.000000` | `0.000000` | pass |
| a disabled mask leaves every pixel untouched | `(0.400000, 0.200000, 0.100000, 0.500000)` | `(0.400000, 0.200000, 0.100000, 0.500000)` | pass |
| a self-intersecting polygon is not simple | `false` | `false` | pass |
| an ordinary square is simple | `true` | `true` | pass |
| and a layer carrying one is drawn unmasked rather than blank | `(0.400000, 0.200000, 0.100000, 0.500000)` | `(0.400000, 0.200000, 0.100000, 0.500000)` | pass |
| a mask of two points is not drawn either | `(0.400000, 0.200000, 0.100000, 0.500000)` | `(0.400000, 0.200000, 0.100000, 0.500000)` | pass |
| a matte at alpha 0.25 scales the layer it mattes to a quarter | `(0.250000, 0.250000, 0.250000, 0.250000)` | `(0.250000, 0.250000, 0.250000, 0.250000)` | pass |
| a matte of the same alpha but a different colour gives the same result | `(0.250000, 0.250000, 0.250000, 0.250000)` | `(0.250000, 0.250000, 0.250000, 0.250000)` | pass |
| a fully transparent matte hides the layer | `(0.000000, 0.000000, 0.000000, 0.000000)` | `(0.000000, 0.000000, 0.000000, 0.000000)` | pass |
| a fully opaque matte leaves the layer alone | `(1.000000, 1.000000, 1.000000, 1.000000)` | `(1.000000, 1.000000, 1.000000, 1.000000)` | pass |
| the matte moves with its own transform, so the covered half is where the matte is | `hidden at x=0, shown at x=2: (0.000000, 0.000000, 0.000000, 0.000000) / (1.000000, 1.000000, 1.000000, 1.000000)` | `hidden at x=0, shown at x=2: (0.000000, 0.000000, 0.000000, 0.000000) / (1.000000, 1.000000, 1.000000, 1.000000)` | pass |
| both layers are drawn before either is used as a matte | `2` | `2` | pass |
| a matte layer is still drawn in its own right unless it is told not to be | `2` | `2` | pass |
| matte-only takes the matte layer out of the drawn stack | `1` | `1` | pass |
| and the layer that is left is the one that uses the matte, still matted | `a, matted` | `a, matted` | pass |
| undo puts the matte layer back in the stack | `2` | `2` | pass |
| matte-only is saved and comes back | `matte-only` | `matte-only` | pass |
| and the reopened project draws the same one layer | `1` | `1` | pass |
| the copy that was masked really was cut, so the check below is not vacuous | `(0.000000, 0.000000, 0.000000, 0.000000)` | `(0.000000, 0.000000, 0.000000, 0.000000)` | pass |
| masking a cel handed out by the cache does not change what the cache still holds | `(0.215861, 0.051269, 0.014444, 1.000000)` | `(0.215861, 0.051269, 0.014444, 1.000000)` | pass |
| and the second request was a hit, so it really was the cached copy | `1 hit` | `1 hit` | pass |
| a valid mask is accepted | `accepted` | `accepted` | pass |
| and the layer now carries it | `4 vertices` | `4 vertices` | pass |
| undo removes it and leaves no mask behind | `none` | `none` | pass |
| a self-intersecting mask is refused, for crossing itself | `MASK_INVALID_OUTLINE: The mask crosses itself, which this build does not draw.` | `MASK_INVALID_OUTLINE: The mask crosses itself, which this build does not draw.` | pass |
| and a refused command leaves no mask on the layer | `none` | `none` | pass |
| a mask of two points is refused, for having too few points | `MASK_INVALID_OUTLINE: A mask needs at least three points, and this one has 2.` | `MASK_INVALID_OUTLINE: A mask needs at least three points, and this one has 2.` | pass |
