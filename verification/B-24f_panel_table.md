# B-24f: a path key chosen, moved, deleted, pasted and graphed like a property's

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

B-24e let a path key be dragged and eased on its own. B-24f names a mask's path `mask:<n>` and a shape's `shape:<n>` wherever a request names a property, as an effect's setting is named `fx:`, so a path's key goes through the requests a property's keys already go through: chosen with other keys and moved with them, moved by the arrow keys, deleted, eased, held, copied and pasted, and drawn in the graph editor.

A path is not one number, so the graph draws how far its points have travelled: the mean distance its points move from one key to the next, added up key after key, and part of the way along a segment by where the renderer puts the path. The speed graph is then pixels a second, which is what After Effects draws for a path.

Every row calls what the window calls, on `Fixtures/projects/cel_holds_project.json`, and reads back what the page is given. The distances were worked out by hand, not read from the build.

## What this does not cover

Whether the marks light, follow the hand and open their menu, and whether the graph draws the line: `verification/B-24f_path_key_playtest.md`, for a person. So is letting a hold go bringing back the ease the key had, which the page remembers and this table cannot see; the table sends what the page sends then.

A path key's value is its shape, which is drawn, not typed or dragged up and down in the graph. Key speed and influence typed in the menu's dialog, and the three kinds of bezier, are a number's and are not offered for a path; its speed is pulled in the graph.

| Check | Expected | Actual | Result |
|---|---|---|---|
| a mask keyed at frame 0 and frame 8, and the layer's rotation keyed at frame 0 | frame 0 linear at x 100; frame 8 linear at x 300 / frame 0 | frame 0 linear at x 100; frame 8 linear at x 300 / frame 0 | pass |
| the graph of the path is how far its points have gone on average, in pixels: two of its four points move 200 pixels, so 100 in all, half of it by frame 4, none before the first key and all of it after the last | 0.000, 50.000, 100.000, 100.000 | 0.000, 50.000, 100.000, 100.000 | pass |
| the mask's key at 8 chosen with the rotation's key at 0, and both dragged two frames later, as one move | frame 0 linear at x 100; frame 10 linear at x 300 / frame 2 | frame 0 linear at x 100; frame 10 linear at x 300 / frame 2 | pass |
| one Undo puts both back | frame 0 linear at x 100; frame 8 linear at x 300 / frame 0 | frame 0 linear at x 100; frame 8 linear at x 300 / frame 0 | pass |
| a path key moved onto another key of the same path is refused, as a property's is, and nothing moves | refused: true; frame 0 linear at x 100; frame 8 linear at x 300 | refused: true; frame 0 linear at x 100; frame 8 linear at x 300 | pass |
| a key that is not there is refused, in words | This path has no key at frame 5. | This path has no key at frame 5. | pass |
| Delete on a chosen path key takes it off | frame 0 linear at x 100 | frame 0 linear at x 100 | pass |
| Delete on both keys stops the path, which keeps the shape the first of them had | no keys; x 100 | no keys; x 100 | pass |
| the diamond, sent with the path's name, sets a key holding the shape the path has there: half way | frame 0 linear at x 100; frame 4 linear at x 200; frame 8 linear at x 300 | frame 0 linear at x 100; frame 4 linear at x 200; frame 8 linear at x 300 | pass |
| F9 on a path key reaches it through the request it sends for a property's key | frame 0 ease 0.333,0.000,0.667,0.667 at x 100; frame 8 linear at x 300 | frame 0 ease 0.333,0.000,0.667,0.667 at x 100; frame 8 linear at x 300 | pass |
| and the graph follows the ease: on this curve time runs straight, so half way the path has gone 2 x 0.5 x 0.25 + 0.125 = 0.375 of its 100 pixels, worked out by hand | 0.000, 37.500, 100.000 | 0.000, 37.500, 100.000 | pass |
| Ctrl+Alt+G holds it: the graph stays flat until the next key | frame 0 hold at x 100; 0.000, 0.000, 100.000 | frame 0 hold at x 100; 0.000, 0.000, 100.000 | pass |
| a path key pasted at frame 12 holds the copied shape | frame 0 ease 0.333,0.000,0.667,0.667 at x 100; frame 8 linear at x 300; frame 12 linear at x 300 | frame 0 ease 0.333,0.000,0.667,0.667 at x 100; frame 8 linear at x 300; frame 12 linear at x 300 | pass |
| and one Undo takes the paste away | frame 0 ease 0.333,0.000,0.667,0.667 at x 100; frame 8 linear at x 300 | frame 0 ease 0.333,0.000,0.667,0.667 at x 100; frame 8 linear at x 300 | pass |
| a path that is not there is refused, in words | Cel has no mask 3. | Cel has no mask 3. | pass |
| a shape's key is moved and held the same way | frame 0 linear at x 100; frame 4 hold at x 300 | frame 0 linear at x 100; frame 4 hold at x 300 | pass |

**16 of 16 checks pass.**
