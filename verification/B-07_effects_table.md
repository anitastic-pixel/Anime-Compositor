# B-07 - the layer effect stack

**60 of 60 checks passed.**

Generated by `tests/b07_effects.rs`. Covers requirement R-05, document 21's "Effects" section, and document 25's fixtures FX-E-001, FX-E-002, FX-T-001, FX-T-002 and the single-pixel impulse.

## What to look at

An effect changes a layer's own pixels before the layer is moved, faded or composited. Three of them are in this build: exposure makes a layer brighter or darker by stops, tint pulls its colour toward another colour, and Gaussian blur softens it. Every number below comes from the arithmetic document 21 states, worked out before the code was run and written into the test beside each row.

Six rows are worth reading on their own:

- **The blur is checked against a kernel written twice.** The weights this build uses are compared against weights generated separately in the test from document 21's definition, and then pinned three further ways that do not depend on either piece of code: they sum to one, they are symmetric, and each one's ratio to the middle weight is the Gaussian curve. A box blur would pass the first two and fail the third.
- **A single bright pixel, blurred, is checked pixel by pixel.** Document 25 asks for exactly this test. All 1156 numbers in the result have to match the independent kernel's outer product, which is what the word "separable" claims and which nothing short of checking every pixel would establish.
- **Order matters, and the table shows it.** The same two effects in the two possible orders give two different pixels. A build that stored a stack and applied it in whatever order was convenient would pass every other row in this file.
- **A blur makes the layer bigger, and the picture must not move.** Document 21 requires the layer's bounds to expand by the kernel radius so a glow near the edge of a cel is not cut off in a straight line. The row that matters is the one after: the centre of the frame's alpha is in exactly the same place before and after, so the extra margin did not shift anything.
- **The edge of a cel fades rather than smearing.** When a blur reaches past the edge of the drawing, what it finds there is nothing, not another copy of the edge pixel. Most image libraries do the opposite by default, and the difference is a hard bright rim around every blurred cel. Two rows say so: an opaque square still holds exactly as much of itself after the blur as before, which repeating the edge would inflate, and the far corner of the enlarged layer is the one faint tap the arithmetic allows rather than a solid pixel.
- **The blurred frame is identical at six tile sizes.** The renderer divides a frame into tiles and works on them in parallel. A blur reads its neighbours, so a blur done inside a tile would show seams at the tile boundaries. In this build the effects run over the whole layer before the tiles are cut, and that row is six renders of the same frame at six tile sizes, all required to come out identical.

## What this does not cover

There is no user interface for effects yet: the commands exist and are checked here, but nothing puts them on screen. An unknown effect's survival through save and reopen is checked in `verification/B-09_persistence_table.md` against `Fixtures/projects/unknown_effect_project.json`; what this file adds is that such an effect is not drawn and says so, once per frame, which is what marks an export of it incomplete.

## Checks

| Check | Expected | Actual | Result |
|---|---|---|---|
| FX-E-001 exposure at zero stops leaves the pixel alone | `(0.500000, 0.250000, 0.125000, 0.500000)` | `(0.500000, 0.250000, 0.125000, 0.500000)` | pass |
| FX-E-002 exposure of one stop doubles RGB and leaves alpha | `(0.500000, 0.500000, 0.500000, 0.500000)` | `(0.500000, 0.500000, 0.500000, 0.500000)` | pass |
| exposure gives the same pixel whether the catalogue's RGB is read straight or premultiplied | `(0.250000, 0.100000, 0.400000, 0.500000)` | `(0.250000, 0.100000, 0.400000, 0.500000)` | pass |
| FX-T-001 a tint of amount zero leaves the pixel alone | `(0.500000, 0.250000, 0.125000, 0.500000)` | `(0.500000, 0.250000, 0.125000, 0.500000)` | pass |
| FX-T-002 a tint of amount one replaces the straight colour and keeps alpha | `(0.100000, 0.200000, 0.300000, 0.500000)` | `(0.100000, 0.200000, 0.300000, 0.500000)` | pass |
| a tint of amount one half lands exactly half way, in straight colour | `(0.300000, 0.100000, 0.150000, 0.500000)` | `(0.300000, 0.100000, 0.150000, 0.500000)` | pass |
| a full tint does not paint colour into a fully transparent pixel | `(0.000000, 0.000000, 0.000000, 0.000000)` | `(0.000000, 0.000000, 0.000000, 0.000000)` | pass |
| kernel radius is ceil(3*sigma), and sigma zero has none | `0->0, 0.1->1, 1->3, 2->6, 1.5->5` | `0->0, 0.1->1, 1->3, 2->6, 1.5->5` | pass |
| sigma 0.5: the kernel has 2*radius+1 weights | `5` | `5` | pass |
| sigma 0.5: the weights sum to one | `1.000000` | `1.000000` | pass |
| sigma 0.5: the weights are symmetric about the centre | `0` | `0` | pass |
| sigma 0.5: every weight's ratio to the centre is exp(-d^2/2*sigma^2) | `0` | `0` | pass |
| sigma 0.5: the weights match an independently written kernel | `0` | `0` | pass |
| sigma 1: the kernel has 2*radius+1 weights | `7` | `7` | pass |
| sigma 1: the weights sum to one | `1.000000` | `1.000000` | pass |
| sigma 1: the weights are symmetric about the centre | `0` | `0` | pass |
| sigma 1: every weight's ratio to the centre is exp(-d^2/2*sigma^2) | `0` | `0` | pass |
| sigma 1: the weights match an independently written kernel | `0` | `0` | pass |
| sigma 2.5: the kernel has 2*radius+1 weights | `17` | `17` | pass |
| sigma 2.5: the weights sum to one | `1.000000` | `1.000000` | pass |
| sigma 2.5: the weights are symmetric about the centre | `0` | `0` | pass |
| sigma 2.5: every weight's ratio to the centre is exp(-d^2/2*sigma^2) | `0` | `0` | pass |
| sigma 2.5: the weights match an independently written kernel | `0` | `0` | pass |
| the blurred buffer grows by the kernel radius on every side | `17x17, origin moved by (3, 3)` | `17x17, origin moved by (3, 3)` | pass |
| every pixel of the impulse response is w[i]*w[j] from the independent kernel | `0 of 1156 channels differ` | `0 of 1156 channels differ` | pass |
| and the largest disagreement anywhere in the response | `at most 1e-6` | `at most 1e-6` | pass |
| the blurred impulse still holds exactly the alpha it started with | `1.000000` | `1.000000` | pass |
| the response is symmetric about the impulse on both axes | `0` | `0` | pass |
| a blurred half-transparent red has the same straight colour everywhere: no fringe | `1 colour: (0.80000, 0.00000, 0.00000)` | `1 colour: (0.80000, 0.00000, 0.00000)` | pass |
| a sigma of zero changes neither the pixels nor the bounds | `4x4, (0, 0), (0.500000, 0.250000, 0.125000, 0.500000)` | `4x4, (0, 0), (0.500000, 0.250000, 0.125000, 0.500000)` | pass |
| blurring an empty image leaves it empty, with no NaN anywhere | `0` | `0` | pass |
| the middle of a large flat region is unchanged by a blur | `(0.500000, 0.250000, 0.125000, 0.500000)` | `(0.500000, 0.250000, 0.125000, 0.500000)` | pass |
| blurring an opaque 8x8 cel neither gains nor loses alpha | `64.0000` | `64.0000` | pass |
| the far corner of the expanded buffer is one kernel tap squared, not a repeated edge pixel | `0.000020` | `0.000020` | pass |
| exposure then tint | `(0.750000, 0.750000, 0.750000, 1.000000)` | `(0.750000, 0.750000, 0.750000, 1.000000)` | pass |
| tint then exposure is a different pixel | `(1.250000, 1.250000, 1.250000, 1.000000)` | `(1.250000, 1.250000, 1.250000, 1.000000)` | pass |
| two blurs in a stack expand the bounds twice and the offsets add | `23x23, (9, 9)` | `23x23, (9, 9)` | pass |
| a switched-off effect is not drawn and is not reported as a fault | `(0.250000, 0.250000, 0.250000, 1.000000), nothing reported` | `(0.250000, 0.250000, 0.250000, 1.000000), nothing reported` | pass |
| an unsupported effect is bypassed and says so | `(0.250000, 0.250000, 0.250000, 1.000000), vendor.future.effect not implemented` | `(0.250000, 0.250000, 0.250000, 1.000000), vendor.future.effect not implemented` | pass |
| an out-of-range parameter is bypassed by itself, and the rest of the stack still runs | `(0.500000, 0.500000, 0.500000, 1.000000), core.gaussian_blur invalid parameter` | `(0.500000, 0.500000, 0.500000, 1.000000), core.gaussian_blur invalid parameter` | pass |
| each effect declares its bounds expansion, and only the blur has one | `exposure 0, tint 0, blur(2) 6, unsupported 0` | `exposure 0, tint 0, blur(2) 6, unsupported 0` | pass |
| a blurred layer arrives at the renderer six pixels larger on each axis | `10x10` | `10x10` | pass |
| the blur does not move the picture: the alpha centroid is where it was | `(15.50, 15.50)` | `(15.50, 15.50)` | pass |
| and the frame really is blurred, not merely unmoved | `different` | `different` | pass |
| the blurred frame is the same at every tile size, including sizes that do not divide it | `0 of 6 tile sizes differ` | `0 of 6 tile sizes differ` | pass |
| a layer carrying an effect this build does not have says so once per frame | `1 EFFECT_UNSUPPORTED` | `1 EFFECT_UNSUPPORTED` | pass |
| effects are added in order | `one, two` | `one, two` | pass |
| an effect can be put at a chosen place in the stack | `first, one, two` | `first, one, two` | pass |
| undo puts the stack back exactly as it was | `one, two` | `one, two` | pass |
| a second effect with an instance ID already in use is refused | `COMMAND_INVALID_VALUE: This layer already has an effect called one.` | `COMMAND_INVALID_VALUE: This layer already has an effect called one.` | pass |
| a negative sigma is refused rather than clamped | `EFFECT_PARAMETER_INVALID: A Gaussian blur needs a sigma of zero or more, and this is -1.` | `EFFECT_PARAMETER_INVALID: A Gaussian blur needs a sigma of zero or more, and this is -1.` | pass |
| a tint amount above one is refused for the same reason | `EFFECT_PARAMETER_INVALID: A tint amount runs from 0 to 1, and this is 1.5.` | `EFFECT_PARAMETER_INVALID: A tint amount runs from 0 to 1, and this is 1.5.` | pass |
| and a refused command leaves the stack alone | `one, two` | `one, two` | pass |
| switching an effect off keeps it in the stack | `one, two; two is off` | `one, two; two is off` | pass |
| an effect's settings can be changed in place | `3 stops` | `3 stops` | pass |
| settings for a different kind of effect are refused | `COMMAND_INVALID_VALUE: Effect one is a core.exposure, and these are settings for a core.tint.` | `COMMAND_INVALID_VALUE: Effect one is a core.exposure, and these are settings for a core.tint.` | pass |
| a command naming an effect that is not there is refused | `COMMAND_TARGET_MISSING: There is no effect called nope on this layer.` | `COMMAND_TARGET_MISSING: There is no effect called nope on this layer.` | pass |
| an effect can be removed | `two` | `two` | pass |
| and undo puts it back where it was, not on the end | `one, two` | `one, two` | pass |
| the stack is saved and comes back in order, with its settings and its bypass flag | `one core.exposure 3 stops on, two core.gaussian_blur sigma 1 off` | `one core.exposure 3 stops on, two core.gaussian_blur sigma 1 off` | pass |
