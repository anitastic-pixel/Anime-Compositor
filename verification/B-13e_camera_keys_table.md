# B-13e: what a keyed camera does

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

The owner played `verification/B-13c_camera_playtest.md` on 2026-09-15 and asked for the camera's values to update as they change, to be keyframable, and to drag like every other changeable value. The first was already true. B-13d gave the depth the other two the next day and this is the camera, which is the last piece of D-58.

There is no new command behind it and no new contract. Document 24 line 91 said that once a command could name the camera as its target instead of a layer, every property command that already exists would reach it unchanged - a base, a key, a move, an ease, a path - and that is what the rows below are. Every request in them is a command that was already there, carrying `target=camera` where it usually carries a layer's id. The owner chose the shape when asked: key it where it is, with the camera staying a property of the composition rather than becoming a layer.

## What to look at

- **A camera nobody has pointed is keyed at the camera the renderer would have used.** The first two rows key a composition whose file holds no camera at all, and what gets written is D-58's default: the 50 mm lens on a 36 mm back, in the middle, pulled back by its own zoom. Pressing the diamond holds what the panel was already showing, which is what a stopwatch does everywhere else.
- **The lens crosses the wire in pixels.** `property.set_base` has always carried what the file holds - an opacity goes over it as nought to one - so a lens goes over it as the pixels D-58's arithmetic uses, and the millimetres a person reads are turned over at the control beside opacity's conversion. The row that sets 1920 and reads back 36 mm is that boundary in one line.
- **The second key writes itself**, as it does for every other animated property: changing a keyed camera value writes a key at the frame the page names rather than moving the base. The row after it checks the base was left alone, which is the half a person cannot see, and it is the reason the Camera block's three boxes no longer send `camera.set_property` - that command sets a base, and on a keyed camera it would have moved a number nothing is drawn from.
- **Only the place can carry handles.** A camera's place is a point that travels and its depth and lens are lengths, so D-53's handles reach one of the three and are refused by name on the other two.
- **Six refusals and none of them in the history**, which is document 26's rule and is checked in one row after them.
- **Undo takes the camera back out of the file.** The last row reopens the question B-13c answered for the panel: a project made before the camera existed is not given one by being looked at.

## What this does not cover

The numbers. Where a layer at a depth actually lands is D-58's arithmetic, checked against independently generated values in `verification/B-13c_camera_table.md`. A keyed camera is sampled at the frame and then goes through the same two lines, so keying changed nothing about it.

Easing a camera key. F9 reaches the camera's keys and `verification/D-52_ease_table.md` is where that machinery is checked; the hold row above is enough to show the camera reaches it. B-13e's notes said the graph editor reached it too, and that was wrong: the graph draws whichever layer is selected, and the camera is deliberately not in the layer list, so there is no way to point the graph at it. B-13f fixed F9 - which was sending a layer id for a camera key, and being refused - and left the graph editor as work the owner has not been asked about. Step 7 of the playtest sheet is where a person confirms the rest.

Whether animating a camera is comfortable, and whether the Camera group belongs at the top of the timeline. No table can say either, which is what `verification/B-13e_camera_keys_playtest.md` is for.

A camera that is a layer. The owner raised it as a question and chose to key the camera where it is instead, so the camera is deliberately not in the layer list: not in the parent chooser, not in the matte chooser, not selected by Select All and not deleted by Delete. D-58 names the camera-layer rig as a later contract and it stays one.

| Check | Expected | Actual | Result |
|---|---|---|---|
| a composition nobody has pointed a camera at has no camera keys |  |  | pass |
| the diamond puts a key where the playhead is, holding the lens already shown | 2666.6666666666665@0 linear | 2666.6666666666665@0 linear | pass |
| keying a camera that is not in the file writes the default one the panel showed | [960,540] | [960,540] | pass |
| changing the lens on another frame writes a second key rather than a base | 2666.6666666666665@0 linear, 1920@24 linear | 2666.6666666666665@0 linear, 1920@24 linear | pass |
| and the base it did not touch is still the base | 2666.6666666666665 | 2666.6666666666665 | pass |
| and the millimetres the panel reads from that base are the 50 mm it always showed | 50 | 50 | pass |
| a camera key is taken hold of and put down on another frame like any other key | 2666.6666666666665@0 linear, 1920@30 linear | 2666.6666666666665@0 linear, 1920@30 linear | pass |
| undo puts it back on the frame it came from | 2666.6666666666665@0 linear, 1920@24 linear | 2666.6666666666665@0 linear, 1920@24 linear | pass |
| a camera key holds rather than travels when it is told to | 2666.6666666666665@0 hold, 1920@24 linear | 2666.6666666666665@0 hold, 1920@24 linear | pass |
| the camera's place keys as a pair of numbers, as a layer's position does | [960,540]@0 linear | [960,540]@0 linear | pass |
| and takes motion path handles, from a route that names no property at all | [960,540]@0 linear path [-40,0,40,0] | [960,540]@0 linear path [-40,0,40,0] | pass |
| a depth nobody has keyed is still set as a base, which is W-10's rule | -1920 | -1920 | pass |
| a length is not a path, and is told so in the words of what was asked for | The camera's zoom is a number, not a path. Only its place can carry handles. | The camera's zoom is a number, not a path. Only its place can carry handles. | pass |
| a property the camera does not have is named back with the three it does | "tilt" is not one of the camera's properties. They are position, depth and zoom. | "tilt" is not one of the camera's properties. They are position, depth and zoom. | pass |
| a request naming no property is asked which one | Which of the camera's properties? Its position, its depth or its zoom. | Which of the camera's properties? Its position, its depth or its zoom. | pass |
| a keyed property needs the frame the value belongs to, and says which to send | The camera's zoom is keyframed, so a value belongs to a frame. Say frame=<frame>. | The camera's zoom is keyframed, so a value belongs to a frame. Say frame=<frame>. | pass |
| an interpolation the window does not have is quoted back with the three it does | An interpolation is hold, linear or ease. Not "wobble". | An interpolation is hold, linear or ease. Not "wobble". | pass |
| a command that only a layer has is refused by name rather than asking which layer | layer.rename changes a layer, not the camera. The camera takes a value, a keyframe, a move, an ease and a path. | layer.rename changes a layer, not the camera. The camera takes a value, a keyframe, a move, an ease and a path. | pass |
| none of those six refusals put anything in the history | 6 | 6 | pass |
| undoing everything takes the camera back out of a file that never had one | null | null | pass |

**20 of 20 checks pass.**
