# B-12a: relinking, and what is said before anything changes

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

W-02 is one sentence: relink "by explicit user choice; preserve layer identity, timing, masks and effects", and "present changed dimensions, frame range or alpha interpretation before applying the change". The presenting is the part that cannot be done by a core command, because it happens before there is a command, so it is built here and checked here. That relinking preserves layer identity once it is applied is the core's own behaviour and is checked in `verification/B-09_persistence_table.md`; this table checks the window's half.

Relinking is three requests rather than one, and that is the requirement rather than a convenience: choosing the drawings works out what would happen and says it, and a second request either agrees to it or drops it. Between the two, nothing about the project has moved - the rows below check the drawings, the history and the file rather than trusting the sentence that says so.

## What to look at

- **The proposal is on the frame.** The sentence a person is being asked to agree to travels back with the picture, like everything else this window says, so a panel offering a relink cannot outlive the proposal it describes. Cancel it, apply it, or open another project, and the panel goes with the next frame.
- **All three of W-02's changes are stated whether or not they changed.** The size is given even when it is the same, the range and the gaps are given in full, and the alpha reading is stated as carried over. "Unchanged" is an answer; silence is not.
- **What cannot be read is said to be unreadable.** Relinking is what a person does when the media has gone, so the usual case is that there is nothing to compare the new size against. That case says so rather than reporting the drawings as the same size.
- **A proposal belongs to one sequence.** Agreeing to it while another sequence is selected is refused, and the proposal is still there afterwards.
- **The gap survives.** Relinking to a sequence missing drawing 7 leaves drawing 7 missing, which document 28 renders as nothing.

## What this does not cover

The file dialog, as everywhere else in this window: a test has no hands to answer one, so every row begins at the selection a person made.

Masks. W-02 names them among what a relink preserves, and it does preserve them - relinking replaces one asset record and cannot reach a layer - but this build has no gesture for drawing a mask, so there is no mask here to photograph surviving one.

Relinking to a sequence in a different colour space. A PNG states neither the colour space nor the alpha mode, so nothing read off the new drawings could contradict the project; a format that states them is not in G1.

| Check | Expected | Actual | Result |
|---|---|---|---|
| relinking without naming a sequence asks which one | Which sequence should be relinked? Choose one in the media bin. | Which sequence should be relinked? Choose one in the media bin. | pass |
| naming a sequence but no drawings asks for the drawings themselves | Which drawings should the sequence point at? Choose the files themselves, not the folder they are in. | Which drawings should the sequence point at? Choose the files themselves, not the folder they are in. | pass |
| a sequence that is not in the project is refused | That media is not in this project. Nothing was changed. | That media is not in this project. Nothing was changed. | pass |
| and agreeing to a relink nobody proposed is refused | There is no relink waiting for that sequence. Choose the replacement drawings first, so that what would change can be read before it happens. | There is no relink waiting for that sequence. Choose the replacement drawings first, so that what would change can be read before it happens. | pass |
| choosing the replacement drawings answers with what relinking would do | Relinking "Cel" to layer3_%03d.png would give it 11 drawings, numbered 0 to 11, and drawing 7 would be missing. The drawings are 1920x1080. What it points at now cannot be read, so there is nothing to compare that with. The colour is read as sRGB with straight alpha, which is what this project already recorded: a PNG does not state either, so relinking does not change how the pixels are read. The layers using it keep their stacking, transforms, exposures, masks and effects. Nothing has changed yet. One drawing is missing from layer3_%03d.png: 7. Add the missing files to the folder and relink the sequence, or leave the gap if the hole is intended. | Relinking "Cel" to layer3_%03d.png would give it 11 drawings, numbered 0 to 11, and drawing 7 would be missing. The drawings are 1920x1080. What it points at now cannot be read, so there is nothing to compare that with. The colour is read as sRGB with straight alpha, which is what this project already recorded: a PNG does not state either, so relinking does not change how the pixels are read. The layers using it keep their stacking, transforms, exposures, masks and effects. Nothing has changed yet. One drawing is missing from layer3_%03d.png: 7. Add the missing files to the folder and relink the sequence, or leave the gap if the hole is intended. | pass |
| the panel offering it reads it off the frame, word for word | Relinking "Cel" to layer3_%03d.png would give it 11 drawings, numbered 0 to 11, and drawing 7 would be missing. The drawings are 1920x1080. What it points at now cannot be read, so there is nothing to compare that with. The colour is read as sRGB with straight alpha, which is what this project already recorded: a PNG does not state either, so relinking does not change how the pixels are read. The layers using it keep their stacking, transforms, exposures, masks and effects. Nothing has changed yet. One drawing is missing from layer3_%03d.png: 7. Add the missing files to the folder and relink the sequence, or leave the gap if the hole is intended. | Relinking "Cel" to layer3_%03d.png would give it 11 drawings, numbered 0 to 11, and drawing 7 would be missing. The drawings are 1920x1080. What it points at now cannot be read, so there is nothing to compare that with. The colour is read as sRGB with straight alpha, which is what this project already recorded: a PNG does not state either, so relinking does not change how the pixels are read. The layers using it keep their stacking, transforms, exposures, masks and effects. Nothing has changed yet. One drawing is missing from layer3_%03d.png: 7. Add the missing files to the folder and relink the sequence, or leave the gap if the hole is intended. | pass |
| and the frame says which sequence it is about | asset-cel | asset-cel | pass |
| the drawings the sequence points at have not moved | asset-cel "Cel" 2 drawings | asset-cel "Cel" 2 drawings | pass |
| there is nothing to undo, because nothing was done | 0 | 0 | pass |
| leaving it as it is says so | The relink was dropped. The sequence still points at the drawings it did. | The relink was dropped. The sequence still points at the drawings it did. | pass |
| the panel goes with it |  |  | pass |
| the sequence still points where it did | asset-cel "Cel" 2 drawings | asset-cel "Cel" 2 drawings | pass |
| and dropping a second time has nothing to drop | There is no relink waiting to be dropped. | There is no relink waiting to be dropped. | pass |
| a proposal about one sequence cannot be applied to another | There is no relink waiting for that sequence. Choose the replacement drawings first, so that what would change can be read before it happens. | There is no relink waiting for that sequence. Choose the replacement drawings first, so that what would change can be read before it happens. | pass |
| and the proposal it was not about is still waiting | asset-cel | asset-cel | pass |
| agreeing names what was done, so that undo can be recognised | Relink Cel | Relink Cel | pass |
| the sequence now points at the drawings that were chosen | asset-cel "Cel" 11 drawings | asset-cel "Cel" 11 drawings | pass |
| the gap is still a gap: the drawing that is not there was not invented | (no such drawing) | (no such drawing) | pass |
| the layer that used it is the same layer, holding the same drawings for the same frames | 0-2:1, 2-5:2 | 0-2:1, 2-5:2 | pass |
| the panel is gone, because there is nothing left to agree to |  |  | pass |
| and it is one thing to undo | Undone: Relink Cel | Undone: Relink Cel | pass |
| which puts the drawings back | asset-cel "Cel" 2 drawings | asset-cel "Cel" 2 drawings | pass |
| relinking a sequence that can be read says what size it is now and what size it would become | The drawings are 8x8, where the ones it points at now are 1920x1080. | The drawings are 8x8, where the ones it points at now are 1920x1080. | pass |
| and says the alpha is read the way this project already reads it, because a PNG does not say | The colour is read as sRGB with straight alpha, which is what this project already recorded: a PNG does not state either, so relinking does not change how the pixels are read. | The colour is read as sRGB with straight alpha, which is what this project already recorded: a PNG does not state either, so relinking does not change how the pixels are read. | pass |

**24 of 24 checks pass.**
