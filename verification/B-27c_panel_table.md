# B-27c: the pick whip in the window

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

D-83 decided what the pick whip writes and B-27b built it in the core, checked against FX-WHIP-001 to 020 in `verification/B-27b_pickwhip_table.md`. This is the window's half: the spiral beside an expression sends `property.link` when it is let go on another property's row, and the spiral on a layer's row or beside the Parent list sends `layer.set_parent` when it is let go on another layer's row.

Every row sends what the page sends, on `Fixtures/pickwhip/pickwhip_project.json`, and reads back the text the property was given. The expected text is `Fixtures/pickwhip/expected_pickwhip.json`'s, written by `tools/pickwhip_reference.py`.

## What this does not cover

That the line follows the hand, that the right row lights and the label says the right thing, and that Escape or letting go elsewhere does nothing. That is `verification/B-27c_pickwhip_playtest.md`, for a person. Whether the linked values are right is B-27b's table.

| Check | Expected | Actual | Result |
|---|---|---|---|
| FX-WHIP-001: layer-target position dropped on null-1 position, one step of history | `thisComp.layer("null-1").transform.position`, 1 step | `thisComp.layer("null-1").transform.position`, 1 step | pass |
| FX-WHIP-002: layer-target rotation dropped on null-1 rotation, one step of history | `thisComp.layer("null-1").transform.rotation`, 1 step | `thisComp.layer("null-1").transform.rotation`, 1 step | pass |
| FX-WHIP-003: layer-target rotation dropped on null-1 position, one step of history | `thisComp.layer("null-1").transform.position[0]`, 1 step | `thisComp.layer("null-1").transform.position[0]`, 1 step | pass |
| FX-WHIP-004: layer-target scale dropped on null-1 rotation, one step of history | `temp = thisComp.layer("null-1").transform.rotation; [temp, temp]`, 1 step | `temp = thisComp.layer("null-1").transform.rotation; [temp, temp]`, 1 step | pass |
| FX-WHIP-005: layer-target opacity dropped on null-1 opacity, one step of history | `thisComp.layer("null-1").transform.opacity`, 1 step | `thisComp.layer("null-1").transform.opacity`, 1 step | pass |
| FX-WHIP-006: layer-target position dropped on null-1 anchor, one step of history | `thisComp.layer("null-1").transform.anchorPoint`, 1 step | `thisComp.layer("null-1").transform.anchorPoint`, 1 step | pass |
| FX-WHIP-007: layer-target scale dropped on null-1 scale, one step of history | `thisComp.layer("null-1").transform.scale`, 1 step | `thisComp.layer("null-1").transform.scale`, 1 step | pass |
| FX-WHIP-008: layer-target position dropped on camera position, one step of history | `thisComp.activeCamera.position`, 1 step | `thisComp.activeCamera.position`, 1 step | pass |
| FX-WHIP-009: camera depth dropped on null-1 rotation, one step of history | `thisComp.layer("null-1").transform.rotation`, 1 step | `thisComp.layer("null-1").transform.rotation`, 1 step | pass |
| FX-WHIP-010: null-1 position dropped on layer-trail position, one step of history | `thisComp.layer("layer-trail").transform.position`, 1 step | `thisComp.layer("layer-trail").transform.position`, 1 step | pass |
| FX-WHIP-011: layer-shake position dropped on null-1 position, one step of history | `thisComp.layer("null-1").transform.position`, 1 step | `thisComp.layer("null-1").transform.position`, 1 step | pass |
| FX-WHIP-012: layer-target position dropped on camera depth, one step of history | `temp = thisComp.activeCamera.depth; [temp, temp]`, 1 step | `temp = thisComp.activeCamera.depth; [temp, temp]`, 1 step | pass |
| FX-WHIP-013: layer-target position dropped on null-1 position, one step of history | `thisComp.layer("null-1").transform.position`, 1 step | `thisComp.layer("null-1").transform.position`, 1 step | pass |
| FX-WHIP-013: null-1 position dropped on layer-target position, one step of history | `thisComp.layer("layer-target").transform.position`, 1 step | `thisComp.layer("layer-target").transform.position`, 1 step | pass |
| Undo names the drop as it names a typed expression | Set the position expression | Set the position expression | pass |
| and Undo takes the drop back | (no expression) | (no expression) | pass |
| FX-WHIP-020: Target's Position dropped on its own Position: nothing is written. | A property cannot be linked to itself.; nothing changed | A property cannot be linked to itself.; nothing changed | pass |
| a separated half is not a place to drop, and says so | An expression cannot read position_x, so nothing can link to it. | An expression cannot read position_x, so nothing can link to it. | pass |
| nor is a property that does not exist | "wobble" is not a property that can be linked. | "wobble" is not a property that can be linked. | pass |
| the parent whip sends what the Parent list sends | both: /layer.set_parent?layer=…&parent=…&frame=frame | both: /layer.set_parent?layer=…&parent=…&frame=frame | pass |
| Target whipped onto Null 1 rides on it | null-1 | null-1 | pass |
| and Null 1 whipped back onto Target is refused as a loop | true | true | pass |

**22 of 22 checks pass.**
