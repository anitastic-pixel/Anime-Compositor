# B-28g: printing the Sheet

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

D-84d prints the Sheet as a paper timesheet through the system's print dialog, which can also save a PDF. The window writes the whole printable page from the Sheet's own cells: six seconds a page as two halves of three, a header on each, empty rows past the cut's end and a heavy line under its last frame.

Each FX-PRINT case is read from document 25 itself, set up in the window, and checked against the page the window writes: its header, its halves page by page, its columns and its end line. Every printed cell is then compared with the cell the Sheet shows on screen. The printable pages for FX-PRINT-001 to 003 are kept beside this table as `B-28g_sheet_print_040.html`, `B-28g_sheet_print_300_frames.html` and `B-28g_sheet_print_30_fps.html`, to open in any browser.

## What this does not cover

What the paper looks like, whether the print dialog opens, and whether a PDF saved from it holds the pages. That is `verification/B-28g_sheet_print_playtest.md`, for a person.

| Check | Expected | Actual | Result |
|---|---|---|---|
| document 25 has four cases | 4 | 4 | pass |
| FX-PRINT-001: the header of the first page (and the last) | s01 c012, Sheet 1 of 1, 2 + 0, 24 fps | s01 c012, Sheet 1 of 1, 2 + 0, 24 fps | pass |
| FX-PRINT-001: the halves, page by page, left then right | 0 to 71, 72 to 143 | 0 to 71, 72 to 143 | pass |
| FX-PRINT-001: each half's columns, left to right | frame, Dialogue, A, B, C, Camera | frame, Dialogue, A, B, C, Camera | pass |
| FX-PRINT-001: the heavy line under the last frame | 47 | 47 | pass |
| FX-PRINT-001: every printed cell is the Sheet's own (720 cells, empty past the end) | none differ | none differ | pass |
| FX-PRINT-002: the header of the first page (and the last) | s01 c012, Sheet 1 of 3, Sheet 3 of 3, 12 + 12, 24 fps | s01 c012, Sheet 1 of 3, Sheet 3 of 3, 12 + 12, 24 fps | pass |
| FX-PRINT-002: the halves, page by page, left then right | 0 to 71, 72 to 143, 144 to 215, 216 to 287, 288 to 359, 360 to 431 | 0 to 71, 72 to 143, 144 to 215, 216 to 287, 288 to 359, 360 to 431 | pass |
| FX-PRINT-002: the heavy line under the last frame | 299 | 299 | pass |
| FX-PRINT-002: every printed cell is the Sheet's own (2160 cells, empty past the end) | none differ | none differ | pass |
| FX-PRINT-003: the header of the first page (and the last) | s01 c012, Sheet 1 of 1, 1 + 18, 30 fps | s01 c012, Sheet 1 of 1, 1 + 18, 30 fps | pass |
| FX-PRINT-003: the halves, page by page, left then right | 0 to 89, 90 to 179 | 0 to 89, 90 to 179 | pass |
| FX-PRINT-003: the heavy line under the last frame | 47 | 47 | pass |
| FX-PRINT-003: every printed cell is the Sheet's own (900 cells, empty past the end) | none differ | none differ | pass |
| FX-PRINT-004: `/sheet/print` writes no pages | 0 pages | 0 pages | pass |
| FX-PRINT-004: the printable page and Ctrl+P say why | There is nothing to print: no layer here shows drawings. / There is nothing to print: no layer here shows drawings. | There is nothing to print: no layer here shows drawings. / There is nothing to print: no layer here shows drawings. | pass |
| FX-PRINT-004: Print... is not offered with no drawing columns | hidden | hidden | pass |
| the page prints what the window writes, and Ctrl+P asks for it | present | present | pass |

**18 of 18 checks pass.**
