# B-04b — rate-limiting repeated frame-level warnings

**28 of 28 checks passed.**

Generated by `tests/b04b_frame_log.rs`. Covers the logging rule of document 28: "Repeated frame-level warnings should be rate-limited while retaining counts/ranges."

## What to look at

The reference shot has one drawing deliberately missing from layer 3. That layer holds each drawing for two frames, so over the shot's 240 frames the missing one is asked for twenty times, and every one of those raises the same warning. Twenty copies of one message is how a real problem gets buried.

The first rows walk the shot frame by frame and show what the log looks like after rate-limiting: **the first three occurrences in full, then one summary saying twenty frames were affected and naming which ones** — 14 to 15, 38 to 39, and so on through 230 to 231. Nothing is thrown away. The summary carries the same identifier, the same severity and the same suggested fix as the warning it stands for, so it is still actionable on its own.

Two rows are worth reading beyond that:

- **The ranges name every affected frame and no others.** Twenty frames go in, twenty come back out of the summary's own text. A rate limiter that quietly lost the suppressed frames would be worse than no rate limiter, because the log would then under-report a real defect.
- **Two different problems in one shot stay apart.** The grouping rows record two subjects, and two identifiers, into one log and require four records out, not two. If everything collapsed into one summary, a missing drawing and a failed decode would become one indistinguishable line.

## What this does not cover

Nothing in this build walks a composition's frames in production — that loop is B-08's work, and until it exists this collector is a facility with no installed caller. The test drives the walk itself, over the real reference shot, which shows the behaviour is right but not that it is switched on.

The number three, and the choice to log the first few in full rather than only a summary, are not in document 28. They are registered as **D-25** in `Markdown/14_Decisions_Risks.md` and are marked PROVISIONAL, awaiting a ruling.

## Checks

| Check | Expected | Actual | Result |
|---|---|---|---|
| reference shot: frames that raise the warning at all | `14,15,38,39,62,63,86,87,110,111,134,135,158,159,182,183,206,207,230,231` | `14,15,38,39,62,63,86,87,110,111,134,135,158,159,182,183,206,207,230,231` | pass |
| reference shot: how many times it is raised | `20` | `20` | pass |
| reference shot: records in the rate-limited log | `4` | `4` | pass |
| reference shot: the three logged in full are the first three occurrences | `Frame 14 exposes drawing 7 / Frame 15 exposes drawing 7 / Frame 38 exposes drawing 7` | `Frame 14 exposes drawing 7 / Frame 15 exposes drawing 7 / Frame 38 exposes drawing 7` | pass |
| reference shot: the first logged record is the diagnostic itself, unaltered | `true` | `true` | pass |
| reference shot: the summary states the total | `layer3 drawing 7: 20 frames affected. The first 3 are logged in full above.` | `layer3 drawing 7: 20 frames affected. The first 3 are logged in full above.` | pass |
| reference shot: the summary states the ranges and how many were suppressed | `Frames 14 to 15, 38 to 39, 62 to 63, 86 to 87, 110 to 111, 134 to 135, 158 to 159, 182 to 183, 206 to 207, 230 to 231. 17 further identical warnings were not logged individually.` | `Frames 14 to 15, 38 to 39, 62 to 63, 86 to 87, 110 to 111, 134 to 135, 158 to 159, 182 to 183, 206 to 207, 230 to 231. 17 further identical warnings were not logged individually.` | pass |
| reference shot: the ranges name every affected frame and no others | `20` | `20` | pass |
| reference shot: the summary keeps the identifier it summarises | `MEDIA_SEQUENCE_GAP` | `MEDIA_SEQUENCE_GAP` | pass |
| reference shot: the summary keeps the severity | `WARNING` | `WARNING` | pass |
| reference shot: the summary keeps the remediation of the first occurrence | `Add the missing file and relink the sequence, or change the exposure to a drawing that exists.` | `Add the missing file and relink the sequence, or change the exposure to a drawing that exists.` | pass |
| a group at the limit is logged in full and summarised not at all | `3` | `3` | pass |
| one occurrence past the limit produces one summary | `4` | `4` | pass |
| and the summary says one was suppressed, over frames 0 to 3 | `Frames 0 to 3. 1 further identical warnings were not logged individually.` | `Frames 0 to 3. 1 further identical warnings were not logged individually.` | pass |
| a limit of zero logs nothing in full and keeps the count and ranges anyway | `1 / Frames 0 to 4. 5 further identical warnings were not logged individually.` | `1 / Frames 0 to 4. 5 further identical warnings were not logged individually.` | pass |
| two subjects under one identifier stay apart: two logged, two summaries | `4` | `4` | pass |
| and each summary counts only its own subject's frames | `layer3 drawing 7: 3 frames affected. The first 1 are logged in full above. / layer4 drawing 2: 3 frames affected. The first 1 are logged in full above.` | `layer3 drawing 7: 3 frames affected. The first 1 are logged in full above. / layer4 drawing 2: 3 frames affected. The first 1 are logged in full above.` | pass |
| two identifiers under one subject stay apart: two logged, two summaries | `4` | `4` | pass |
| and the summaries carry the two identifiers, not one twice | `MEDIA_MISSING / MEDIA_DECODE_FAILED` | `MEDIA_MISSING / MEDIA_DECODE_FAILED` | pass |
| records stay in the order they were recorded, summaries after them | `MEDIA_MISSING f0 / MEDIA_DECODE_FAILED f0 / MEDIA_MISSING sum / MEDIA_DECODE_FAILED sum` | `MEDIA_MISSING f0 / MEDIA_DECODE_FAILED f0 / MEDIA_MISSING sum / MEDIA_DECODE_FAILED sum` | pass |
| a quiet group beside a noisy one is not summarised | `3 / noisy: 4 frames affected. The first 1 are logged in full above.` | `3 / noisy: 4 frames affected. The first 1 are logged in full above.` | pass |
| ranges: two runs and a singleton | `14 to 15, 38 to 39, 41` | `14 to 15, 38 to 39, 41` | pass |
| ranges: one frame | `7` | `7` | pass |
| ranges: one unbroken run | `0 to 4` | `0 to 4` | pass |
| ranges: out of order and repeated input still reads as one honest set | `3 to 5, 9` | `3 to 5, 9` | pass |
| ranges: a run that crosses zero is still legible | `-2 to 1` | `-2 to 1` | pass |
| ranges: a gap of one frame is a gap, not a run | `1, 3` | `1, 3` | pass |
| ranges: nothing | `` | `` | pass |
