# B-12a: what the layer panel does

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

This is the window's half of the layer commands in document 24. The core's half - that adding, deleting, renaming and reordering a layer are correct and undoable - is checked in `verification/B-05_model_table.md` and is not repeated here. What is checked here is the part between a row in a list and that core, which is where a window goes wrong: a button that names a layer no longer there, a new layer given an identifier something else is already using, a toggle that decides what a layer currently is from a list drawn a minute ago, a refusal that leaves a control springing back with nothing said.

The fixture is `Fixtures/projects/unknown_effect_project.json`, which names an effect no version of this build has. It is used for every row on purpose: a panel is a second reader of the project, and a second reader is a second chance to lose what it cannot model. The last row is the one that would catch that - after every edit above and every undo of them, what a save would write is compared against the file that was opened, byte for byte.

## What to look at

- **A refusal is a sentence, never silence.** Six rows here are commands that were turned down, and each one is checked for the words the person would read - which drawing, which layer, and that the front layer is already at the front.
- **A refused command changes nothing.** Document 26 requires it, and the rows after each refusal check the history is no deeper and the layers are in the same order.
- **Undo names what it takes back.** "Undone" on its own would be true and useless.
- **Unsaved work is a comparison, not a flag.** Undoing every edit back to the file makes the window say the project is saved again, because the core compares the document against the last save rather than counting edits.

## What this does not cover

The page. Every row here calls the same function the window's URL scheme calls, with the same text the page would put in it, so what is checked is everything from the request inwards. That a button is wired to the right request, that the list is drawn front-first, and that the keyboard reaches all of it are in the photographs beside this table, not in it.

The transform and the effect stack are edited from the same inspector and are checked in `verification/B-12a_transform_table.md` and `verification/B-12a_effects_table.md`. The matte and the mask are shown and cannot yet be changed; that is later work and is named as missing rather than quietly absent.

| Check | Expected | Actual | Result |
|---|---|---|---|
| the project the panels draw is the project a save would write | true | true | pass |
| an effect this build does not have reaches the panels rather than vanishing | true | true | pass |
| the panels are told which composition is on screen | comp-main | comp-main | pass |
| a project just opened has nothing to undo | 0 to undo, 0 to redo | 0 to undo, 0 to redo | pass |
| adding a layer without saying which drawing is refused, and asks | Which drawing should the new layer show? | Which drawing should the new layer show? | pass |
| a drawing that is not in the project is refused by name | There is no imported drawing called asset-nothing. | There is no imported drawing called asset-nothing. | pass |
| neither refusal put anything in the undo history | 0 | 0 | pass |
| a new layer is added in front of the ones already there | Cel, Shadow | Cel, Shadow | pass |
| its identifier is not one the project was already using | layer-cel, layer-1 | layer-cel, layer-1 | pass |
| it covers the whole composition, which is five frames from zero | 0 to 5 | 0 to 5 | pass |
| a second new layer gets an identifier of its own | layer-cel, layer-1, layer-2 | layer-cel, layer-1, layer-2 | pass |
| renaming with no name is refused | A layer needs a name. | A layer needs a name. | pass |
| renaming changes the name and nothing else | Cel, Cast shadow, Highlight | Cel, Cast shadow, Highlight | pass |
| a name in Japanese survives the trip through the query string | 影 | 影 | pass |
| hiding a visible layer switches it off | false | false | pass |
| and pressing it again switches it back on | true | true | pass |
| locking a layer locks it | true | true | pass |
| and pressing it again unlocks it | false | false | pass |
| moving a layer forward puts it one place nearer the front | Cel, Highlight, 影 | Cel, Highlight, 影 | pass |
| moving it back puts it where it was | Cel, 影, Highlight | Cel, 影, Highlight | pass |
| the front layer cannot go further forward, and is told so by name | Highlight is already at the front. | Highlight is already at the front. | pass |
| the back layer cannot go further back, and is told so by name | Cel is already at the back. | Cel is already at the back. | pass |
| neither refusal put an entry in the history that would undo nothing | Cel, 影, Highlight | Cel, 影, Highlight | pass |
| a command naming a layer that is not there is refused by name | layer-gone is not a layer in this composition. | layer-gone is not a layer in this composition. | pass |
| a command naming no layer at all asks which one | Which layer? Choose one in the layer list. | Which layer? Choose one in the layer list. | pass |
| deleting a layer removes it | Cel, Highlight | Cel, Highlight | pass |
| and the two refusals before it left the history exactly as deep as it was | 11 | 11 | pass |
| the Undo button is told what it would take back | Delete layer layer-1 | Delete layer layer-1 | pass |
| undoing says what it took back | Undone: Delete layer layer-1 | Undone: Delete layer layer-1 | pass |
| and the layer is back where it was, with the name it had | Cel, 影, Highlight | Cel, 影, Highlight | pass |
| redoing says what it put back | Redone: Delete layer layer-1 | Redone: Delete layer layer-1 | pass |
| and the layer is gone again | Cel, Highlight | Cel, Highlight | pass |
| a document that has been edited is unsaved work | true | true | pass |
| undoing back to the file makes it not unsaved work again | false | false | pass |
| and the composition is the one that was opened | Cel | Cel | pass |
| there is nothing left to undo, and it says so | There is nothing to undo. | There is nothing to undo. | pass |
| after all of the above, a save would write the file that was opened | identical, including the effect this build cannot model | identical, including the effect this build cannot model | pass |

**37 of 37 checks pass.**
