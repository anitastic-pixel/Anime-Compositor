# B-13b: what the parent chooser does

Generated by `app/src/main.rs`, module `editing`. Re-run with `cargo test --workspace`.

D-57 lets a layer ride on another layer's transform, so that a mouth cel moves with the head cel it belongs to rather than being keyed twice. That the arithmetic is document 21's - the parent's whole transform applied after the child's own, up the chain - is checked against independently generated numbers in `verification/B-13b_parenting_table.md` and is not repeated here. What is checked here is the part between a chooser in a panel and that arithmetic: that the layer chosen is the layer used, that the arrangements D-57 forbids are refused in a sentence, and that the two things this window owes a person at the moment they happen - a shape that could not be kept exactly, and a parent being deleted - are said rather than left to be found in the picture.

One command carries choosing a parent, changing it and clearing it. `layer.set_parent` is added to document 24 for it, and it is the only layer command that carries the frame, because document 21 works the keep-place conversion at one frame and on an animated parent the answer differs from frame to frame.

## What to look at

- **The layer keeps where it is on screen.** Choosing a parent is a statement about what moves with what, not an instruction to move the layer, so the values are converted through the parent's chain and the layer stays put. That is After Effects' default and it is what the rows above check the answers of.
- **When the shape cannot be kept, the window says so rather than hiding it.** A parent stretched more one way than the other, under a layer that is turned, asks for a slant no layer can hold; the anchor lands exactly where it was and the rest of the layer moves. The change is allowed and a sentence says what happened, with Ctrl+Z named in it.
- **Deleting a parent is the one place parenting does not copy the matte.** A deleted matte leaves its reference behind, because the layer it shaped can wait to be undone without moving. A child pointing at a deleted parent would jump instead, so the children let go where they stand - and in the same entry, so that one undo brings back the layer and everything that was riding on it.
- **A loop is refused, twice over.** The chooser does not offer a layer itself, and the core refuses the whole loop of which that is only the shortest; the rows here go through the request the chooser sends, so what they check is the second line of defence.
- **Clearing is the same command with no layer named**, which is what the "none" entry in the list sends. A layer that is not in this composition is a different thing and is refused.

## What this does not cover

The numbers. These rows check what the chooser does to the project; where a parented layer's pixels actually land is FX-PARENT-001 to 008 in `verification/B-13b_parenting_table.md`, worked from document 21's four steps by a generator that never builds a matrix.

Parenting without keeping place. After Effects offers it on a modifier key; D-57 leaves it undecided and nothing in W-04 asks for it, so this window always keeps place.

The camera. B-13c is a separate contract, and parenting does not imply it.

| Check | Expected | Actual | Result |
|---|---|---|---|
| the fixture's layer rides on nothing | null | null | pass |
| choosing a parent says which layer this one now rides on | Set parent to layer-1 | Set parent to layer-1 | pass |
| and the panels are given it back | "layer-1" | "layer-1" | pass |
| a parent is not a matte, and choosing one shaped the layer by nothing | null | null | pass |
| clearing the parent says so | Clear parent | Clear parent | pass |
| and the layer rides on nothing again | null | null | pass |
| undo puts back the parent that was cleared | "layer-1" | "layer-1" | pass |
| a layer that is not in this composition cannot be a parent, and is named | The layer chosen as a parent, layer-gone, is not in this composition. | The layer chosen as a parent, layer-gone, is not in this composition. | pass |
| a layer cannot ride on itself | That parent would make two layers ride on each other. Choose a layer that is not already riding on this one. | That parent would make two layers ride on each other. Choose a layer that is not already riding on this one. | pass |
| and two layers cannot ride on each other | That parent would make two layers ride on each other. Choose a layer that is not already riding on this one. | That parent would make two layers ride on each other. Choose a layer that is not already riding on this one. | pass |
| a request that does not say which frame is refused rather than assuming one | Which frame? Say frame=<frame>. | Which frame? Say frame=<frame>. | pass |
| no layer named at all is asked for | Which layer? Choose one in the layer list. | Which layer? Choose one in the layer list. | pass |
| none of those five refusals put anything in the history | 2 | 2 | pass |
| and the parent is the one that was chosen | "layer-1" | "layer-1" | pass |
| a locked layer refuses a parent, and says which rule stopped it | The layer "Cel" is locked, so it was not changed. Unlock the layer to edit it. | The layer "Cel" is locked, so it was not changed. Unlock the layer to edit it. | pass |
| which changed nothing | 3 | 3 | pass |
| a parent scaled evenly keeps the layer exactly, and the answer says only what it did | Set parent to layer-1 | Set parent to layer-1 | pass |
| a parent stretched one way, under a turned layer, says the shape could not be kept | Set parent to layer-1 It could not keep its shape exactly: the parent is stretched more one way than the other and something in the chain is turned, so the anchor is where it was and the rest of the layer has moved. Ctrl+Z undoes it. | Set parent to layer-1 It could not keep its shape exactly: the parent is stretched more one way than the other and something in the chain is turned, so the anchor is where it was and the rest of the layer has moved. Ctrl+Z undoes it. | pass |
| and it did happen: the sentence is a warning about a change that landed | "layer-1" | "layer-1" | pass |
| a locked layer riding on it stops the deletion rather than being left behind | The layer "Cel" is locked, so it was not changed. Unlock the layer to edit it. | The layer "Cel" is locked, so it was not changed. Unlock the layer to edit it. | pass |
| and neither layer went anywhere | Cel, Head | Cel, Head | pass |
| deleting a layer lets go of what was riding on it | Clear parent and 1 more | Clear parent and 1 more | pass |
| the layer that was riding on it is still here, riding on nothing | null | null | pass |
| and letting go and deleting are one entry in the history, not two | 12 | 12 | pass |
| so one undo brings the layer back | Cel, Head | Cel, Head | pass |
| with the layer riding on it again | "layer-1" | "layer-1" | pass |
| undoing everything gives back the file that was opened | identical, including the effect this build cannot model | identical, including the effect this build cannot model | pass |

**27 of 27 checks pass.**
