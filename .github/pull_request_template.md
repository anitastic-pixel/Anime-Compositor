## What changed

<!-- One or two sentences. Which backlog item (B-nn) and requirement (R-nn) this closes. -->

## Verification artifact

<!-- Required. The thing the owner judges: fixture table (expected vs actual, full precision),
     screenshots, exported frames, trace images, timing table with machine and build recorded.
     A PR with no artifact is not ready, regardless of test status. See document 12. -->

## What was NOT run

<!-- Required. Tests skipped, platforms untested, numbers estimated rather than measured. -->

---

- [ ] No expected value in `Fixtures/` or document 25 was changed by this PR.
      (A fixture change is a specification proposal: separate PR, `spec-change` label, approved before code depends on it.)
